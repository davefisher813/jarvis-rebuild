# STATE.md - JARVIS Track 3 Rebuild

Continuity file (RULE 13). Re-read this first at the start of any session. STATE.md wins if it disagrees with chat memory.

## Phase
Visual Phase.

## Current step
Visual Phase. STEP 3 is BLOCKED until a COMPLETE FEATURE INVENTORY is produced and confirmed by Dave (new FULL FEATURE COVERAGE rule). Icon/scrim RULE 9 token proposal is PAUSED, resumes after inventory confirmation.

## Files maintained
- manifest.md - v1, 398 atomic rules (305 design-system / 85 convention / 8 HIG), 379 M / 19 V, band 300-700 PASS. Mutate only via RULE 9.
- step0-package.md - 0C sample rules, 0D coverage map, 0E reference notes, 0F screen inventory, 0G ambiguity list, 0H batch groupings, 0I deferred log.
- STATE.md - this file.
- uniformity.css - NOT yet created (STEP 2).

## Sources confirmed read
- jarvis-design-system.css - full (378 lines). Visual truth, used as-is.
- JARVIS_UI_Samples_v2.html - present, dark + light blocks. Locked visual reference.
- Catalog - 25 files; screen inventory extracted (0F). Content/structure only; styling + real org names + sentence-case titles superseded.
- v347-WORKING.html absent (not in project). v348-unified.html present (assembly ref, not deep-read). JARVIS_Today_Light_Realistic.html present (light Today ref). Handoff May16 file absent (Dave: proceed without it).

## Approved batches
None.

## Manifest version
v1.

## Locked demo dataset
Not yet locked (STEP 1).

## STEP 1 locked decisions
- Naming: "Schedule" everywhere (tab label + screen title). Overrides the catalog's "Calendar" tab label per Dave's explicit instruction (Dave's word > catalog).
- Tab bar (final): Today · Tasks · Schedule · Brain · More.
- More tab = hub for everything NOT on the bottom bar: Messages, Goals, Life Map, Themes, Settings, Account (exact list finalized from catalog when the More batch is built).
- Demo dataset: APPROVED as proposed. demo-data.md is now LOCKED. Every screen draws from it.

- RULE 4 reading (LOCKED by Dave): uniformity.css is production-only, real app rules only, no demo/catalog chrome. The zoo carries its catalog scaffolding locally and clearly separated (like v2). "Only design system + uniformity" governs the rendered components; each must trace strictly to those two files.
- uniformity.css bumped to v1.1: added G5 category-color utility classes (.cat-bg-*, .cat-fg-*) so avatars/dots/bars/icons/fills apply --cat-* without inline styles. Surfaced by the zoo build.

- STEP 2.5 Component Zoo: APPROVED by Dave. Machine gate clean, visual confirmed.
- Icon decision (LOCKED): STEP 3 screen mockups use real Lucide glyphs (inline SVG), not placeholder boxes. Production = Lucide React (CONV 11, stroke 1.5).

- RULE 18 (Testing mandatory, BFFSA bridge-app method) [LOCKED, effective now]: Every feature ships with an interactive test harness. The harness lists every path, state, and edge case as numbered steps ("tap each step in order"); each step states its expected result; tapping a step runs the REAL behavior (not a mock) and shows live captured console output; each step shows a visible PASS or FAIL. A feature is not done until every step passes. The harness lives behind a TEST_MODE flag, OFF at Milestone B (define Milestone B when milestones are finalized).
- FULL FEATURE COVERAGE [LOCKED, effective now]: The catalog is a design reference, not a complete feature list. A confirmed COMPLETE FEATURE INVENTORY governs scope. It is produced before STEP 3 and confirmed by Dave via widget (Dave names anything missing, e.g. Music). Every feature in the confirmed inventory MUST be built and MUST ship with a passing RULE 18 harness.

## Zoo build additions to uniformity (v1.2)
- G5 .pill-bffsa (inverting tier pill) and G6 .cat-bg-*/.cat-fg-* category utilities. Token-clean, existing tokens only. Surfaced during zoo build per RULE 4.
- Open RULE 9 suggestion: dark on-color for light-fill category surfaces (money yellow, friends mint) for contrast.

## Open items (for STEP 1)
- G1: exact tab order + what lives under More.
- G2: voice "Tap to speak" affordance placement (RULE 17, needs Dave).
- G3: onboarding screen sequence.
- G4: confirm Calendar tab label vs Schedule screen.
- Build + lock the neutral demo dataset (6 personas, times, projects, messages, tasks, events, life-map areas, brain entries).

## Last decision locked
Proceed through STEP 0 multi-part; proceed without the May16 handoff file (Dave taps).


## Persistence anomaly (logged this session)
- manifest.md on disk was found replaced by a foreign 148-row file at start of the 0C turn (different schema: Category column, M/V tags, codes COL/CAT/MSG/SUG/URG). STATE.md was intact and is mine.
- Per RULE 13 (STATE.md wins), rebuilt manifest.md to the recorded v1 (472 rules, 0 em dashes, 454 MACHINE / 18 VISUAL). Foreign file backed up as manifest_ondisk_148_backup.md.
- RISK: file persistence between turns appears unreliable. If a file is missing/changed at the next session start, re-verify against STATE.md before building on it.

## Next action
Component Zoo (STEP 2.5) built: zoo.html (preview, both themes), zoo-test-panel.html, zoo-audit.md. uniformity bumped to v1.2 (added .pill-bffsa G5, .cat-bg/.cat-fg G6 during zoo build per RULE 4). Machine gate clean (0 FAIL/0 UNVERIFIED). Awaiting Dave's VISUAL confirmation + zoo approval. Then STEP 3 batch 1.
## Visual review fixes (zoo, this session) + RULE 9 design-system amendment
Dave reviewed rendered zoo on device, flagged 3 issues. All resolved:
1. Schedule rows had no category color (SYS-001/004/005 not enforced). Added category color via .cat-dot leading each event (Apple Calendar list-view convention, matches Life Map dots; Dave confirmed dot over rail per RULE 17): Team Standup=cat-brain, Lunch With Maya=cat-tucci, Marathon=cat-health. Token-clean inline (mockup-allowed). Note: brain #5E9BFF and tucci #5AC8FA both read blue; accurate to data, not a bug. (.cat-bar chrome utility left defined but unused, available for future timeline/block views.)
2. Completion check glyph was top-left in its circle. .task-check now display:inline-flex + center + color:#fff + font-size var(--t-meta).
3. Filter chips read as ovals/balloons. ROOT CAUSE: canonical jarvis-design-system.css line 371 forced min-height:var(--tap-min)=44px onto .chip (touch target applied to visible pill). v2 reference (line 244) has no such rule; iOS HIG ~28-32px; design-system's own .hit44 comment says extend touch "without changing look".

RULE 9 AMENDMENT (Dave approved: "whatever is best and most like iOS"):
- jarvis-design-system.css: removed `.chip` from the line-371 min-height:44px selector group. Chip now renders at natural v2 height (~28px capsule).
- Markup: all chips now carry `.hit44` to preserve 44pt touch target via the pseudo-element (no visible inflation).
- Scope: chips ONLY. .segmented .seg / nav buttons / stepper / btn-sm still in the 44px group, unchanged.
- A working editable copy now lives at /mnt/user-data/outputs/jarvis-design-system.css (project /mnt/project copy is read-only). This working copy is the rebuild truth going forward; it diverges from the project source by exactly this one-line chip change. Future zoo/screen builds must embed THIS working copy, not /mnt/project.
- Applied identically in: jarvis-design-system.css (working), zoo.html embed, zoo-test-panel.html embed.

Machine gate after fixes: 0 em dashes (all files), chip removed from 44px group in all 3 files, all 5 chip instances carry hit44, prior fixes intact. Still 0 FAIL / 0 UNVERIFIED on machine checks.

## STEP 3 Batch 1 built (Shell + Nav + Global States)
- Zoo approved by Dave ("Zoo passes, start STEP 3"). STEP 3 unblocked.
- LOCKED DECISION (icons): mockup icons = provisional Lucide line glyphs, inlined (offline-safe), stand-in for SF Symbols. Final iOS app ships SF Symbols. Dave deferred to recommendation. Applies to ALL batches.
- Artifacts: step3-batch1.html (preview, both themes), step3-batch1-test-panel.html (toggles: theme/state/tab/large-title/toast), step3-batch1-audit.md.
- Rendered: shell device frame with each global state in context (loaded/loading/empty/error/offline), nav variants (large expanded, collapsed sticky inline, back+action, icon action), tab bar states (+badge), toasts x4, sync banners x4.
- Built from working jarvis-design-system.css (chip fix carried) + uniformity v1.2 + local chrome. Machine gate clean: 0 undefined classes, 0 raw inline, 0 em dashes, smart punctuation in copy, tab order locked.
- A prior stale zoo-batch1.* set existed (unfixed design-system, assumed uniformity v1.3, Lucide) from an abandoned attempt; NOT used. New files use step3-batch1.* names.
- STATUS: awaiting Dave's visual confirmation + batch approval. Next batch (2) = Today screen.

## Batch 1 fix: base text color in multi-theme mockup
- Dave caught black title text on dark cards. Cause: design-system sets base color via `html,body { color: var(--tx-1) }`, but --tx-1 only resolves under [data-theme], which the mockup sets on the per-theme PANE, not body. Default-color text (row titles, .nav-large, .nav-title carry no explicit color) inherited black.
- Fix: set `color: var(--tx-1)` on the themed container (.theme-pane in preview, #stage in test panel) so default text resolves per theme. Subtitles/meta already set --tx-3 explicitly and were fine.
- This is a mockup-structure fix (two themes in one document); the real app sets data-theme on the root so body color works directly. No design-system change.

## STEP 3 Batch 2 built (Today screen)
- Artifacts: step3-batch2.html (preview both themes), step3-batch2-test-panel.html (theme + loaded/loading/empty), step3-batch2-audit.md.
- Sections (order): Smart Start, Snapshot, Up Next, Your Day, Active Projects, Life Map, Recent Messages, From Your Brain, JARVIS Suggests, Voice bar. All content from LOCKED demo-data.md.
- uniformity bumped to v1.3: G7 snapshot tiles (.snapshot/.stat-tile/.stat-num/.stat-label), G8 voice bar (.voice-bar/.voice-mic/.voice-name/.voice-hint). Token-clean.
- Machine gate clean: 0 undefined classes, 0 raw inline, 0 em dashes, smart quotes, tab order locked, chip fix carried.
- OPEN FLAGS raised to Dave on this screen: (1) voice bar PLACEMENT provisional (G2/RULE 17); (2) money-yellow proj-icon "Q" low-contrast = the open RULE 9 contrast issue, now visible; (3) Active Projects has no per-project counts (none in demo-data, not invented); (4) Smart Start brief + Up Next countdown + snapshot counts are derived from demo-data, not literal fields.
- STATUS: awaiting Dave's visual confirmation + batch approval. Next batch (3) = Tasks + Schedule.

## Batch 2 universal-rule sweep + contrast resolution (Dave: "apply all rules to everything")
- Dave flagged that universal rules were applied unevenly. Did a full sweep on Today and fixed:
  - 44pt hit targets: .hit44 on "See all"/"View" links.
  - Truncation: .truncate on message name, life-map name, project tag (universal single-line rule).
  - Title Case minor word: "Time to Focus".
  - Alignment: large title gets .screen-pad so its left edge aligns with card group (was s-4 vs cards s-5).
  - Safe insets: voice bar uses .screen-pad.
- CONTRAST RESOLVED (was repeatedly deferred): added RULE 9 token --on-fill-dark: #000000 to design-system :root (theme-constant; --cat-bffsa-on was wrong because it inverts per theme). uniformity .cat-bg-money/.cat-bg-friends now use var(--on-fill-dark). Readable glyphs on yellow/mint in BOTH themes. Open zoo contrast item is now CLOSED.
- Voice bar placement committed (pill above tab bar, catalog-derived); stop flagging as provisional.
- design-system working copy now diverges from /mnt/project by TWO documented changes: (1) chip out of 44px group, (2) --on-fill-dark token. uniformity at v1.3.

## STANDING RULE (locked): systemic category color on EVERY category-bearing item
Dave flagged twice that items were left grey. From now, EVERY screen: any row/item/tile that belongs to a category MUST show its category color (cat-dot, proj-icon fill, avatar, etc.). Default ON. Do not ship a grey category list. Applied retroactively to Today: JARVIS Suggests rows now have cat-dots (family/money/brain). Check this on every future batch before presenting.

## Batch 2: color on non-row containers (Dave flagged Smart Start was pure grey)
- Smart Start card: eyebrow "SMART START" now accent red; each brief line has a leading category dot (money/money/brain) matching the items it references.
- Snapshot stat numbers now use --tint (theme accent), not grey.
- From Your Brain eyebrow now cat-brain blue.
- Color applied via dots/accent/blue only (theme-safe); avoided money-yellow/friends-mint as TEXT (light-theme contrast). Standing rule reinforced: NO grey containers; color every block.

## Batch 2: Smart Start readability rebuild (Dave: "it all blends, doesn't look iOS")
- Top of Today restructured (Dave): hero large title = "Good morning, Dave." (date as tight subtitle), "Today" title removed; the brief section header is now "Focus" (Dave picked over Today/Priorities). Brief rows = item primary + trailing grey time + sentence-case category subtitle + leading category dot + dividers.

## Batch 2 bugfix: message avatars were colorless
- Cause: token typo, msgs tuple stored "cat-tucci" then code prefixed "--cat-" -> var(--cat-cat-tucci) (undefined) -> no fill.
- Fix: tuple now bare ("tucci"/"brain"/"family") -> var(--cat-tucci) etc. Avatars colored. Swept whole file: no other undefined --cat- tokens.
- Note for Dave: urgency labels (EOD/2H/WAITING) are colored caps by the urgency rule (BDG-006, color=severity); neutral times stay grey by design. Distinct on purpose.

## Batch 2 fix: Life Map dot/bar color mismatch
- Was dot=category, bar=status -> two color systems in one row (pink dot + green bar). Dave flagged mismatch.
- Fix (Apple Activity-rings model): dot AND bar both = category color (fixed per area); status shown by the colored status WORD (Active/Drifting/Slipping) + the fill length. Dot and bar now match per row.

## Batch 2 bugfix: test panel light theme broken
- Cause: panel CSS was built by line-filtering CHROME, which truncated multi-line rules (e.g., .device lost its `background: var(--bg)` continuation line) -> device had no bg -> fell through to a dark layer -> dark-on-dark section headers in light theme.
- Fix: panel now embeds full CHROME intact + PANEL_CHROME overrides last. Preview file was never affected (uses full CHROME). Lesson: never line-filter multi-line CSS.

## STEP 3 Batch 3 built (Tasks + Schedule)
- Artifacts: step3-batch3.html (both screens, both themes), step3-batch3-test-panel.html (theme + screen toggle, tap-to-complete), step3-batch3-audit.md.
- TASKS: filter chips (capsule + hit44: All/Today/Upcoming/Done), grouped sections Today/Upcoming/Anytime, task rows = category-tinted check circle (iOS Reminders) + Title-Case title + optional project subtitle + trailing when (urgency caps text or grey date).
- SCHEDULE: segmented Day/Week/Month, date subheader, day timeline with category dots + Now 9:41 marker. Reuses sched-row.
- All Today-batch lessons applied up front: category color on every item, trailing-meta times, iOS grouped lists/dividers, hit44, truncation, no caps subjects, full chrome in panel (no line-filter), no double-prefix token bug, base text color on themed container.
- Task->category inferred where demo-data silent (flagged in audit). Book Flights = default accent (no project).
- Machine gate clean: 0 undefined classes, 0 raw inline, 0 em dashes, 0 undefined token refs, tab order locked, both themes.
- STATUS: awaiting Dave visual review + batch approval. Next: Batch 4 = Brain + Messages.

## Batch 3 fix: segmented + Week/Month (RULE 9 amendment #3)
- Dave flagged segmented Day/Week/Month: chunky look + couldn't click to see week/month.
- FIX a: de-chunked `.segmented .seg` out of the 44px min-height group in working design-system (3rd RULE 9 amendment; chip, --on-fill-dark token, segmented). iOS segmented ~30px; touch via wide segment.
- FIX b: built real Week (week selector strip + day events) and Month (May 2026 grid + day events) views. Sunday-start grid. Today 21 = accent circle + event dot. Single-day demo populates selected day in all views.
- Interactivity in TEST PANEL only (RULE 6): segmented tap switches Day/Week/Month; preview static Day. Panel embeds SCHED_DAY/WEEK/MONTH as JS, swaps on .seg click. Hint text added to panel.
- Re-gate clean (both files). build_batch3.py rewritten (old saved build_batch3.bak.py).
- STILL awaiting Batch 3 approval. Next after approve: Batch 4 = Brain + Messages.

## STEP 3 Batch 4 built (Brain + Messages)
- Artifacts: step3-batch4.html (both screens, both themes), step3-batch4-test-panel.html (theme + screen toggle, tap message to toggle unread), step3-batch4-audit.md. build_batch4.py.
- MESSAGES: iOS Messages list. nav-large "Messages" + search bar; card of msg-rows = leading unread-dot column + category avatar (av-40, cat bg) + msg-head (name + time) + preview. Maya(tucci)/Daniel(brain) unread, Sofia(family) read.
- BRAIN: nav-large "Brain" + "Updated 2 days ago"; lead insight card (brain-blue eyebrow + demo insight); iOS Settings-style grouped sections (Who You Know / How You Think / How You Write / Org Context / Setup), each row = colored proj-icon (glyph) + label + value/chevron. 12 rows.
- Icons: pulled exact Lucide SVGs via lucide-static (users, heart, shield, book-open, compass, pen-line, building-2, circle-check, cloud, search) into /home/claude/ic. No hand-authored paths.
- Readability applied up front: dark glyph/initial (on-fill-dark) on light fills (tucci/money/friends); bffsa-on glyph + NEW hairline ring (.proj-icon.ring, box-shadow 1px var(--divider)) so the inverting BFFSA fill stays visible on matching card bg in both themes.
- Inferred/design-choice (flagged in audit): Brain row icons+colors are a design choice (catalog gives labels only); Baseball=health inferred; Adversarial=accent red.
- Machine gate clean both files: 0 undefined classes, 0 raw inline, 0 em dashes, 0 undefined token refs, tab order locked.
- STATUS: awaiting Dave visual review + batch approval. Next: Batch 5 = More + Goals + Life Map + Themes + Settings.

## LOCKED DECISION: font weight scale
- Weight tokens run ~one step heavier than stock iOS BY DESIGN (brand look). normal 400 / regular 500 / medium 600 / semi 700 / bold 800. So "semi"=iOS Bold(700), "bold"=iOS Heavy(800). Dave confirmed: KEEP the heavier JARVIS scale. Do NOT flag or "fix" type weight again; row labels at 400 already match iOS.

## CORRECTION (Batch 4 Brain org names) + demo-data org rule
- Locked demo-data: BFFSA/Tucci/Elite are USER-CONFIGURED at signup, NOT demo names. Demo org PLACEHOLDERS = Northside Select (elite red), Metro Youth League (tucci teal), Coastal Alliance (bffsa inverts). Use these everywhere for combined-preview consistency.
- Fixed Brain Org Context: BFFSA/Tucci/Elite Squad/Baseball -> Northside Select / Metro Youth League / Coastal Alliance (3 orgs). Rebuilt batch4 files. (Data correction, not a re-gate.)

## STEP 3 Batch 5 built (More + Goals + Life Map + Themes + Settings)
- Artifacts: step3-batch5.html (5 screens, both themes), step3-batch5-test-panel.html (theme + 5-way screen toggle), step3-batch5-audit.md, build_batch5.py.
- MORE: nav-large "More" + profile row (av-56 accent "D" / Dave / dave@example.com placeholder email, iOS account line) + 2 grouped nav cards (Messages/Goals/Life Map/Projects ; Themes/Settings). Colored proj-icons + chevrons. Tab=More.
- GOALS: nav-large + "3 active". 3 goal rows DERIVED from locked projects (goals not in locked dataset, FLAGGED): Q3 Partnership/money 62%, Marathon Training/health Week 6, Spring Fundraiser/elite On track(good). lifemap-bar/fill reused; bar widths illustrative.
- LIFE MAP: locked 4 areas (Family active/Health drifting/Money active/Friends slipping). cat-dot + bar SAME category color (rule 5), status word colored (good/warn/red). Bar widths illustrative (flagged).
- THEMES: Appearance = two preview tiles (Dark selected ring+check / Light) + "Match Device" switch (off). Only Dark/Light defined in system; NO extra skins invented (flagged). New chrome: .switch, .theme-tile/.tile-preview.
- SETTINGS: iOS grouped sections General(Account/Notifications/Appearance=Dark) / Privacy & Data(Privacy/Sync & Backup=On) / Assistant(Voice) / About(v3.4.8) / Sign Out (row-danger red). 
- New icons fetched via lucide-static: message-circle,target,map,folder,palette,settings,users,bell,sun-moon,lock,info,log-out,moon,sun (+ earlier set).
- Machine gate clean both files: 0 undef classes, 0 raw inline, 0 em dashes, 0 undef token refs.
- STATUS: awaiting Dave review + approval. Next: Batch 6 = Onboarding (last screen batch), then STEP 4 combined preview.

## STEP 3 Batch 6 built (Onboarding) - LAST screen batch
- Artifacts: step3-batch6.html (5 steps, both themes), step3-batch6-test-panel.html (theme + step jump + Back/Next), step3-batch6-audit.md, build_batch6.py.
- Flow: Welcome -> Sign In -> Choose Template -> Notifications -> All Set. No tab bar (modal flow). Progress dots on steps 2-4.
- WELCOME: JARVIS wordmark + tagline + Get Started + "I already have an account".
- SIGN IN: brain badge + "Create your account"; Continue with Apple (btn-apple = tx-1/bg, INVERTS per theme; placeholder for Apple's official SIWA component, flagged) + Continue with Email (mail) + legal line. Auth=Clerk per plan.
- CHOOSE TEMPLATE: 3 cards Personal(brain/users)/Business(money/briefcase)/Student(accent/graduation-cap). Student SELECTED + "Popular" pill (the wedge). New chrome .tmpl/.tmpl-ic.
- NOTIFICATIONS: bell badge priming + Enable + Maybe later.
- ALL SET: check badge + Enter JARVIS.
- New icons: apple(dropped-fruit-not-logo), mail, arrow-right, check, briefcase, graduation-cap. Apple icon NOT used (Lucide apple = fruit, not Apple logo; SIWA button text-only).
- Composed (no source onboarding in catalog); copy + template descriptions from three-template plan; flagged in audit.
- Machine gate clean both files.
- STATUS: awaiting approval. ALL 6 screen batches then done -> STEP 4 combined preview (all screens as one app), STEP 5 Visual Lock.

## DECISION: three-template differentiation timing
- Three-template spec (Personal/Business/Student differences: categories, entity types, terminology, which sections show, seed content/copy) is PARKED as the FIRST task AFTER Visual Lock (post STEP 5), before/driving the Supabase schema.
- Visual system stays template-AGNOSTIC (same screens all 3; only data/categories/copy differ). Student is the wedge -> build Student fully first, Personal/Business as later configs.
- Dependency: Monetizing Jarvis strategic plan may already define the differences (not yet read); translate it into the spec, or define if absent.

## STEP 4 built: combined preview (one app)
- Artifact: step4-combined.html. Interactive prototype reusing approved Batch 1-6 screens (build_combined.py imports build_batch2-6, strips each screen's tab bar, supplies one consistent tab bar).
- App mode: tab nav (Today/Tasks/Schedule/Brain/More) + More pushes sub-screens (Messages/Goals/Life Map/Themes/Settings) with cx-navbar Back. Onboarding mode: 5-step flow, buttons advance, Enter JARVIS -> app. Theme toggle. Meta-controls OUTSIDE device; device has only real app chrome.
- 10 app screens + 5 onboarding embedded. Machine gate clean (0 em, 0 undef cls, 0 raw inline, 0 undef tok).
- Known gaps (flagged): Projects row no dedicated screen (no-op); Schedule=Day (week/month in batch3 panel); global transient states in batch1 artifacts; Apple SIWA placeholder.
- STATUS: awaiting Dave review of combined preview. Next: STEP 5 Visual Lock.

## STEP 5 Visual Lock PROPOSED (awaiting confirm)
- VISUAL-LOCK.md written: frozen file set (design-system working copy + uniformity v1.3 + step3-batch1-6 + step4-combined + demo-data + manifest + ic/ Lucide set), the 3 RULE 9 amendments, all locked decisions, deferred log (scrim token, Projects screen, global states wiring, schedule week/month, goals data model, themes skins, illustrative values, Apple SIWA, behavior-level items).
- Final sweep: all 7 rendered screen files CLEAN (0 em, 0 undef cls, 0 raw inline, 0 undef tok), both themes. 3 amendments confirmed in design-system.
- On confirm: visual phase COMPLETE. Next = three-template spec (parked first task), then code phases 6-14.

## VISUAL LOCK CONFIRMED 2026-05-21 - VISUAL PHASE COMPLETE
- Dave confirmed the Visual Lock. STEP 0-5 done. Visual system frozen per VISUAL-LOCK.md.
- Next (parked decision): three-template spec (Personal/Business/Student), then code phases 6-14.
- Dependency: Monetizing Jarvis strategic plan may already define the template differences; not yet read. Awaiting Dave's choice: draft spec now from what we know, or he brings the strategic plan in first.

## STRATEGIC PIVOT (Dave directive, 2026-05-21): WEDGE not platform
- DO NOT build the three-template spec. Strategy already decided: build the WEDGE, not the platform.
- BUILD PERSONAL ONLY for now. Multi-template / module platform is DEFERRED until Personal ships and the wedge validates.
- Do NOT build Personal/Business/Student variations or any module entities. Modules are NOT stubbed.
- Proceed to CODE PHASE architecture (STEP 6) for Personal.
- DATA LAYER: design an EXTENSIBLE typed-entity model (base item + type-specific schemas) so domain modules can plug in LATER without a rewrite, but IMPLEMENT ONLY Personal entities now: orgs, workstreams, projects, meetings, contacts. Extensibility required; building modules now is FORBIDDEN.
- Target module architecture to be documented SEPARATELY (not built).

## STEP 6 data-layer architecture drafted (Personal)
- Docs: step6-data-architecture.md (build-now Personal data layer), module-architecture.md (DEFERRED target, documented not built).
- Model = extensible typed-entity: entity_type registry (extensibility seam) + item (base, common cols + JSONB data validated vs type schema, server updated_at, NATIVE delete no tombstones) + item_link (generic typed edges = relationships) + scalar_setting (server-authoritative single-value sync e.g. theme).
- Personal types seeded: org, workstream, project, meeting, contact. RLS owner_id=Clerk sub; per-table realtime.
- OPEN SCOPE Q raised to Dave: Tasks tab needs a `task` entity, Messages needs `message` entity (core Personal, not modules). Did NOT add silently. Awaiting his call (in core now vs fold/defer). Goals/LifeMap/Brain treated as out-of-core unless told otherwise.
- Module arch (how modules plug in later: new entity_type rows + schemas + RLS/views + UI reg, core untouched) documented separately, NOT built.
- STATUS: awaiting Dave review of the data model + the task/message scope call. Next: auth wiring (Clerk->Supabase JWT), RLS SQL, updated_at trigger, seed types, map screens to queries.

## STEP 6 cont: task+message confirmed, schema migration written
- Dave confirmed: task and message ARE in Personal core. 7 types: org, workstream, project, meeting, contact, task, message.
- step6-data-architecture.md updated (task/message rows + relationships; open scope Q resolved).
- 0001_personal_core.sql written: entity_type, item, item_link, scalar_setting; indexes; server-authoritative updated_at triggers; RLS (owner_id = current_external_id() = Clerk JWT sub) on all 4 (entity_type read-only to authed users); native DELETE (no tombstones); per-table realtime publication; 7 seeded Personal entity_type rows with JSON Schemas + SF Symbol icons. All schemas valid JSON, 0 em dashes.
- Color is instance-driven (data.category); JSON-Schema validation app-side now, pg_jsonschema DB hardening deferred (noted in file).
- NEXT: Clerk->Supabase auth wiring + map each locked screen to its read/write queries (access patterns), then seed demo data / build out.

## RUN MODE (Dave, 2026-05-21): keep building, explain plain English
- Dave is the product owner, NOT reading code/SQL. Going forward: I make ALL technical decisions myself with sensible defaults; I do NOT hand him technical files to approve.
- I keep building the real artifacts (specs/SQL/code a dev or Claude Code implements; cannot deploy live here).
- I explain EACH step in plain English, no jargon.
- I only surface decisions that are genuinely his: what the app does, what's in/out of scope, anything that costs money. In plain English with a recommendation.

## DECISION: login = Apple + Email, ENFORCEMENT DEFERRED to App Store launch
- Sign-in methods: Sign in with Apple + Email (Clerk).
- Do NOT enforce login until the app is live on the App Store. Dave's personal/dev version must run with NO login wall.
- Technical (mine): auth behind a flag. Dev/personal build = no login, fixed local owner_id, RLS bypassed locally. Production/App Store build = Clerk login enforced. Build the capability now, switch it on only at launch.
- Payments (Stripe) similarly = launch-time, not now.

## STEP 6 cont: full Personal build architecture documented
- personal-build-architecture.md: covers every piece for the real build, plain English + technical. Sections: data (done), login (done/deferred), the assistant brain (Anthropic API server-side proxy, key-safe, per-user AI budget + rate limit), sync/offline, notifications, voice, App Store & payments, reliability/privacy/abuse (Sentry, rate limiting, Plausible/PostHog, no ads), deferred items.
- REAL DECISIONS surfaced to Dave (plain English): (1) AI budget per user (generous vs tight); (2) payments = Apple IAP for in-app subs (compliant default, verify Apple rules at launch; Stripe for web/non-Apple); deferred.
- KEY FLAG logged: Apple App Store generally REQUIRES Apple In-App Purchase for in-app digital subscriptions (15-30% cut); Stripe likely NOT allowed for in-app digital subs. Rules shifting (courts/EU); verify at launch. Project brief said "Payments: Stripe" - reconcile at launch.
- STATUS: Dave wants every detail covered before real build; keep going. Awaiting his 2 real decisions (AI budget, payments-ack) but can continue covering details.

## STEP 6 cont: screen-by-screen functional spec
- screen-functional-spec.md: every locked screen = what it shows (reads) / what you can do (writes) / states / AI behavior. Plain English.
- 6 OPEN PRODUCT questions surfaced (not money, all Dave's): (1) Messages = what? (in-app inbox / email-text integration / JARVIS nudges) BIGGEST; (2) Brain knowledge data sources; (3) Goals real fields vs derived; (4) Life Map status auto vs manual; (5) Projects screen GAP (never designed, needs list+detail); (6) Onboarding drop template-choice step for Personal-only.
- STATUS: keep covering details per Dave. Offered: keep going vs resolve the 6 open questions.

## MAJOR GAP CAUGHT (Dave, 2026-05-21): visual phase missed features
- Dave flagged: "Where is email? Where is the music option? Did you actually look through all features?" CORRECT catch.
- I speced only from the ~10 screens I rebuilt, NOT the full source app. Did a full pass over all 28 source files.
- EMAIL: real, missed. Messages (catalog 14) is a unified EMAIL + TEXTS inbox with Drafts + Scheduled sends + filters (All/Unread/Email/Texts/Drafts). I built a 3-row chat list. Big miss.
- MUSIC: NOT in any source file (searched all 28, zero hits). New scope if wanted; told Dave honestly.
- Also missed/under-built: Notes(18), Maps/locations(12), Notifications feed(17), Search(17), Money/finance card(17), Contacts list(17), Media/photos(13), Charts(11), full Voice screen(09), and detail screens (project/life-area/account/about/advanced/backup/notifications/themes/voice -detail).
- VISUAL LOCK was INCOMPLETE (core screens only). Missed features must be designed + re-locked OR cut from Personal scope before real build.
- feature-inventory.md written (complete, from source). Awaiting Dave's SCOPE call: which missed features are IN for Personal.

## INCIDENT: overwrote original feature-inventory.md (ship list) - rebuilt
- I overwrote the confirmed feature-inventory.md (had FEAT ids, P/B/S tags) without checking it existed. Full original body NOT recoverable from transcripts (created in an uncaptured/compacted session). Only header+legend survived (head -5).
- Recovered the governing rules: FULL FEATURE COVERAGE (inventory = ship list, confirmed pre-STEP 3) + RULE 18 (every feature ships with an interactive test harness, numbered steps, real behavior, PASS/FAIL, behind TEST_MODE flag).
- Rebuilt feature-inventory.md COMPLETE from source (FEAT-01..25). Personal-scoped. Marks built vs NOT-built vs under-built. Music = FEAT-25 [Missing], Dave's call.
- KEY FINDING: STEP 3 built only the core screens; many confirmed-inventory features were NOT built (Email/Texts full inbox, Notes, Maps, Notifications feed, Search, Money card, Contacts, Media, Charts, Projects screen, detail screens, full Voice). Visual Lock was therefore INCOMPLETE.
- LESSON: never overwrite a file without checking it exists first (create_file failed -> I should have read it fully before cat-overwriting, and backed it up).
- AWAITING: Dave confirms Personal feature scope (which NOT-built features are in) + Music decision.

## SCOPE CONFIRMED (Dave, 2026-05-21): build ALL missing features + Music
- Build all missing/under-built features into Personal, design + RE-LOCK everything (Visual Lock v2).
- Music (FEAT-25): Dave WANTS it, will describe it. Hold design until description received.
- Re-lock plan (missing-feature batches): R1 Messages(Email+Texts+Drafts+Scheduled)+Contacts; R2 Notes+Media/photos+Search; R3 Maps/locations+Notifications feed+Money card+Charts; R4 Projects(list+detail)+detail screens(life-area/account/about/advanced/backup/notifications/themes/voice); R5 full Voice; R6 Music. Then combined preview v2 + Visual Lock v2.
- Demo data will need extending for richer surfaces (e.g. full inbox) using LOCKED personas; flag each extension.
- NEXT: await Music direction, start R1 (full Messages).

## MUSIC (FEAT-25) SPEC (Dave): suggested music during tasks
- JARVIS suggests music matched to the current task / focus session; plays via connected streaming (Spotify / Apple Music). Brain-driven, task-aware. Includes playback control + now-playing strip. NOT a standalone player. Surfaces on Today/Tasks + a now-playing strip. Build in R6.

## FEAT-25 Music: spec'd (Dave brainstorm, 2026-05-21)
- Concept: JARVIS sets context-matched music during tasks via the user's Apple Music/Spotify. NO music engine on our side; lean on services' existing playlists.
- Pick: task -> mood (deep focus/light/creative/workout/wind-down) -> existing service playlist. BLEND: start with services' mood playlists, learn toward user's own/likes.
- UI: suggest+tap default (auto-start optional, off by default), now-playing strip, fades on complete, voice control.
- Streaming: Apple Music (native default) + Spotify; user connects either; their sub plays (no licensing cost); built-in focus sounds fallback.
- music-feature-spec.md written; feature-inventory FEAT-25 -> Confirmed/spec'd.
- Music is R6 in the re-lock plan. NEXT: start R1 (full Messages: Email+Texts+Drafts+Scheduled) + Contacts.

## FEAT-25 Music refinement: taste/artist personalization
- Dave: must pick up on the user's liked artists, type of music, history. Folded in: matching keys off the user's top/liked artists, genres, history (services track this) so sets sound like THEIR taste at the right energy, not generic. Spotify = seed by artists + tune energy/instrumentalness; Apple Music = personalized mixes/stations. Service engine does taste work; JARVIS supplies context. music-feature-spec.md updated.

## FEAT-25 Music: scope finalized (Dave directives, 2026-05-21)
IN: suggest+user-sign-off (default, no surprise playback); plan ahead = schedule + repeat for known tasks/events; assign/pin playlists to tasks/events/categories (manual override wins); preferences customizable in SETTINGS (service, suggest-vs-auto, genre guardrails e.g. no-lyrics-in-deep-work, focus-sounds prefs, per-category defaults); part of ONBOARDING (connect Apple Music/Spotify + prefs); woven into BRAIN (music+productivity insights); Focus Sounds (handful of non-music: rain/cafe/brown noise); learns over time (skips/repeats/what kept per task); Lock-in mode (focus set + timer + silence notifications); Start kick (energizing first-few-min opener); taste personalization (artists/genres/history); blend (services' playlists -> learn toward own).
CUT: session arc (ease-in/deep/wind-down) + auto-extend = far-fetched.
PARKED (not v1): completion payoff, calendar-aware prep, state-shift, "why this" line, reads-the-room quiet hours.
music-feature-spec.md rewritten to match. Music is R6 in re-lock plan.

## FEAT-25 Music: user control + auto (Dave, 2026-05-21)
- Every suggestion is adjustable: accept / swap playlist or song / pick own, one tap. Nothing forced.
- Three control levels: Auto (hands-off, JARVIS runs it), Suggest (default, offer+tap), Manual (user picks). Auto toggle OFF by default. Spec updated.
- BRAINSTORM PILE not yet folded into spec (await converge): batch1 = lock-in mode, session arc+flow protection, completion payoff, calendar-aware prep, "why this" line, focus sounds; batch2 = hard-start kick, state shift, skip=learning, "same as last time", no-more-picking, reads-the-room; further = genre guardrails, wind-down/sleep, feed the Brain. Capture all when Dave converges.

## FEAT-25 Music: spec reconciled to disk (authoritative) + control folded in
- music-feature-spec.md on disk is the current truth and is further along than the prior STATE summary. It already contains: concept; how it picks (mood + taste, blend); CONTROL (Auto/Suggest/Manual, Auto off by default, every suggestion one tap to swap/replace/pick own) <- Dave's latest folded here; plan-ahead (schedule+repeat); assign/pin playlists (manual override wins); Lock-in mode + Start kick; Focus Sounds; learns over time; Settings; Onboarding; Brain insights; streaming (Apple Music default + Spotify, their sub plays). CUT: session arc + auto-extend (far-fetched). PARKED (not v1): completion payoff, calendar-aware prep, state-shift, "why this" line, reads-the-room.
- Spec now comprehensive. Music is R6 in re-lock plan. Ready to start R1 (full Messages + Contacts) when Dave gives go.

## FEAT-25 Music LOCKED v1 (Dave, 2026-05-21). Build = R6. NOW BUILDING R1 (Messages: Email+Texts+Drafts+Scheduled + Contacts).

## R1 BUILT (Messages FEAT-05 + Contacts FEAT-18) - awaiting Dave VISUAL confirm
- Files: relock-R1-messages.html (preview both themes), relock-R1-test-panel.html (RULE 18, 9 steps), relock-R1-audit.md. Builder: /home/claude/build_relock_R1.py.
- Full inbox: filter chips (All/Unread/Email/Texts/Drafts), Email(5), Texts(4: 3 iMessage+1 SMS w/ channel badges), Drafts(2, warn accent), Scheduled(2, blue accent, Edit/Send now/Cancel). Contacts(6, role subtitles).
- uniformity bumped v1.4 (G9 messages, G10 contacts; token-clean). demo-data extended (flagged R1 inbox extension; locked previews preserved for Maya/Daniel/Sofia).
- MACHINE gate clean: 0 em, 0 undefined classes, token-only inline, both themes, counts verified.
- Compose/detail/thread screens deferred to R4 (detail screens).
- NEXT after R1 approval: R2 (Notes + Media/photos + Search).

## R1 v2 REBUILT to iOS-native (Dave: v1 didn't look iOS; wants email/text separated)
- Email|Texts segmented control splits them. Email = iOS Mail (no avatars, unread dot, sender/subject/2-line preview, chevron; Drafts+Scheduled grouped under Email). Texts = iOS Messages (avatars, preview). Dropped category pills + channel badges + inline buttons (un-iOS). Added compose FAB.
- uniformity v1.5 (G11) added; v1.4 inbox styling superseded/unused.
- Gate clean (0 em, 0 undefined, token inline, both themes; counts verified). Files regenerated: relock-R1-messages.html, relock-R1-test-panel.html (9 steps), relock-R1-audit.md.
- Awaiting Dave VISUAL verdict. Alt option offered: fully separate Email/Texts screens instead of segmented.

## R1 v2.1: reduced subtext (Dave). Email/drafts/scheduled = sender+subject only (no preview line). Texts = single-line preview. Gate clean.

## R1 v2.2 page-level visual pass (Dave). Fixed: time tx-4->tx-3 (light-mode legibility), texts preview tx-3->tx-2 (tier from time), removed floating red compose FAB -> iOS nav-bar top-right compose button (.nav-compose, accent tint); Contacts gets top-right "+". uniformity v1.6. Gate clean. Confirmed sender/subject, grp headers, drafts orange To:, blue Sends line, contacts all read clean both themes.

## R1 APPROVED (Dave, 2026-05-21). FEAT-05 Messages + FEAT-18 Contacts done (iOS-native). Starting R2: Notes(FEAT-06)+Media(FEAT-14)+Search(FEAT-16).

## R2 BUILT (Notes FEAT-06 + Media FEAT-14 + Search FEAT-16) - awaiting Dave verdict. Files: relock-R2-screens.html, relock-R2-test-panel.html (7 steps), relock-R2-audit.md. uniformity v1.7 (G12/13/14). iOS-native, no category pills on notes, minimal subtext. Gate clean. demo-data extended (flagged). Builder /home/claude/build_relock_R2.py. NEXT: R3 (Maps/locations + Notifications feed + Money card + Charts).

## R2 EXPANDED to all functional states (Dave: functionality renders more visuals, address them all). 14 states: Notes(list/open/new/empty), Media(grid/sheet/photo-picker/files-picker/attached/viewer/empty), Search(initial/results/no-results). Interactive tap-through prototype (relock-R2-test-panel.html, 11 steps). uniformity v1.8. Fixed latent bug: nav-large used undefined --t-title -> --t-h1 (fixed R1+R2, both rebuilt). Gate clean. Builder /home/claude/build_relock_R2.py (old v1 -> build_relock_R2_v1.bak.py).

## R1 GATE REBUILD (after Dave STOP). Re-anchored DS+v2 in full. R1 rebuilt from canonical components ONLY (msg-row/msg-head/msg-name/msg-subject/msg-preview/unread-dot/pill/nav-large/nav-action/segmented/conn-row/chev/eyebrow/av), zero inline styles (category color via .cat-bg-* classes; uniformity v1.9 adds tucci on-fill-dark + cat-bd-*). Restored: category-colored avatars, channel pills, 1-line preview. Removed improvised mail-row/imsg-row/nav-compose/send-meta. Machine gate clean. relock-R1-gate.md = traced tokens + v2 diff for Dave visual confirm (cannot self-PASS). Builder backed up build_relock_R1_preGate.bak.py. NEXT (only after R1 confirmed): re-audit+rebuild R2 to canonical; then R3. R2/R3 NOT started.

## R1 declutter (Dave): names+subjects truncate to one line (.truncate), time nowrap+no-shrink, channel pills (IMESSAGE/SMS) removed (channel shows in thread, not list). uniformity v2.0 (.msg-name flex/min-width, .msg-time nowrap). Gate clean: 0 inline, 0 undefined, 0 em, both themes.

## R1 subtext reduction (Dave: less subtext in general). One subtext line max per row: email/drafts/scheduled = subject only (preview dropped); texts = message line only; contacts = name only. Gate clean.

## R2 GATE REBUILD. uniformity PRUNED (295->148 lines): removed all improvised v1.4-v1.8 classes (note-row/ne-*/empty-*/as-*/srch-*/file-*/topbar-*/grp-head/upload-*/recent-*/nav-compose/ph/pv-*). R2 (14 states) rebuilt from canonical: .row+.cat-dot+.conn-name+.conn-meta+.chev (notes/files/search/recent), .empty-state, .action-sheet button(.cancel), .nav-bar/.nav-back(+ios chevron)/.nav-action/.nav-large, .search-bar, .eyebrow, .t-h2/.t-body. New --scrim token (DS, resolves deferred G4). New components in uniformity v2.1 (no canonical equiv): photo-grid/tile/check, photo-viewer/pv-stage, doc, sheet-scrim, row-x, lead-tint/muted, is-placeholder, empty-icon glyph, nav-back chevron. Notes list = title+date only (less subtext). Machine gate clean (0 inline, 0 undefined, 0 em, both themes). R1 rebuilt against pruned uniformity, still clean. relock-R2-gate.md = traced tokens + v2 diff for Dave confirm. Builder backup build_relock_R2_preGate.bak.py; uniformity backup uniformity_preGateR2.bak.css. NEXT after R2 confirm: R3 (Maps+Notifications+Money+Charts). R3 NOT started.

## R2 GATE color fix (Dave: color coding thin). Systemic color applied: notes + search results = filled category tiles (.proj-icon .cat-bg-*); files = type-color glyphs (.fg-good/red/blue, Files-app convention); note editor = category kicker (.cat-fg-tucci); add actions = accent (.lead-tint). Grey only on photo placeholders (real photos in app) + recents (no category) + dividers/secondary text. uniformity v2.2 (.proj-icon .ic size, .fg-*). 18 category tiles, file-type colors, gate clean.

## R3 GATE BUILD. Maps/Notifications/Money/Charts (5 states): map(stage SVG+location rows), notifs(canonical msg-row, grouped)+empty, money(hero card cat-fg-money + lifemap-row SVG spend bars + txn rows), insights(SVG vertical bars + stat tiles G7 + completion ring + sparkline). Charts = inline SVG, geometry in SVG attrs (zero inline), color via cat-fg-*/fg-tint (fill/stroke=currentColor). uniformity v2.3 (chart/map-stage/hero-num/ring/delta/day-axis). Re-anchored on catalogs 11/12/17 for intent (old catalogs are inline-styled; derived clean SVG versions). Machine gate clean (0 inline,0 undefined,0 em, both themes). relock-R3-gate.md = traced tokens + diff. Builder build_relock_R3.py. No interactive panel yet (low interactivity; add if needed). NEXT after R3 confirm: R4 (Projects + detail screens). R4 NOT started.

## R3 Money expanded (Dave: budgeting for everyone + upload from other apps/accounts). Money Overview now: budget hero (May Budget, $1160 left, overall bar), "Add to Budget" import rows (scan receipt/statement, add from screenshot of other budgeting app, connect account) PROMINENT, "Budget by Area" category bars (over-budget=red), Accounts (checking/savings/visa, neg=red), Recent txns. New money_import screen (scan/screenshot/connect/import file; imported items land in Recent). 6 states now. Gate clean. budget_cat/import_row/acct_row compose from canonical (.row/.lifemap-row/.msg-body/.conn-name + bar_h SVG); backbar() added.

## R4 GATE BUILD. 11 screens: Projects list (canonical proj-row) + Project detail (eyebrow kicker, progress ring, task-row w/ cat-bd border + urgency, team), Life Area detail (status, ring, suggestion-row, people, Call Mom CTA), Settings: More(profile + colored icon-tile nav rows + sign out), Account(profile + plan/login), Notifications(toggle rows + quiet hours), Themes(appearance segmented + color skins w/ swatches+check), Backup(sync-banner + toggles), Advanced(toggles + data rows), About(app-icon + version + legal), Voice(toggles + voice style choices + wake word). uniformity v2.4 (.profile/.app-icon/.swatch/.destructive-row/.detail-head/.btn-block/.fg-warn). All compose from canonical (.proj-row/.task-row/.suggestion-row/.row/.switch/.radio/.segmented/.sync-banner/.eyebrow). Machine gate clean (0 inline,0 undefined,0 em, both themes); R1-R3 reverified clean. relock-R4-gate.md. NEXT after R4: R5 (full Voice interaction UI), then R6 (Music), then combined preview + Visual Lock v2.

## R4 color-clarity fix (Dave). (1) Detail/section kickers now GREY (.eyebrow, no cat-fg) matching the grey proj-tag in lists; category color lives in tiles/dots/rings/borders, not in header text. (2) Project count pills all pill-subdued (grey) so the category tile is the only color per row (removed warn/good pills that clashed with the tile). (3) Life-area: dropped redundant pink kicker + floating orange DRIFTING header; status moved inside the ring card next to the orange ring. Gate clean.

## R4 PASSED (Dave). Final nit: "On Track" was a status in a count slot -> changed to "4 Tasks" so all project-list trailing items are consistent neutral counts (5 Tasks/3 Today/2 Tasks/4 Tasks). Logic locked: category=tile color (one per row); LIST trailing=neutral count pills (overview, calm); DETAIL=per-task urgency as colored TEXT (red/amber/grey) per gate rule 6 (urgency=text not pill). Kickers grey. R1-R4 all clean. NEXT: R5 full Voice interaction UI.

## R5 GATE BUILD. Voice interaction UI (4 states): idle (orb + Tap to Speak + command chips), listening (orb pulse + Listening + live transcript + Tap to stop), thinking (orb + spinner), response (user/ai conversation bubbles + canonical sched-row result card "Goldman -> 4:00 PM" + Undo/Done + G8 capture bar). uniformity v2.5 (.voice-screen/.voice-orb/.voice-prompt/.voice-transcript/.voice-hint2/.chip-wrap/.convo/.bubble/.convo-actions). Reuses canonical .chip/.spinner/.sched-row/.btn + G8 voice-bar. Full-screen mode w/ X dismiss. Machine gate clean (0 inline,0 undefined,0 em, both themes). relock-R5-gate.md. NEXT: R6 (Music), then combined preview + Visual Lock v2.

## R5 spacing fix (Dave). Result card: replaced spread sched-row (60px time rail gap) with a compact .row (cat-dot + title/loc + trailing time). .convo-actions top padding 0 -> s-4 so Undo/Done separate from the card. Consistent s-4 vertical rhythm convo/result/actions. Gate clean.

## NOTES UPGRADE Phase 1 (visual) BUILT - awaiting Dave verdict. Per JARVIS_Notes_Upgrade_Spec.md (Dave pasted it; not on disk/project knowledge). Chose VISUAL batch (Dave picked) honoring spec note that functional/code rework is parked to Notes code phase. 6 screens: List (R2 container reused), Editor-Blocks (text/heading/checklist/light-table/attachments + Add Block), Add Block inserter (8 Phase-1 types), Templates picker (6 starter), Connections (category/event/task + create-tasks action), Create Tasks (task-rows + Create CTA). Phase 2/3 OUT (charts/AI/program/domain templates/two-way sync/backlinks/goals/surface-on-Today). uniformity v2.6 (.inline-dot/.block-h/.note-list/.check-line/.ntable+.num+.sum/.add-block/.row-stack); all compose on canonical .cb/.row/.task-row/.proj-icon/.conn-name/.conn-meta/.chev/.urgency/.sheet-scrim/.action-sheet/.btn. CONSISTENCY FIX flagged: editor kicker now grey text + colored cat-dot (was R2 colored "Tucci" word) per R4 grey-kicker rule. Machine gate PASS (0 inline,0 undefined,0 em,both themes); R1-R5 reverified clean against v2.6. Test panel = 6 DOM-assertion steps. Files: relock-RNotes-screens.html, relock-RNotes-test-panel.html, relock-RNotes-gate.md. Builder /home/claude/build_relock_RNotes.py. Icons added: type/list/list-ordered/table/tag/text/list-todo/calendar-plus/link. NEXT after verdict: R6 (Music), then combined preview + Visual Lock v2.

## RECURRING DEFECT FIXED (Dave frustrated, sent binding gate). Root cause: preview frames never rendered iOS status bar or top/bottom safe-area zones; --safe-top/--safe-bottom = env()=0 in artifact preview so DS L368-370 safe padding did nothing; content jammed 12px from top edge on EVERY screen of EVERY batch. Refs DO render it (v348 .ios-status L597; Today_Light_Realistic .statusbar L48/132). FIX in shared frame chrome of ALL 6 builders: added .statusbar (9:41+cellular+wifi+battery, height 50) + .home-indicator (134x5 pill, height 34); frame 760->800; content clears both zones. Patched build_relock_R1..R5 + RNotes (CHROME+=_SBCSS; STATUSBAR/HOMEIND consts; device() wraps). App markup stays token-pure (0 inline); raw px only in device chrome (status-bar/home-indicator/frame metrics = iOS device values, flagged). All 6 rebuilt: 0 inline/0 undefined/0 em/statusbar+home/both themes. relock-FULL-reaudit.md written. AWAITING Dave visual confirm of the safe-area fix before continuing. Notes Upgrade verdict still pending under this fix. If recurring issue was something else, Dave points at one frame.

## NOTES color-coding fix (Dave: recurring color issue, R5 was fine, Notes regressed). Matched R4 systemic-color standard: connections rows grey glyphs -> colored category tiles (proj-icon cat-bg-tucci) like R4 nav_row; create-tasks rows -> task-check cat-bd-tucci + colored urgency text like R4 task_row (dropped cb-as-selection, subtitle "become Tucci Tasks"); editor kicker 10px dot -> colored category tile matching the list. Add Block/Templates keep accent glyphs (non-category, interactive=accent, flagged intentional). Panel conn assertion updated (proj-icon>=4). Gate PASS (0 inline/undef/em, both themes); safe-area/status-bar fix retained. AWAITING Dave verdict on Notes.

## NOTES "everything blue" fix (Dave). Cause: demo note was Tucci (brand color light blue) AND dark interactive tint is blue -> stacked monochrome; plus menu glyphs were lead-tint (accent=blue in dark). Fix: (1) demo note re-themed to HEALTH/green ("Marathon Plan V3": training checklist, paces table, attachments) across editor/connections/create-tasks so category color is distinct from the blue tint; (2) Templates+Add Block leading glyphs lead-tint -> lead-ink (neutral label color, var(--tx-2)) = iOS menu-icon treatment, not accent (uniformity v2.6b). Remaining blue-in-dark is only the add-block link + one done checkbox (legit interactive). Category variety preserved in list. cat-bd-health task markers; file-type colors kept. Gate PASS (0 inline/undef/em, both themes). Panel assertions updated (Long Run Sunday, Create 3 Tasks). AWAITING Dave verdict.

## NOTES UPGRADE PASSED (Dave, color fix accepted). Standing instruction GOING FORWARD: use VARIED category example data across a batch (not a single-category flow that reads as one color); e.g. mix tucci/health/family/elite/brain/money/friends so systemic color shows variety. Applies R6 onward (passed batches unchanged). NEXT: R6 Music (last re-lock batch), then combined preview + Visual Lock v2.

## R6 MUSIC BUILT (FEAT-25, visual) - awaiting Dave verdict. 7 states: Suggest(offer card + Play/Swap/Replace + For-Your-Day rows)/Now Playing(albumart-lg + transport + SVG progress + APPLE MUSIC pill + Swap)/Lock-In(hero-num timer + Notifications Silenced + Start Kick + End Session)/Focus Sounds(6 ambience rows, Rain playing)/Assign(scope segmented Task/Event/Category + playlist radio list, pinning to Family)/Settings(service rows + Suggest/Auto/Manual segmented + No-Lyrics/Start-Kick toggles + Focus Sounds + per-category defaults)/Connect(empty + Apple Music/Spotify/Focus Sounds btns). VARIED categories (elite/tucci/health/family/money/brain/friends) per new rule. uniformity v2.7 (.albumart/-lg, .np/-title/-artist, .xport/.play, .scrub-times, .lockin/.lockin-sub) all compose on canonical .card/.row/.segmented/.slider?->SVG progress/.btn/.pill/.switch/.radio/.empty-state/.proj-icon tiles/.convo-actions. Progress = inline SVG (geometry in attrs, fg-tint), zero inline. Status bar+safe areas present (global fix). Machine gate PASS (0 inline/undef/em, both themes); R1-R5+RNotes reverified clean. Files relock-R6-screens.html / -test-panel.html (7 steps) / -gate.md. Builder /home/claude/build_relock_R6.py. Icons added: music/list-music/play/pause/skip-back/skip-forward/headphones/cloud-rain/waves/coffee/flame/volume-2/pin/bell-off/zap/radio/disc-3/shuffle/repeat. NEXT after R6 verdict: combined preview (all batches) + Visual Lock v2 sign-off, then Code Phase.

## R6 MUSIC REWORKED to mirror Apple Music (Dave: mirror Apple Music with JARVIS theme, like we mirror iOS). Album art -> Apple-Music personalized-mix covers (category-hued gradient + sheen + scrim + mix-name overlay; real cover art loads from service in live app; thumbnails small w/ music glyph). Now Playing -> Apple Music player layout (grabber, big cover, track+artist + round more btn, thin scrubber elapsed/-remaining, large transport w/ accent play, volume slider w/ speaker icons, Lyrics/AirPlay/Queue tools row; JARVIS context kicker centered above). Suggest -> AM playlist-header (cover+title+desc+Play) + Swap/Replace. Assign/For-Your-Day -> AM library rows (cover thumb+title+sub). uniformity v2.7 rewritten (.albumart/-lg + ::after scrim + -title, .np-head/.np-track/.np-artist/.np-more, .scrub-times, .xport/.play accent, .volume/.vol-bar, .np-tools(+button), .np-context, .np-meta, .lockin). Cover gradient = neutral rgba sheen over cat hue (DS-card technique), not invented color. Icons added chevron-down/airplay/quote/volume-1. Old builder -> build_relock_R6_v1.bak.py. Gate PASS (0 inline/undef/em both themes); all batches reverified clean. AWAITING Dave verdict.

## R6 verdict tweaks (Dave). (1) Focus Sounds now colored icon tiles (proj-icon cat-bg: Rain=brain/Cafe=money/Ocean=tucci/Brown Noise=health/Fireplace=elite/White Noise=family), 6 distinct colors + PLAYING tag. (2) Big cover boxes: added .albumart-edit camera upload overlay on Suggest + Now Playing covers (custom user image; service art default). uniformity .albumart-edit added; camera icon added. Gate PASS (0 inline/undef/em, both themes); all batches clean. AWAITING Dave verdict (then combined preview + Visual Lock v2).

## COMBINED PREVIEW + VISUAL LOCK v2 BUILT (Dave approved R6, said build it). jarvis-combined-preview.html = interactive gallery, 7 batch tabs + Dark/Light toggle, 51 frames pulled live from each batch builder (R1 EMAIL_S/TEXTS_S/CONTACTS_S; R2-R6/RNotes STATES/GROUPS). build_combined.py. 0 em/0 undefined/0 inline in frames. jarvis-visual-lock-v2.md = sign-off summary (coverage, locked standards: status bar/safe areas, token purity, systemic color, caps, cards, Apple Music mirror). AWAITING Dave Visual Lock v2 sign-off. After sign-off -> Code Phase STEP 6 (architecture). NO code phase work until Dave signs.

## COMBINED-PREVIEW AUDIT FIXES (Dave, pre-sign-off). (1) R3 Money: yellow "May Budget" cat-fg-money kicker INSIDE hero card -> grey group("May Budget") eyebrow ABOVE card (matches Add to Budget/Budget by Area). Yellow now only on data bar. Money full audit: only that 1 violation; rest compliant (cat bars, money-tile accounts, cat-dot txns, red over-budget status, grey amounts/headers). (2) R2 note_open kicker: cat-fg-tucci "Tucci" colored TEXT -> grey eyebrow + proj-icon cat-bg-tucci tile (matches R4 sign-off + RNotes editor). (3) Light cards: added [data-theme="light"] .card{background:var(--surface-1)} = solid white (DS glass-bg 0.72 read grey; gate wants solid white in light). Remaining light greys = intentional placeholders (empty photos, map stage, bar tracks, modal scrim). Only colored-text kickers were R2+R3; all other cat-fg = data-viz (bars/rings/pins) kept. Rebuilt all + combined; 0 inline/undef/em all 8 files. AWAITING Dave re-review / sign-off.

## LOCKED RULE: iOS HIG SPACING (app-wide, uniformity v2.8). Fixed at the shared leverage classes so all 51 screens inherit it: layout margin 16pt (.pad-x/.grp/.sub-bar/.seg-card/.detail-head/.doc = var(--s-4)); list rows 44pt with 12pt vertical / 16pt leading (.row family = var(--s-3) var(--s-4) + min-height var(--tap-min)); section rhythm 8/12/16; player/voice/convo blocks on the 16pt margin. Overrides the old JARVIS 18pt edge / 14pt row padding. NEVER reintroduce 18pt edges or 14pt row padding. Any future spacing = these tokens only. Rebuilt all 7 builders + combined; gate clean.

## MONEY COLOR-SCHEME FIX (Dave: import icons all red, off). (1) import_row: was accent glyph ic(...,"lead-tint") = red in light/blue in dark = wall of red, inconsistent with Accounts on same page -> now colored category TILE (proj-icon cat-bg-X) matching acct_row. Varied iOS-Settings style: Scan=money, Screenshot=tucci, Connect=health, Import File=brain. Theme-stable (no longer flips red/blue). (2) Dining budget bar was elite=RED at 76% (NOT over) = misread as over-budget alert -> changed to friends=teal. Red on Money page now reserved for negatives/over-budget only (Travel over=fg-red, Visa balance, neg txns). Rebuilt R3+combined, gate clean.

## LOCKED RULE: ROW-LEADING ICONS = COLORED CATEGORY TILES (app-wide, Dave-approved). Every list/menu/action ROW that has a leading icon uses a colored tile (.proj-icon cat-bg-X), NOT a bare glyph (lead-ink / lead-tint / lead-muted). Applies to Settings, Money (import + accounts), Connections, Focus Sounds, Notes templates, Notes Add Block, and any new row type. Pick varied category colors (iOS-Settings style) unless the row belongs to one category. EXCEPTIONS (NOT row-leading icons, keep as-is): iOS action sheets (tinted text, no icon), toolbar/format buttons (e.g. player np-tools, voice controls), inline glyphs inside text, data-viz. Converted Notes Add Block (8 blocks) + template picker (6) from monochrome lead-ink to tiles. Rebuilt RNotes+combined.

## CROSS-REFERENCE (rules vs all 51 frames). Fixed: R2 Media ADD rows (Take Photo / Choose from Library / Attach a File) were accent glyphs -> colored tiles (tucci/family/health), same rule as Money import. FLAGGED EXCEPTION (kept, awaiting Dave): R2 Search RECENT rows use a grey clock glyph (ic clock lead-muted) not a colored tile = iOS recent-search pattern; recommend keeping grey clock as an explicit exception to the tile rule. All other rules PASS: 0 inline / 0 undefined class / 0 em dash (machine, all 7 files); both themes; status bar + home indicator on every frame; 0 colored-text kickers; rows 44pt min (v2.8); light cards solid white; urgency = colored text not pill; category color in tiles/dots/bars/rings. Note: canonical master prompt is read-only to me (/mnt/project + project knowledge); rule text provided to Dave to paste; logged here as the working rule.

## LOCKED: CODE-PHASE TEST PROCEDURE (Dave-approved, all 3 upgrades). Behavior-first clickable harness: ordered buttons run real in-memory logic, pass/fail + live state, steps unlock sequentially. Approve behavior BEFORE writing production code; then build Supabase code to match; same step defs run as automated tests against real code. UPGRADES: (1) single source of truth = approved step list drives harness now + generated automated tests later, no drift; (2) core + edge/failure steps (bad input rejected, double-action no crash, empty input, offline/reconnect); (3) coverage checklist per feature, each requirement ticks green only when a covering step passes. One harness app with feature picker. Gate: feature behavior locked only when all steps pass + all coverage green + Dave approves; real code accepted only when same steps pass against it; tombstone + sync get permanent dedicated steps. Files: TEST_PROCEDURE.md (contract), test-harness-v2.html (reference template, Notes fully built as the example).

## VISUAL LOCK v2 SIGNED (Dave). Visual phase complete: 51 frames, both themes, gate clean, all rules locked (R4 color logic, iOS spacing v2.8, row-leading tiles + search-recents grey-clock exception). CODE PHASE STARTED.
## CODE PHASE STEP 1: CORE DATA MODEL (the spine). Per locked test procedure: behavior approved before code, data model first. Added "Core Data Model" feature to test-harness-v2.html (now default tab; Notes kept as the example). Tests the universal invariants in memory: D1 create, D2 read, D3 update, D4 hard delete, D5 tombstone-proof reload, D6 per-user isolation, D7 server-time-wins (scalar sync fix), D8 offline-queue/reconnect, D9 safe no-op on missing id, D10 concurrent edits deterministic by server time. Core + edge steps, coverage checklist, single-source spec. AWAITING Dave to tap through + approve the core data behavior; then build Supabase schema + store (RLS = D6, hard delete = D4/D5, server timestamps = D7/D10) to match, and re-run these steps against the real code. No production code written yet.

## CODE STEP 1 FIX (harness caught a real sync flaw pre-code). Step 10 (offline edit then reconnect) failed: server clock was not monotonic, so an offline edit reconnecting after an artificially high explicit timestamp (step 7 ts=10 vs clock=4) got stamped 5, judged stale (5<10), and dropped. FIX in makeCore.applyUpdate: clock is now monotonic (on accepting an explicit ts, advance clock=max(clock,ts)), so a reconnecting offline edit always gets the latest time and wins over prior state. Keeps server-time-wins (D7) + concurrent resolution (D10) correct. Verified via Python mirror: all 11 steps PASS. This is the LWW-by-server-time model (chosen for scalar sync); deeper per-field/CRDT merge is out of scope. Awaiting Dave re-run + approval.

## CODE STEP 1 APPROVED (Dave): core data model behavior, all 10 steps pass. Next = build it for real (Supabase schema + RLS [D6], hard delete [D4/D5], server-time-wins monotonic [D7/D10], offline queue/reconnect [D8], typed store) + auto-generated tests from the 10 approved steps, runnable locally. NOTE: discussed build logistics. Recommended: fresh build chat in Track 3 project for context headroom; persistence = Dave commits each deliverable to the existing private GitHub repo; sandbox cannot reach live Supabase (build + unit-test here, wire to real Supabase + device on Dave machine).

## HANDOFF PACK PREPARED for fresh build chat: CODE_PHASE_HANDOFF.md (START HERE brief) + STATE.md + TEST_PROCEDURE.md + test-harness-v2.html + jarvis-design-system.css + uniformity.css + jarvis-visual-lock-v2.md + jarvis-combined-preview.html. Dave to start a new chat in Track 3 project, load the pack, build the approved Core Data Model first.

## CODE STEP 1 BUILT (Core Data Model, real code). Fresh build chat. Decisions locked via widget: (1) Supabase Auth now, swap to Clerk at Milestone B; (2) build the engine only (base item + scalar_setting + entity_type seam, NO Personal entity types seeded yet); (3) in-memory automated tests now + a device script for live Supabase/RLS. Project jarvis-core: Vite-less lib (React/Capacitor wire-up comes with the first feature), TS + Vitest. Files: src/core/{types,adapter,inMemoryAdapter,supabaseAdapter,store,spec}.ts, src/index.ts, tests/core.spec.ts, supabase/migrations/0001_core_data_model.sql, docs/DEVICE_TEST.md + verify-rls.mjs, README.md. ARCHITECTURE: DataAdapter interface (async) with two interchangeable backends - InMemoryAdapter (faithful 1:1 port of approved harness makeCore, used by tests, no network) and SupabaseAdapter (real, used on device). Store wraps an adapter and adds the offline queue (client concern). spec.ts is the SINGLE SOURCE: the 11 approved steps ported 1:1 from test-harness-v2.html, drives the Vitest suite; harness and spec must be kept in sync (future: regenerate harness from spec). SQL: item (uuid id, owner_id default auth.uid(), entity_type FK, data jsonb, created_at, updated_at) + scalar_setting + entity_type registry; monotonic updated_at trigger (greatest(now(), old+1us)) = D7/D10 ordering; item_apply_patch(p_id,p_patch) RPC = atomic JSONB merge inside RLS (security invoker) = D6+D9; RLS owner-only select/insert/update/delete on item + scalar_setting; entity_type readable by authenticated. Seeded placeholder type 'item' so the FK holds until real types arrive. TESTS: 23 pass (11 ordered steps D1-D10, 10 coverage assertions, 2 permanent guardians = tombstone D4/D5 + offline-sync D8). typecheck clean. 0 em/en dashes anywhere incl comments. LOGISTICS: sandbox cannot reach Supabase (verified) so RLS/trigger proven on device via docs/verify-rls.mjs (needs 2 test users; checks D6 isolation, D7/D10 monotonic time, D9 no-op, D4/D5 hard delete). SCOPE FLAGS (deliberate, in README): only updates queue offline (creates/deletes immediate, matches approved core); LWW-by-server-time only (no CRDT, matches line 404 agreed scope); explicit serverTime is a test affordance honored by in-memory adapter, ignored by SupabaseAdapter (server owns time). MISSING-FROM-PACK FLAG: step6-data-architecture.md + module-architecture.md referenced in STATE but not in this pack; built from the approved harness (the binding source) instead. AWAITING Dave: review, then wire to his real Supabase project + run verify-rls.mjs on device, commit jarvis-core to the private repo. NEXT after that: first feature (Notes is the harness example) through the same gate.

## CODE STEP 1 VERIFIED ON REAL SUPABASE (device check PASSED, Dave ran it, 2026-05-22). Schema 0001_core_data_model.sql applied to a fresh Supabase project (Supabase Auth, free org). RLS verification block (two simulated authed users via request.jwt.claims + set local role authenticated, wrapped in begin/rollback so nothing persists) returned ALL CHECKS PASSED: D1/D2 create+read; D6 isolation (B blocked from read/update/delete of A's row, cannot alter A's data); D3 owner update; D7/D10 monotonic updated_at advances; D9 missing-id no-op; D4/D5 hard delete with no resurrection. Combined with the 23/23 in-memory Vitest suite, the Core Data Model is now proven end to end (engine logic + real Postgres RLS). Repo: github.com/davefisher813/jarvis-rebuild (jarvis-core/). ENGINE = DONE. Note: project still named warm-project-pool default (cosmetic, renamable). NEXT GATE: first Personal feature through BOTH gates (design gate JARVIS_DESIGN_GATE.md + behavior gate TEST_PROCEDURE.md); Notes is the harness example. App shell/routing/theme-skin layer (remainder of STEP 6 architecture) NOT yet built; open decision next session = shell-first vs feature-first. Feature additions merged provisionally (FEAT-26 Quick Capture, FEAT-27 AI routing, FEAT-28 note full-text search, FEAT-29 outside-app voice; create-tasks-from-note already in Notes Phase 1); final FEAT numbers pending the master feature-inventory.md which is not in the pack.

## CODE STEP 2 STARTED: app shell, slice step 1 BUILT (scaffold + theme layer + engine wired). Path confirmed by Dave via widget: thin vertical slice (shell then Notes), not features-in-isolation, not a big speculative shell. Built app/ as a sibling to jarvis-core/ in the repo, importing the engine via Vite alias @core -> ../jarvis-core/src/index.ts (single engine source, no duplication). Stack: React 18 + Vite 5 + TS, React Router (installed, routes come at the nav step). Files: app/{package.json,vite.config.ts,tsconfig.json,index.html} + src/{main.tsx, App.tsx (DEV SCAFFOLD only, not a product screen, not gated), theme/ThemeProvider.tsx, data/client.ts, styles/(locked jarvis-design-system.css + uniformity.css copied in), vite-env.d.ts}. THEME LAYER: drives [data-theme] dark/light on the document root (the locked CSS already styles both); default dark; persists to localStorage now, will move to server scalar_setting('theme') when settings sync is built. SKINS: NOT wired - the locked CSS has no skin token mechanism (R4 Themes screen showed swatches only); deferred until skin tokens exist. DATA CLIENT: makeStore() uses createSupabaseAdapter(url,anonKey,accessToken) when VITE_SUPABASE_URL+VITE_SUPABASE_ANON_KEY are set, else InMemoryAdapter fallback so the shell runs with no network. RESULT: npm install clean; tsc --noEmit CLEAN; vite build OK (83 modules, locked CSS bundled 32kB); 0 em dashes; the .js->.ts engine specifiers resolved fine under both tsc (bundler) and Vite. Capacitor/iOS DEFERRED to the wrapper phase per plan. Engine technical reality reconfirmed from disk (exports: Store, InMemoryAdapter, SupabaseAdapter, createSupabaseAdapter, types; Store.create returns Promise<string> id). NEXT (gated): slice step 2 = Sign-In screen (Supabase Auth) through the DESIGN GATE (value-to-token table + literal scan + 3 offenders + diff vs locked Sign In screen), built here + verified on device; then nav shell (tab bar + routing); then Notes through behavior harness + design gate.

## STEP 2 amendment (Dave: avoid issues at skin implementation + a coming GAMING MODE): generalized the theme layer to a MULTI-AXIS appearance system BEFORE building further, so skins and gaming mode are additive later (no provider rewrite, no screen changes). src/theme/ -> src/appearance/AppearanceProvider.tsx. Three independent root data-attributes: data-theme (dark/light, has CSS now), data-skin (default only, no CSS yet), data-mode (standard only, no CSS yet). APPEARANCE_OPTIONS registry = single source of what Settings may offer; adding a skin/mode = add a value + a [data-skin=x]/[data-mode=x] CSS token block, nothing else. Persists whole appearance object to localStorage (jarvis.appearance), will move to server scalar_setting later. The real skin-readiness is TOKEN PURITY (all 51 screens use vars, gate-enforced) so token overrides reskin everything automatically; a single hardcoded color would break a skin, which the design gate blocks. tsc clean, vite build OK, 0 em dashes. OPEN DESIGN DECISIONS for when skins/gaming mode are built: (1) does a skin recolor only accent/surface tokens or ALSO the category brand palette (affects the systemic-color rule; token structure should separate category palette from accent/surface); (2) is gaming mode visual/effects-only (fits the mode axis cleanly) or does it also change layout/flows/features (then those parts are feature-level, not theming). NEXT unchanged: Sign-In screen through the design gate.

## SLICE STEP 2 BUILT: Sign In screen (design APPROVED by Dave "Good", then real React built). Design gate passed via signin-preview.html (both themes) + signin-gate.md (value-to-token table, scan: 0 inline/0 raw colors in proposed CSS/0 em dashes, only flagged device chrome non-canonical; 3 offenders addressed; diff noted screen was never in v2 lock so designed fresh). RULE 9 additions approved: .btn-apple (var(--tx-1)/var(--bg) inverts per theme), .signin-* layout, icon sizes. Real code: app/src/styles/components.css (approved RULE 9 classes), app/src/auth/supabaseClient.ts (guarded singleton, null when no env), app/src/auth/AuthProvider.tsx (session via onAuthStateChange, signInWithApple=OAuth apple, signInWithEmail=signInWithOtp, signOut), app/src/screens/SignIn.tsx (matches approved design; uses .screen real safe areas, NO faked chrome; lucide-react Brain/Mail/Apple, Apple is a flagged placeholder for Apple's official SIWA button). App.tsx now gates: !session -> SignIn, session -> dev placeholder (nav+features pending). main.tsx wraps AppearanceProvider > AuthProvider > Router. Decisions locked at design: app mark = brain-blue (cat-bg-brain); copy "Welcome to JARVIS"/"Your day, handled."; no Choose Template step (Personal-only, sign in -> app); auth = Supabase. lucide-react@0.439 added. tsc clean, vite build OK, 0 em dashes. Email-entry screen is the NEXT gated screen (Continue with Email currently stubbed). NEXT per agreed sequence: EVENT BUS + gamification capture slice (schema only, no UI, fires from Milestone A so gaming system at Milestone C has history), THEN Notes (first feature, behavior harness + design gate).

## SLICE STEP 3 BUILT: EVENT BUS + capture slice (infrastructure, no UI, fires from Milestone A so the Milestone C gaming system has history). Per the prior gaming plan: define event SCHEMA now, defer mechanics. Files in app/src/events/: types.ts (PERMANENT schema: EVENT_SCHEMA_VERSION=1; JarvisEvent envelope {id,type,ts,v,entityType?,entityId?,props?}; EventType union = app.opened/app.foregrounded/auth.signed_in/auth.signed_out/screen.viewed/entity.created|updated|deleted/task.completed/focus.started|completed/action; "action"+props.name = extensible escape hatch so new feature events need no schema bump), bus.ts (EventBus pub/sub, stamps id/ts/v, throwing listener never blocks others), log.ts (LocalEventLog append-only over an EventStorage seam; localStorage impl; cap 10000 safety net), index.ts (singleton bus + local log subscriber + emit() helper). Wired: main.tsx emits app.opened on load; AuthProvider emits auth.signed_in/out on Supabase auth events. Tests: app now has vitest; src/events/events.test.ts = 8 passing (stamp envelope, deliver to subscribers, unsubscribe, throwing-listener isolation, log append/reload/corrupt/cap, bus->log capture). tsc clean, vite build OK, 0 em dashes. DURABILITY FLAG (raised to Dave): localStorage is per-device + bounded (~5MB) = NOT durable history; before launch add a SERVER SINK (owner-scoped append-only events table) subscribing to the same bus so gaming history survives device loss + syncs cross-device. Mechanics (points/XP/levels/achievements) derived from this log at Milestone C, never computed now. NEXT: Notes = first feature, through BOTH gates (behavior harness per TEST_PROCEDURE, then design gate per JARVIS_DESIGN_GATE; Notes IS in the v2 lock so there's a real screen to diff against). Notes will emit entity.created/updated/deleted + (later) task.completed via the bus.

## NOTES BUILD NOTE (Dave reviewed the v2 Notes Upgrade screens). Connections screen demo data is MONOCHROME: 4 "Linked To" tiles all cat-bg-health (green), violating the varied-category demo rule, so the color system does not read. FIX WHEN BUILDING NOTES: vary the Connections demo categories (e.g., a Work project, a Health routine, a Personal goal, a Contact) so colors differ. Create Tasks LEFT GREEN ON PURPOSE: its 3 checklist boxes are cat-bd-health because the tasks are generated from one Health note and inherit its category, which is correct, not a bug (only the urgency labels differ, amber/grey). The cat-bg/cat-bd mechanism is correct on both; only Connections demo CONTENT changes, handled at the Notes design gate.

## NOTES FEATURE: behavior APPROVED + real code BUILT (behavior gate green). Dave tapped through and approved the expanded 18-step Notes Upgrade harness (notes-harness.html: blocks add/edit/reorder/delete, checklist->tasks linked + category inherited, connection add/remove, Meeting Notes template seeds blocks, lifecycle delete/tombstone/tasks-survive, edges: empty-title reject, double-delete, empty-checklist 0 tasks, offline/reconnect, missing-block/connection no-op). Headless pre-check 18/18, coverage 18/18. REAL CODE built in app/src/notes/: types.ts (ENTITY_NOTE/ENTITY_TASK, Block/Connection/NoteData/TaskData, BlockType union = heading/text/bulleted_list/numbered_list/checklist/table/photo/file, TEMPLATES = blank/meeting/todo/tracker/brief/journal mirroring the locked Templates screen), NotesService.ts (async layer over the engine Store; notes stored as item entity_type "note" with content in data; tasks as entity_type "task" linked one-way via fromNote; block/connection edits are read-modify-write writing the changed field back; onEvent DI hook -> event bus, no-op in tests), notesSpec.ts (the 18 steps as async, single source for tests), notes.test.ts. Engine API reconfirmed from disk: Store.create(ownerId, ENTITY_TYPE, data:ItemData) [entityType is 2nd arg], update(ownerId,id,patch:ItemData,serverTime?), ItemData=Record<string,Json>; cast nested Block[]/Connection[] via "as unknown as ItemData". Migration 0002_notes_entities.sql seeds entity_type 'note','task' (run on Supabase after 0001; InMemoryAdapter has no FK so tests pass without it). vitest.config.ts got the @core alias. RESULT: 30/30 app tests pass (8 events + 18 Notes steps + coverage + 3 permanent guards: tombstone R12, sync R17, tasks-survive R13); tsc clean; vite build OK; 0 em dashes. NEXT: DESIGN GATE on the Notes screens (list/open/block-editor/add-block/templates/connections/create-tasks) per JARVIS_DESIGN_GATE, applying the Connections varied-category fix; then the screen + behavior both locked = Notes feature done.

## NOTES DESIGN GATE: COMPLETE (all 6 Notes Upgrade screens approved by Dave, one at a time). Built in app/src/notes/screens/ + app/src/shell/TabBar.tsx, all from canonical classes, both themes, gate evidence per screen (value-to-token table, literal scan 0 inline/0 raw colors/0 em dashes, three offenders, diff vs locked frame; never self-passed):
- #46 NotesList.tsx (list; varied category tiles already correct) APPROVED
- #47 NoteEditor.tsx (block renderer: heading/text/checklist/table inline + file/photo grouped in attachments card + Add Block; table wrapped in tbody for React) APPROVED
- #48 AddBlockSheet.tsx (bottom sheet, 8 block types, varied tiles; canonical .sheet-scrim absolute over device) APPROVED
- #49 Templates.tsx (New Note picker, 6 templates, distinct tiles, keys match types.ts TEMPLATES) APPROVED
- #50 Connections.tsx (MONOCHROME FIX APPLIED: lock had all 4 tiles cat-bg-health/green; now health/tucci/elite/money, content+labels+icons unchanged) APPROVED
- #51 CreateTasks.tsx (stays green by design, tasks inherit note category; urgency warn/muted tags) APPROVED
Note: "note open" and "block editor" are the SAME locked frame (#47), so 6 screens not 7. Base Notes empty-state (#7) is OUTSIDE the Upgrade batch, still optional.
ICON-SIZE FIX: the canonical CSS export was MISSING base .ic context sizes (the locked combined-preview defined them in its own chrome). Added to app/src/styles/components.css: .nav-action .ic / .nav-back .ic = 23px, .tab .ic = 26px, and .row > span[class*="fg-"] > .ic = 20px (attachment icons). Without these, bare-.ic icons (pencil, tab, attachment glyphs) collapsed. Real app uses lucide-react which self-sizes, but these keep it consistent + fixed the previews.
PREVIEW METHOD (locked-in): gate previews render the screen inside the locked DEVICE FRAME chrome (extracted .device flex-column + .scroll flex:1 + .tab-bar + .statusbar + .home-indicator from the combined preview, preview-only NOT canonical) so the tab bar pins to the bottom + status bar fills the top + correct per-theme --bg. Two device frames stacked vertically (DARK then LIGHT) for mobile viewing. React screens use canonical .screen (real iOS chrome) = the one standing allowed exception.
STATUS: app green = tsc clean, vite build OK, 30/30 tests, 0 em dashes. lucide-react icons used across screens (FileText, PenLine, MoreHorizontal, Check, Plus, Image, AlignLeft, Heading, List, ListOrdered, ListTodo, Table, Paperclip, CalendarDays, Tag, ListChecks, Home, Calendar, Brain, MoreHorizontal). 
NEXT (integration, not yet done): wire the 6 gated Notes screens into a NAV SHELL (tab bar routing) + connect them to the real NotesService (live data instead of demo content) so Notes actually runs in the app; App.tsx still gates SignIn vs a dev placeholder. Optional: Notes empty-state (#7). Then move to other Personal-template features (Today, Tasks, Schedule, Brain hub).

## NAV SHELL + LIVE NOTES WIRING: BUILT. Notes now runs end to end (not demo content). New/changed files in app/src/:
- data/store.ts: makeStore(accessToken?) -> Supabase Store when VITE_SUPABASE_URL+ANON_KEY set, else InMemory; exports backendConfigured.
- data/NotesProvider.tsx: provides one NotesService(makeStore(token), userId, emit) via context; useNotes() hook.
- notes/NotesFlow.tsx: the Notes navigation + live wiring. Screens state machine (list/editor/templates/connections/createTasks) + addBlock sheet overlay. toEditorNote() maps stored NoteData blocks -> editor display blocks; starterBlock() per add-block type. Loads via svc.listNotes()/svc.note(); create-from-template = createNote(TEMPLATE_TITLE,"brain")+applyTemplate; add-block = svc.addBlock+reload; create-tasks = svc.tasksFromChecklist. Reports chrome {tabBar: screen==="list"} so the shell hides the tab bar on pushed screens. Demo seed (5 notes) on first mount when seed prop set.
- shell/TabBar.tsx: now FUNCTIONAL (onTab, active); REMOVED from NotesList (shell owns the tab bar).
- shell/Placeholder.tsx: Coming soon screen for Today/Tasks/Schedule/Brain.
- shell/AppShell.tsx: 5-tab host. today/tasks/schedule/brain=Placeholder; more=NotesFlow (Notes is the only built feature, matches #46 More-active). Tab bar shows at Notes list, hides on pushed Notes screens. app-shell flex-column layout (content scroller + pinned tab bar) added to components.css.
- App.tsx: 3 modes -> (no backend = demo: skip auth, InMemory, seed) / (backend, no session = SignIn) / (session = shell on Supabase store with token). main.tsx dropped BrowserRouter (plain state nav, keeps single-file build openable).
- NoteEditor.tsx: added bulleted_list/numbered_list minimal render (FLAG: these two block types have NO locked design yet, rendered as plain lines, need a design later). Connections.tsx: added category/categoryLabel props (Category row now shows the note's real category).
DEMO BUILD: SINGLEFILE=1 npx vite build -> dist/index.html fully inlined (204K), opens offline in demo mode (InMemory, seeded). Delivered as jarvis-notes-demo.html. vite.config gained viteSingleFile() gated on SINGLEFILE env (normal builds unaffected). vite-plugin-singlefile@2.0.2 added.
GREEN: tsc clean, vite build OK, 30/30 tests, 0 em dashes.
WORKS NOW: see notes list (live), open note (live blocks), new note from any template (creates + seeds blocks), add any of the 8 block types, go to Connections (shows real category) + Create Tasks (turns the note's checklist into tasks), switch tabs.
DEFERRED (next slices): title/text inline editing (editor is read-only for text/title), checklist toggle (tap to complete), connections add/remove editing (only Category is live; Event/Task rows are demo placeholders), photo/file real upload (add placeholder rows), note dates in the list (blank for now, wire from updated_at), the full More hub (More currently = Notes directly), bulleted/numbered list visual design.

## TYPE TOKEN CHANGE (Dave's call): --t-meta raised 13px -> 15px in app/src/styles/jarvis-design-system.css. Reason: 13px secondary text was too small. Affects ~21 secondary-text spots app-wide (conn-meta, task-meta, msg-time/preview, sched-time/loc, urgency, chip, num-badge, av-32, proj-icon fallback letter, profile-sub, input-label, btn-sm, segmented seg, voice-hint, sync/error banners, swipe-action, .t-meta). Font-size only, nothing functional depends on it. Verified: tsc clean, build OK, 30/30 tests, demo rebuilt. Watch-spots at 15px (num-badge 22px, av-32, sched-time 60px width, urgency caps) fit fine; verify visually when those screens get built. NOTE: this DIVERGES from the original locked design-system value (the read-only reference CSS in the pack still says 13px); the app's copy is now the source of truth at 15px. Row labels (.conn-name) remain 17px so labels stay a touch bigger than values.

## TITLE CASE AUDIT (Dave): audited ALL visible UI strings against the approved rule (jarvis-design-system.css L106-107 / DESIGN_GATE: Title Case for page/section/card/row titles; ALL CAPS only for eyebrow kickers, pills, urgency, tiers, times; body = sentence case; minor words like to/with/from stay lowercase per STATE example "Time to Focus"). Screens were already compliant (built from the locked frames). ONE fix: Placeholder row title "Coming soon" -> "Coming Soon". Verified clean: nav titles (Notes/New Note/Connections/Create Tasks/Today/Tasks/Schedule/Brain), block headings (This Week/Paces/Attachments), row titles (Category/Event/Task/Create Tasks from Checklist), block-type labels (Text/Heading/Bulleted List/Numbered List/Checklist/Table/Photo/File), template names (Blank/Meeting Notes/To-Do / Checklist/Tracker/Project Brief/Log / Journal), buttons (Continue with Apple/Continue with Email/Add Block/Create N Tasks/Cancel), signin title (Welcome to JARVIS). Eyebrows (Linked To/Actions/Add Block/Templates/From "...") are uppercased by .eyebrow CSS = correct.

## NOTES EDITING: BUILT. Notes is now interactive (was read-only). Tap a checkbox to toggle done; title, heading blocks, text blocks, and checklist item text are editable in place (contentEditable, saved on blur; empty title is ignored and reverts on reload). Wiring:
- NotesService: added toggleChecklistItem(noteId,blockId,index) + setChecklistItemText(noteId,blockId,index,text) (both read-modify-write via the tested editBlock) + normalizeItems() helper.
- NoteEditor.tsx: added Editable contentEditable helper (read-only when no onSave; sets text once + on external change, never while focused so the caret is stable), Checklist now takes onToggle/onEditItem, EditorBlock variants carry id. New props onEditTitle/onEditBlockText/onToggleCheck/onEditCheckItem. Visuals UNCHANGED from the gated screen (same classes/structure); editing just makes existing elements interactive, so no design-gate impact.
- NotesFlow.tsx: toEditorNote now threads block ids; wired editTitle/editBlockText/toggleCheck/editCheckItem -> NotesService -> reload.
- components.css: [contenteditable]{outline:none;caret-color:var(--tint)}.
Behavior was already approved (18-step contract R3 edit block / R9 edit title); added 4 new tests (toggle flips+persists, double-toggle, item text edit, missing-block no-op). RESULT: 34/34 tests, tsc clean, build OK, 0 em dashes. Demo rebuilt. DEFERRED still: connections add/remove editing, photo/file real upload, list dates, More hub, bulleted/numbered list design, table cell editing.

## EDITOR DESCRIPTION REMOVED (Dave: too much subtext). Removed the lead description paragraph (the .t-body after the title) from NoteEditor; dropped EditorNote.description field. Body content lives in editable text blocks instead, so the paragraph was redundant. Editor now goes title -> blocks. DIVERGES from locked #47 (which had a lead paragraph), at Dave's direction. notes-editor-preview.html regenerated without it. tsc clean, demo rebuilt.

## TASKS FEATURE: IN PROGRESS.
- BEHAVIOR: approved. tasks-harness.html (14 steps) tapped through + approved by Dave. Defaults locked: overdue sits in Today group; no-date sits in Upcoming.
- SERVICE: BUILT + TESTED. app/src/tasks/: grouping.ts (pure groupFor/urgencyFor/todayISO; done->Done, no due->Upcoming, due<=today->Today incl overdue; urgency overdue/today/soon), TasksService.ts (thin async over Store on ENTITY_TASK: createTask/toggleDone/editText/setDue/setCategory/deleteTask/task/listTasks/grouped(today); goOffline/reconnect/queueLen passthrough; tasks from notes appear automatically since NotesService.tasksFromChecklist writes same ENTITY_TASK items), tasksSpec.ts (14 approved steps as single source), tasks.test.ts (runs spec + 7 grouping unit tests). Extended TaskData in notes/types.ts: added due?:string|null, made fromNote optional. RESULT: 55/55 tests, tsc clean, 0 em dashes.
- DESIGN: SUBMITTED, awaiting Dave. tasks-page-preview.html (dark+light device frames) + tasks-page-gate.md. Designed fresh from canonical (no locked Tasks screen; Sign In precedent). nav-large "Tasks" + nav-action(+); 3 groups (TODAY/UPCOMING/DONE) via grp+eyebrow+count; rows = task-row > task-check cat-bd-{cat} + task-title + urgency; done = task-row completed + task-check done. URGENCY MAPPING (no blue token in canonical): OVERDUE=urgency-red, TODAY=urgency-warn, soon=urgency-muted. Open decisions for Dave: (1) confirm urgency palette, (2) no from-note badge, (3) no search bar.
- NOT YET DONE: after design approval -> build TasksPage.tsx (React) + wire into AppShell "tasks" tab (replace Placeholder), reading TasksService.grouped(); tap-to-toggle + inline edit like Notes; add-task affordance.

## TASKS DESIGN REBUILT TO CATALOG (Dave: "apply the rules and catalog to the new pages"). Earlier "fresh" Tasks design was wrong; catalog 20 (jarvis-catalog-20-tasks-page.html) is the real Tasks design and must be applied (it covers pages not in the v2 combined-preview lock: tasks/today/goals/brain/empty-states). Catalog uses its own pre-canonical classes (lt/task-chip/capture-bar/bn, inline styles) so it is translated into canonical components, with binding gate rules OVERRIDING the catalog where they conflict.
- NEW Tasks composition (from catalog): nav-large "Tasks" + nav-action(+); FILTER CHIPS (chip-wrap/.chip/.chip.active) Today/Overdue/Upcoming/Done with counts (one filtered list, NOT stacked sections; Overdue is its own chip, supersedes harness "overdue-in-Today" detail); two-line rows (task-row > task-check cat-bd-{cat} + row-stack[conn-name title + eyebrow "CATEGORY · TIME"] + urgency text); VOICE CAPTURE BAR (.voice-bar/.voice-mic/.voice-name/.voice-hint) docked above .tab-bar.
- VOICE BAR: Dave approved APP-WIDE on main tab screens. Resolves provisional placement open item (uniformity.css G8 / RULE 17). Canonical pill (surface-3, r-pill): mic(36 accent) + "JARVIS" + "Tap to speak". Build step: add to shell above tab bar on main tab screens.
- RULE OVERRIDES of catalog (rules win): urgency = colored TEXT not filled pills (OVERDUE red, times amber); category on task-check border not a 4px left bar; tab "Schedule" not "Calendar"; 0 inline styles.
- CSS ADDED (real): components.css .voice-mic .ic{20px} (export gap, same as earlier nav/tab icon fixes). Build OK.
- DEFERRED/flagged: sort strip (needs RULE 9 component, catalog draws it inline-styled), header count subtitle (redundant w/ chips), long-press row menu (behavior, gate later).
- DESIGN GATE RESUBMITTED: tasks-page-preview.html (catalog-faithful, dark+light) + tasks-page-gate.md (value-to-token, 0/0/0 scan, three offenders, catalog-vs-rules table, diff vs #51). NOT self-passed. AWAITING DAVE visual confirm.
- Service (TasksService + grouping + 55 tests) UNCHANGED and still valid; filter chips derive from grouping (overdue = today-group items with urgency kind 'overdue'). After design approval: build TasksPage.tsx (chips drive filter; tap-toggle + inline edit) + add voice-bar to shell + wire tasks tab.
- LESSON for all remaining pages (Today/Goals/Brain/Schedule/empty-states): START from the matching catalog file, translate to canonical, apply binding rules, gate. Do not improvise.

## TASKS FEATURE: BUILT + WIRED (design approved by Dave: two-line rows, single-row chips). React build:
- app/src/tasks/filters.ts: pure filterOf/partition for the 4 chips (Today/Overdue/Upcoming/Done; overdue split out of today's group for the chip view). +2 tests.
- app/src/tasks/screens/TasksPage.tsx: nav-large "Tasks" + nav-action(+); chip-row filters w/ counts (active highlighted); card of two-line task-rows = task-check (cat-bd-{cat} tap-to-toggle; .done green) + row-stack[editable conn-name title + eyebrow CATEGORY] + urgency text on right (urgencyFor -> urgency-red overdue / urgency-warn today / urgency-muted soon; none if no due/done). Empty-state per filter. Inline title edit (contentEditable, save on blur). META DECISION: meta shows CATEGORY only (not "CATEGORY · day"); the due timing is the colored urgency tag on the right (tasks carry a date not a clock time, so one timing slot, no duplication). If Dave wants the day in the meta too, trivial add.
- app/src/tasks/TasksFlow.tsx: tasks tab state; loads+partitions; seeds 6 demo tasks (today x2, overdue x1, upcoming x2, done x1; varied cats tucci/money/elite/family/friends); handlers toggle/editText/new (new = "New Task" due today, inline-edit; proper add sheet DEFERRED). filter state.
- app/src/data/NotesProvider.tsx: REFACTORED to one shared store providing BOTH NotesService + TasksService (+useTasks). Critical so note-checklist tasks appear in Tasks (same in-memory adapter in demo / same DB on device).
- VOICE BAR WIRED: app/src/shell/VoiceBar.tsx (canonical .voice-bar, lucide Mic, "JARVIS"/"Tap to speak", inert tap for now - voice feature is Milestone C). AppShell docks VoiceBar + TabBar at bottom on main tab screens (showDock = tab bar visibility; hides on pushed notes sub-screens). components.css: .voice-dock{pb s-3} + button.voice-bar reset.
- AppShell: tasks tab -> TasksFlow (was Placeholder); today/schedule/brain still Placeholder.
- CSS added earlier this feature: .chip-row (single-row scroll filter strip, RULE 9 component), .voice-mic .ic{20px}.
- RESULT: 57/57 tests, tsc clean, build OK, 0 em dashes. Demo rebuilt (tap Tasks tab; voice bar shows above tab bar).
- DEFERRED (Tasks): proper add sheet (title+date+category), sort strip (needs RULE 9 component), long-press row menu (snooze/edit/delete), due-date editing UI, header count subtitle. Day-in-meta optional.

## SCHEDULE FEATURE: behavior approved + service built. Switched from Today after reading catalog 19 (Today is an aggregator of 7 sections; 6 need unbuilt features; only Tasks has data). Schedule is Today's main feeder (the Up Next timeline) and self-contained, so it goes first.
- Catalog 22 design: month calendar grid (day cells + colored event dots, month nav, today + selected-day highlight) + selected-day timeline (time + title + category pill; canonical sched-row primitive). Uses pre-canonical classes/inline styles -> translate to canonical; month-grid has NO canonical component (flag RULE 9 at design gate), timeline reuses canonical .sched-row/.sched-time.
- BEHAVIOR: approved. schedule-harness.html (13 steps) tapped through + approved.
- SERVICE BUILT: app/src/schedule/: types.ts (ENTITY_EVENT="event"; EventData {title,date(YYYY-MM-DD),start(HH:MM 24h),category,end?}; EventItem), calendar.ts (pure: todayISO, fmtTime 24->12h, eventsForDate sort, dotsForMonth day->categories, monthMatrix 42-cell grid), ScheduleService.ts (async over Store on ENTITY_EVENT: createEvent reject-empty/require date+start, editTitle/editTime/moveDay/editCategory/patch, deleteEvent, event, listEvents, eventsOn, daysWithEvents, countOn; offline passthrough), schedule.test.ts (11 spec steps mirroring harness + 5 calendar-helper tests). RESULT: 73/73 tests, tsc clean, 0 em dashes.
- NEW ENTITY 'event': in-memory dev + demo work WITHOUT a migration. Device deploy needs migration 0003 to seed entity_type 'event' (Dave runs SQL, like 0002) - DEFERRED until Schedule goes to device.
- NOT DONE: ScheduleService not wired (schedule tab still Placeholder). NEXT: design the calendar+timeline (catalog 22, canonical, propose calendar-grid RULE 9 component) -> design gate -> wire schedule tab + ScheduleProvider sharing the store.

## TITLE CASE PASS (Dave: "make sure title case is applied"). Rule: Title Case for page/section/card/row titles; ALL CAPS only eyebrow/pill/urgency/tier/time; body=sentence case; minor words (to/with/from/re) stay lowercase. Fixed demo/sample TITLES (row titles) that were sentence case:
- TasksFlow seed (live): Send Q3 Budget to Alberto / Submit Expense Report / Reply to Wei re: Partner Call Prep / Book Flights to NYC / Coffee with Henry / Review Goldman Pitch Deck.
- Tasks previews (fullscreen + device): Long Run, 8 Miles / Confirm Dinner Reservation / etc.
- Schedule harness + spec/tests: Board Prep Call / Lunch with Mom / Tempo Run, 5 Miles / BFFSA Board Prep.
NOTE: this is for DEMO/SAMPLE data + UI chrome. Real user-typed titles are preserved as entered (we do not auto-transform user input). Checklist items / body text stay sentence case per the rule.

## SCHEDULE DESIGN: SUBMITTED (gate), awaiting Dave. schedule-page-fullscreen.html (toggle dark/light) + schedule-page-preview.html (device frames) + schedule-page-gate.md. Catalog-22-faithful in canonical: nav-large Schedule + add; cal-nav (May 2026 + prev/next); month grid (cal-* RULE 9 component); category dots (cat-bg-*); today=accent text, selected=accent fill (accent IS the JARVIS red, so matches catalog's red AND the interactive=accent rule); Selected Day eyebrow + count; timeline = canonical sched-row/sched-time/sched-body/sched-title + sched-cat (dot+name); voice bar + tab bar. RULE OVERRIDES of catalog: category=dot not filled pill; tab Schedule not Calendar; 0 inline styles. CSS added to components.css: .cal-nav/.cal-steps/.cal-step/.cal-grid/.cal-wd/.cal-cell(.out/.today/.sel)/.cal-dots/.cal-dot/.sched-cat (RULE 9, tokens only; flagged non-token glyph/control sizes: aspect-ratio 1, cal-step 32/16, dots 5/8px, tnum). Build OK. NOT self-passed. After approval: build SchedulePage.tsx (month grid + day timeline, tap day to select, month nav, + add) + ScheduleProvider sharing store + wire schedule tab.

## SCHEDULE: BUILT + WIRED (design approved). React build:
- app/src/schedule/screens/SchedulePage.tsx: nav-large Schedule + add; cal-nav (month/year + prev/next ChevronLeft/Right); cal-grid from monthMatrix (weekday headers + 42 cells; out-of-month .out; today .today accent; selected .sel accent fill; category dots cat-bg-* hidden on selected cell); Selected Day eyebrow "Mon D · N Events"; timeline card of sched-row (sched-time fmtTime 12h + sched-body[sched-title + sched-cat dot+name]); empty-state when no events.
- app/src/schedule/ScheduleFlow.tsx: view {y,m} + selected day state; loads daysWithEvents(view) + eventsOn(selected); month stepper (wraps year); onSelect sets day; onNew creates "New Event" on selected day 09:00 brain (proper add sheet DEFERRED); seeds 6 demo events relative to today (today x3 tucci/family/health, +2 elite, +5 friends, -4 money).
- data/NotesProvider.tsx: now also provides ScheduleService from the shared store (+useSchedule).
- AppShell: schedule tab -> ScheduleFlow (was Placeholder). Remaining placeholders: today, brain.
- RESULT: 73/73 tests, tsc clean, build OK, 0 em dashes. Demo rebuilt (tap Schedule: month calendar w/ dots, tap a day, month nav, + adds an event on the selected day).
- DEFERRED (Schedule): proper add/edit sheet (title+date+time+category), event detail/edit/delete UI, tap event row to edit, week view, device migration 0003 (seed entity_type 'event') when deploying to device.
- TABS LIVE NOW: Tasks, Schedule, More(Notes). Placeholders: Today, Brain. Next sensible: Today (now has Tasks + Schedule data to aggregate) or Brain.

## TODAY HOME: BUILT + WIRED (design approved by Dave across several passes). The daily-driver landing.
- DESIGN JOURNEY (Dave-driven): v1 two-section -> "show everything" full page -> Dave: "not the approved design", cross-referenced catalog 19 vs JARVIS_Today_Light_Realistic.html (they disagree: catalog 7 sections/no icons/Your Day ticker; mockup 5 sections/colored icons/Suggests-first). Dave chose MERGE: mockup's clean layout + colored section icons + catalog's extra sections (Tomorrow/Projects/Brain) folded in, filling as features land. Then: added greeting hero (Dave: "feel like a homepage"); fixed urgency font 15->11 (was oversized, app-wide); improved schedule time readability (tx-3->tx-2, weight, ampm 10->11, tabular, app-wide on .sched-time); added the Your Day AUTO-SCROLL TICKER + pause button (catalog mechanism).
- FINAL DESIGN (8 sections, priority order): greeting hero (avatar bar + "Good Morning, Dave" + date + day-summary line) -> JARVIS Suggests -> Your Day (ticker) -> Today's Tasks -> Tomorrow -> Active Projects -> Life Map -> Recent Messages -> From Your Brain. LIVE-DATA sections built now: Your Day, Today's Tasks, Tomorrow (Schedule+Tasks). The other 5 are designed but render only when their features land (data-driven; not shown with fake data in the real app).
- AUTO-SCROLL TICKER (Your Day): CSS-driven loop (catalog 19). .sched-ticker (252px window, overflow hidden, top/bottom fade via ::before/::after to surface-1) + .ticker-track (animation tickerScroll 40s linear infinite, translateY 0 -> -50%, duplicated day set for seamless loop) + .paused toggles animation-play-state. .ticker-toggle (26px circle, pause/play icon swap). prefers-reduced-motion: animation none. .now-line (accent "Now H:MM" + rule) + .sched-row.past (dim 0.45). React: YourDay.tsx measures single-set height; only animates + shows pause toggle if it overflows 252px, else static card. Pause = local useState.
- FILES: app/src/today/ greeting.ts (greetingFor by hour + longDate/shortDate, deterministic) / todayData.ts (tomorrowISO, nowHHMM, daySummary, todaysTasks=overdue+due-today, nowIndex/isPast for the now-line) / YourDay.tsx (ticker section, overflow-aware, owns pause) / TodayPage.tsx (presentational: hero + YourDay + Today's Tasks + Tomorrow; sec-head w/ colored sec-ico) / TodayFlow.tsx (reads useSchedule+useTasks; eventsOn today/tomorrow + listTasks; greeting/summary; passes to page) / today.test.ts (7 tests). app/src/shared/categories.ts (CAT_LABEL/catLabel, shared).
- CSS added (components.css, RULE 9, tokens only): .today-bar/.today-hero/.today-title/.today-summary (greeting hero); .sec-head + .sec-left + .sec-ico (+ .ico-accent/-blue/-good) (section header w/ colored icon); .see-all; .sched-ticker + ::before/::after + .ticker-track + @keyframes tickerScroll + .ticker-toggle (+icon-play/pause) + reduced-motion; .now-line/.now-label/.now-rule; .sched-row.past; .screen-foot; .soon (preview badge only). jarvis-design-system.css: .urgency 15->11px (eyebrow); .sched-time tx-2 + w-regular + tabular + ampm 11px (readability fix, app-wide).
- CENTRAL DEMO SEED (new): app/src/data/seed.ts seedDemoData(tasks,schedule) - idempotent, relative dates (6 today events incl past+upcoming so ticker overflows + now-line shows; 2 tomorrow; 1 overdue + 2 due-today tasks). AppShell now seeds ONCE on mount (demo mode) + gates render with `ready` flag, so Today has data even if opened first. TasksFlow/ScheduleFlow NO LONGER passed seed (central owns it); their inline seed code is now dead (left in place, harmless). NotesFlow still seeds its own notes.
- AppShell: today tab -> TodayFlow (onGoSchedule/onGoTasks switch tabs for See All). Remaining placeholder: Brain only.
- RESULT: 80/80 tests (engine 23 + events 8 + notes + tasks + schedule + today 7), tsc clean, build OK, 0 em dashes. Demo rebuilt: jarvis-notes-demo.html (tap Today: greeting, auto-scrolling Your Day w/ working pause, today's tasks, tomorrow).
- DESIGN GATE artifacts current: today-page-fullscreen.html / today-page-preview.html / today-page-gate.md (merged 8-section design + ticker).
- TABS LIVE NOW: Today, Tasks, Schedule, More(Notes). Placeholder: Brain. The Milestone-A daily-driver loop (Today/Tasks/Schedule/Notes) is functional.
- DEFERRED (Today): wire the 5 sample sections as features land (Suggests=AI/Milestone C; Active Projects; Life Map=Goals/catalog 21; Recent Messages; From Your Brain=catalog 23). Optional: real user name/avatar (hardcoded "DF"); the catalog's free-time gaps in the ticker; "coming soon" teaser decision for unbuilt sections (the preview "soon" badge is preview-only).

## TEST EVERYTHING PASS (Dave: "TEST EVERYTHING"). Closed the coverage gaps end to end.
- UNIFIED HARNESS (jarvis-test-harness.html): rebuilt to the LOCKED test-harness-v2 format (one app + feature picker + coverage checklist + ordered pass/fail steps + live state), replacing the 3 separate single-feature harness files. Now covers Core, Notes, Tasks, Schedule, AND Today (Today had none). Headless-verified: 53/53 steps pass, 51/51 coverage items green, every feature ends ALL-PASS. Each feature has core + edge/failure steps (empty input, reject bad input, delete-twice, offline->reconnect, tombstone). Money stub = next.
- AUTOMATED SUITE EXPANDED 80 -> 130 (107 app + 23 engine), tsc clean both packages:
  * UI render tests (jsdom + testing-library, per-file @vitest-environment): TodayPage (greeting/summary/overdue-red/urgency colors/section show+hide), YourDay (static vs overflow ticker via mocked scrollHeight; WORKING pause toggle; now-line; past dim; empty), TasksPage (chips+counts/rows/urgency/empty/onToggle), SchedulePage (42-cell grid/timeline+cat dot/empty/month-nav). +16.
  * Supabase ADAPTER tests (app/src/data/supabaseAdapter.test.ts) via a mock supabase-js client (no network): create inserts {entity_type,data} into 'item' + returns id; read maps row->Item (owner_id->ownerId, updated_at->epoch serverTime); read null when absent; apply calls rpc item_apply_patch {p_id,p_patch} -> bool; del is HARD delete (no update/tombstone); listForUser maps rows; throws on backend error. +7. (Proves the real-backend code path's contract; live RLS/sync still device-only.)
  * INTEGRATION tests (app/src/integration.test.ts) over ONE shared Store: note checklist -> tasksFromChecklist -> appear in TasksService.listTasks linked+categorized; deleting note leaves tasks (one-way link); seedDemoData feeds Today (6 today events, 1 overdue + 2 due-today tasks, todaysTasks=3); seed idempotent (9 events / 5 tasks on double run). +4.
- MIGRATION 0003 (jarvis-core/supabase/migrations/0003_event_entity.sql): registers entity_type 'event'. Dave runs in Supabase SQL editor before Schedule works on device (Notes/Tasks already covered by 0002).
- DEVICE_TEST_CHECKLIST.md: the one layer the sandbox can't run (live Supabase = no network). Steps: apply 0003, auth, CRUD across all 4 features, hard-delete-survives-reload (tombstone), offline->reconnect, RLS two-account isolation, optional docs/verify-rls.mjs.
- COVERAGE NOW: behavior (harness, all green) + logic/services (vitest) + UI render (vitest jsdom) + backend adapter contract (vitest mock) + cross-feature integration (vitest) automated. Live Supabase (auth/RLS/sync/realtime) = device checklist (cannot run without network). No silent gaps.

## BRAIN HUB: BUILT + WIRED (design approved by Dave incl colored icons). Completes all 5 Personal tabs.
- Catalog 23 = navigation hub (no card stats): 4 row-sections, each row opens a detail (chev); Setup rows show status. The hub has no data logic; detail pages behind each row are future features.
- DESIGN approved: nav-large "Brain" + subtitle; sections (sec-head title-only, Title Case) -> card of rows = colored icon tile + name + chev (+ status for Setup). Sections: Who You Know (Contacts/Inner Circle/Adversarial), How You Think (Life Philosophy/How You Write/Values), Org Context (BFFSA/Tucci Baseball/Elite Squad), Setup (Onboarding=Complete, Backup=On).
- DAVE FIX: "icons have no color" -> every row icon now a distinct colored tile (iOS-Settings style), all palette tokens: contacts ico-blue, inner-circle cat-family, adversarial ico-accent, philosophy cat-brain, writing cat-health, values cat-money, bffsa/tucci/elite their cats, onboarding ico-good, backup cat-friends. No grey icons (standing rule).
- FILES: app/src/brain/BrainPage.tsx (presentational hub, inline SVG icons to match preview, rows as div role=button calling onOpen), BrainFlow.tsx (hub + lightweight "coming soon" detail w/ nav-back; tab dock stays like iOS drill-down), BrainPage.test.tsx (4 render tests: 4 sections, 11 rows all colored / none ico-surface, 2 status rows, onOpen fires).
- CSS added (components.css, RULE 9, tokens): .ico-surface (defined, now unused on Brain), .row-grow (name fills, pushes chev right), .row-status (trailing status, fg-good).
- AppShell: brain tab -> BrainFlow; Placeholder import removed. NO PLACEHOLDERS LEFT - all 5 tabs (Today/Tasks/Schedule/Brain/More) are real screens.
- RESULT: 111 app tests (+4 Brain) +23 engine = 134 total, tsc clean, build OK, 0 em dashes. Demo rebuilt (tap Brain: 4 colored sections; tap a row -> coming-soon detail -> back).
- DEFERRED (Brain): the real detail pages (Contacts list, Inner Circle, Adversarial, Life Philosophy, How You Write, Values, per-org context, Onboarding flow, Backup settings) - each a future feature. The "From Your Brain" knowledge surface (Today section) is separate.
- MILESTONE A SHELL COMPLETE: all 5 Personal tabs built, gated, wired, tested. Daily-driver loop fully navigable.

## USER-DEFINED CATEGORIES REFACTOR (multi-user). Dave: categories were hardcoded to his orgs; make them user-defined. Chose: do it now. Phased, each phase green.
### PHASE 1 DONE (foundation, screens untouched, build green):
- WHY: category color tokens + labels were named after Dave's orgs (--cat-tucci/elite/bffsa, CAT_LABEL tucci->"Tucci" etc.), not multi-tenant. Sample CONTENT (names, "Tucci Baseball"/"Elite Squad"/"BFFSA") is just placeholder demo data (fine). The category SYSTEM was the real issue.
- NEUTRAL PALETTE (8 slots, same values as originals): red #FF1F30, sky #5AC8FA, pink #FF85B8, yellow #FFCC00, green #34D572, blue #5E9BFF, teal #4FE0BE, graphite #8E8E93. Added to components.css as :root tokens + .cat-bg/bd/fg-{slot} (light fills sky/yellow/teal carry on-fill-dark glyph). ADDITIVE - old org classes still present (removed in Phase 3).
- MODEL: app/src/categories/types.ts (ENTITY_CATEGORY="category"; ColorSlot union of 8; COLOR_SLOTS[]; CategoryData{name,color,icon?,order}; Category{id,data}). defaults.ts (TemplateKey personal|business|student; DEFAULT_CATEGORIES per template, generic + user-editable: personal=Work/Family/Health/Money/Friends/Personal; business=Clients/Operations/Finance/Team/Sales/Admin; student=School/Team/Family/Coach/Friends/Health; each {name,color,icon}). CategoriesService.ts (over Store, entity 'category'; constructor(store,ownerId,onEvent?); list() sorted by order, get, create(name,color,icon?) reject-empty, rename/recolor/setIcon (patch->boolean), remove, reorder(ids[]), seedDefaults(template) idempotent-if-empty). categories.test.ts (9 tests: create/list/order, reject empty, rename/recolor/seticon, rename reject empty+unknown, remove, reorder, seedDefaults idempotent, all defaults valid slot, per-user isolation).
- MIGRATION 0004_category_entity.sql (registers entity_type 'category'; mirrors 0002/0003; Dave runs in SQL editor).
- RESULT: 120 app tests (+9) + 23 engine = 143 total, tsc clean, build OK, 0 em dashes.
### PHASE 2 NEXT (migrate screens + data to id-based categories):
- Items move from data.category=hardcoded-key to data.categoryId (id of a Category). Wire a CategoriesService into the shared provider (useCategories) + a catColor(id)->slot / catName(id) resolver via context.
- Replace each screen's CAT_LABEL/CATEGORY_LABEL map + cat-bg-{hardcodedKey} with dynamic lookup (Notes/Tasks/Schedule/Today/Brain). Tile = cat-bg-{category.color slot}; label = category.name.
- Update central seed.ts: seedDefaults("personal") then tag demo items by category id. Brain "Org Context" section becomes dynamic (user's categories), not fixed BFFSA/Tucci/Elite.
- AppShell: seed categories on first run alongside demo data; pass template (default "personal").
- Update component tests + harness to pass category ids / a categories map.
### PHASE 3 NEXT (cleanup): remove old org-named tokens/classes (--cat-tucci/elite/bffsa + cat-bg/bd/fg-{tucci,elite,bffsa,health,family,money,brain,friends}) once nothing references them; keep only neutral slots.

### PHASE 2 DONE (screens + demo migrated to user-defined categories; build green):
- RESOLVER: shared/categories.ts rewritten -> runtime registry. setCategoryRegistry(cats) (called once at startup by AppShell), catColor(ref)->ColorSlot (registry id -> slot; falls back to value if already a slot, else graphite), catName(ref)->display name. catLabel kept as alias. Old CAT_LABEL map removed.
- PROVIDER: NotesProvider now also builds CategoriesService + exposes useCategories().
- APPSHELL BOOTSTRAP: ready gate starts false in ALL modes; effect runs before any tab renders: categories.seedDefaults("personal") -> list -> setCategoryRegistry(...) -> if seedDemo, seedDemoData(tasks,schedule,cats). Guarantees colors+names resolve before first paint (demo + real).
- SEED genericized + id-tagged: data/seed.ts seedDemoData(tasks,schedule,cats:Category[]) resolves category id by NAME (Work/Friends/Health/Money/Family from Personal defaults). Content de-Daved (Morning Standup/Client Call/Lunch With Sam/Team Sync/Gym/Evening Run/Dentist/Call With Alex/Budget Review; tasks Reply to Sam/Send Invoice/Submit Expense Report/Book Flights/Coffee With Alex). Counts unchanged (9 events, 5 tasks, Today 6/1/2/3).
- SCREENS migrated to catColor/catName: TasksPage, SchedulePage, YourDay, TodayPage (removed local CAT_LABEL maps). Notes: NotesFlow uses useCategories + catName, seeds generic notes (Quarterly Planning/Training Plan/Home Projects/Outreach List/Standup Notes) tagged by category id; new notes default to first category id; Connections/CreateTasks/NotesList/NoteEditor item tiles use catColor.
- ITEM MODEL: items still carry data.category but it now holds a CATEGORY ID (opaque), resolved via registry. (Field NOT renamed to categoryId to limit churn; documented. Optional later rename.)
- DECORATIVE TILES -> SLOTS: every literal cat-bg/bd/fg-{oldkey} in code converted to neutral slot (tucci->sky, elite->red, family->pink, money->yellow, health->green, brain->blue, friends->teal, bffsa->graphite): Templates, AddBlockSheet, Connections, CreateTasks(comment), SignIn, BrainPage org tiles. So nothing in code references old org classes (only CSS still defines them -> Phase 3 can delete).
- DEAD SEED REMOVED: TasksFlow/ScheduleFlow inline org-named demo seeds deleted (were dead; central seed owns demo); their onNew stubs now create uncategorized ("" category) items until the add/edit sheet lands. seed props removed from both Flows.
- TESTS: 3 render tests (TasksPage/SchedulePage/TodayPage) now seed the registry (old key -> slot map) and assert resolved slot classes; integration test seeds categories then seedDemoData(...,cats). 120 app tests + 23 engine = 143 total, tsc clean, build OK, 0 em dashes.
- FLAGGED (not changed, needs Dave's eye): Brain "Org Context" section still shows sample rows BFFSA/Tucci/Elite (now slot-colored). Should become DYNAMIC (render the user's actual categories) - small follow-up, but it's a visible change to the approved Brain screen so left for approval.
### PHASE 3 REMAINING (cleanup): delete old org tokens --cat-{tucci,elite,bffsa,family,money,health,brain,friends} + .cat-bg/bd/fg-{those} from jarvis-design-system.css/uniformity.css (nothing in code references them now; verify cat-dot/cal-dot still fine). Keep only the 8 neutral slots.

### PHASE 3 DONE (cleanup) + BRAIN ORG CONTEXT NOW DYNAMIC:
- CSS CLEANUP: removed old org-named tokens (--cat-elite/tucci/family/money/health/brain/friends + --cat-bffsa/-on, both themes) from jarvis-design-system.css and all .cat-bg/bd/fg-{old} classes + the tucci on-fill + unused .pill-bffsa from uniformity.css. Repointed .brain-kicker (unused) from --cat-brain to --cat-blue. Verified zero var(--cat-{old}) refs remain (only a comment). Only the 8 neutral slots remain. tsc clean, both themes build OK.
- BRAIN "Org Context" -> "Your Categories" (DYNAMIC): BrainPage.tsx now takes categories?: BrainCategory[] and renders a live "Your Categories" section (between How You Think and Setup) from the user's own categories: colored slot tile (cat-bg-{slot}) + category icon (CAT_ICON map: briefcase/heart/dumbbell/wallet/users/user/settings/folder/trending-up/book/trophy, fallback Tag) + name + chev. Static sections (Who You Know, How You Think, Setup) unchanged; their decorative tiles now use slots. No hardcoded BFFSA/Tucci/Elite anywhere. BrainFlow.tsx loads categories via useCategories and passes them; tapping a category row opens the same coming-soon detail.
- BrainPage.test.tsx: 6 tests (static+dynamic sections, omits Your Categories when empty, 11 tiles all colored / none ico-surface, each category tile its own slot, 2 status rows, onOpen fires for static + category rows).
- ARTIFACTS: brain-page-fullscreen.html regenerated (shows Your Categories w/ Personal defaults); jarvis-notes-demo.html rebuilt (live Brain has dynamic Your Categories from seeded Personal categories).
- RESULT: 122 app tests (+2 Brain) + 23 engine = 145 total, tsc clean, build OK, 0 em dashes.
- CATEGORIES REFACTOR COMPLETE (all 3 phases). The category system is fully user-defined + multi-tenant: neutral palette, per-user Category records, template defaults, dynamic resolution everywhere, no org names baked into architecture. Remaining category work is just UX: a category management/edit screen (rename/recolor/add/remove/reorder UI) + onboarding template pick - both future features. Optional later: rename item field data.category -> data.categoryId.

## TASKS ADD/EDIT SHEET: BUILT + WIRED (design approved). First add/edit sheet; unblocks creating/editing tasks with category + due.
- Behavior already existed + tested in TasksService (createTask/editText/setCategory/setDue/deleteTask/toggleDone) -> this was UI + wiring only. Also fixed createTask default category "brain" -> "" (latent old key).
- DESIGN approved: bottom sheet (sheet-scrim + card + sheet-handle). Fields: Task (.input required, .input-error "Add a task name." if empty), Category (.chip-row of user categories; unselected chip = cat-dot + name; SELECTED chip = filled with its own slot color, no dot), Due (.segmented None/Today/Tomorrow/Pick; Pick reveals .input[type=date]). Actions: Save (.btn-primary), Delete Task (.btn-danger, edit only), Cancel (.btn-secondary), all .btn-block stacked.
- CSS added (components.css, RULE 9, tokens): .sheet-form, .field(+:last-child), .sheet-actions, .cat-pick (chip row bleeds to card edge), .field-gap. Reuses canonical .input/.input-label/.input-req/.input-error, .segmented/.seg, .chip/.chip-row/.cat-dot, .btn*, .sheet-scrim/.sheet-handle.
- FILES: app/src/tasks/screens/TaskSheet.tsx (mode new|edit; props initial?,categories:SheetCategory[]{id,name,color:ColorSlot},onSave(TaskDraft{text,category,due}),onDelete?,onCancel; local form state; dueMode derived none/today/tomorrow/pick; todayISO/addDaysISO local). TaskSheet.test.tsx (5: new header/no-delete, empty blocks+error+trim save, category select wears slot color + saves id, due quick-pick sets date, edit prefilled+delete fires).
- WIRING (TasksFlow): + opens New sheet; TAPPING A TASK ROW opens Edit (prefilled, with Delete). New -> createTask(text,{category||undefined,due||null}); Edit -> editText+setCategory+setDue; Delete -> deleteTask; all then reload. Loads categories via useCategories.
- TasksPage CHANGE: removed inline contentEditable title (the sheet is the full editor now); row title is plain; tapping the row-stack opens edit (onOpenTask); check toggle stops propagation. onEditText prop -> onOpenTask. Removed Editable component + useEffect/useRef import. (TasksPage.test only used onToggle; still green.)
- RESULT: 127 app tests (+5) + 23 engine = 150 total, tsc clean, build OK, 0 em dashes. Demo: tap + or any task to create/edit.
- ARTIFACTS: tasks-sheet-fullscreen.html / -preview.html / -gate.md in outputs.
- NEXT: matching Schedule add/edit sheet (createEvent/editTitle/editTime/moveDay/editCategory/deleteEvent all exist+tested); + opens New, tap event row opens Edit. Fields: Title + Date + Start time + Category.

## SCHEDULE ADD/EDIT SHEET: BUILT + WIRED (design approved). Daily-driver create/edit loop now complete for Tasks AND Schedule.
- Behavior pre-existing + tested in ScheduleService (createEvent/editTitle/moveDay/editTime/editCategory/deleteEvent/event) -> UI + wiring only. Fixed createEvent default category "brain" -> "".
- DESIGN approved: same bottom-sheet pattern as Tasks. Fields: Title (.input required), Date + Start in a .field-row (native .input[type=date] + [type=time]), Category (.chip-row, selected = own slot color). Single .input-error ("Add a title, date, and start time.") if any required missing. Actions Save/.btn-danger Delete Event (edit)/Cancel.
- CSS added (components.css, RULE 9): .field-row (two fields side by side, children flex:1 margin-bottom:0). Reuses all Tasks-sheet + canonical classes.
- FILES: app/src/schedule/screens/EventSheet.tsx (mode new|edit; EventDraft{title,date,start,category}; reuses SheetCategory type from TaskSheet; local state; new default start 09:00). EventSheet.test.tsx (4: new header/no-delete, blocks-until-title + saves draft, category switch wears slot + saves id, edit prefilled + delete fires).
- WIRING (ScheduleFlow): + opens New (initial date = selected day, start 09:00); TAPPING AN EVENT ROW opens Edit (prefilled, with Delete). New -> createEvent; Edit -> editTitle+moveDay+editTime+editCategory; Delete -> deleteEvent; then reload. Loads categories via useCategories.
- SchedulePage: sched-row now role=button onClick onOpenEvent(id); added onOpenEvent prop.
- RESULT: 131 app tests (+4) + 23 engine = 154 total, tsc clean, build OK, 0 em dashes. Demo: Schedule tab, tap + or any event to create/edit.
- ARTIFACTS: schedule-sheet-fullscreen/-preview/-gate in outputs.
- DAILY-DRIVER LOOP COMPLETE: Today/Tasks/Schedule/Brain/Notes all real; tasks + events can be created/edited/deleted with category + date/time; categories user-defined. App is now fully runnable for a day (in-memory demo; real Supabase pending device test incl migrations 0003+0004).
- REMAINING POLISH (deferred, not blocking daily use): Tasks sort strip / long-press; Schedule week view + tap-empty-slot-to-add; Notes connections editing + list visual; category management screen (rename/recolor/add UI); onboarding template pick. Then Milestone B (Business/Student templates, Clerk/Stripe) + ship prep (device checklist, Apple Developer, App Store Connect).

## CONVERSATIONAL ONBOARDING: BUILT + WIRED (design approved; redone from a thin form after Dave flagged it must be conversational + thorough, per master prompt "Build your Brain").
- PROFILE STORE: app/src/profile/ types.ts (ENTITY_PROFILE="profile"; ProfileData{name,template:TemplateKey,people[],briefTime?,gmail,calendar,onboarded}; EMPTY_PROFILE), ProfileService.ts (single per-user record over Store; get/isOnboarded/save(patch) create-or-update, no dup). profile.test.ts (3). Migration 0005_profile_entity.sql (entity_type 'profile'; Dave runs in SQL editor). Provider: useProfile() added.
- STEP CONFIG: app/src/onboarding/steps.ts (STEPS: intro, name(text), template(choice personal/business/student), categories, people, connect, time(brief 6/7/8am), done). Data-driven so AI can make it dynamic later.
- FLOW ENGINE: app/src/onboarding/OnboardingFlow.tsx (conversational: JARVIS .bubble-ai turns + user .bubble-user answers accumulate in a .convo transcript; bottom .convo-foot control per kind: text=input+convo-send, choice/time=chips, categories=removable seed rows + Add Category, people=add chips + skip/continue, connect=Gmail/Calendar toggle chips). Intro = branded "Build your Brain" screen (red J wordmark, 3 steps card, privacy note, Begin + Skip). On finish: profile.save({name,template,people,briefTime,gmail,calendar,onboarded:true}) + create kept category seeds (if none yet). Skip = save onboarded + bail (AppShell seeds template defaults). onboarding.test.tsx (3: full walk -> onFinish, intro Skip -> onFinish, remove+add category).
- CSS (components.css, RULE 9, tokens): .jr, .convo/.convo-sender/.convo-foot/.convo-inputbar/.convo-send/.convo-chips, .ob-num, .ob-privacy(+ic/-txt), .ob-skip, .connect-row, .ob-brand-lg, .ob-tagline-1, .ob-host, plus earlier .ob-* layer. Reuses .bubble*/.convo-actions, .chip, .btn, .input, .card, .row, .sec-ico, cat-bg-{slot}.
- WIRING: App.tsx -> AppGate (inside provider): profile.isOnboarded() ? AppShell : OnboardingFlow (in .ob-host). Both demo + session modes gated. AppShell bootstrap now seedDefaults(profile.template ?? "personal") (was hardcoded personal). TodayFlow reads profile.name -> greeting "Good X, <name>" + avatar = initials (fallback "JV"); de-Daves Today (was hardcoded). 
- DEMO behavior: no-backend demo now opens onboarding first (in-memory, no profile); tap through or Skip -> seeded app. Showcases the real built onboarding.
- EARLIER thin-form gate artifacts (onboarding-*.html) replaced with the conversational mock; FLAGGED earlier centering bug fixed (ob-screen flex fill).
- RESULT: 137 app tests (+6) + 23 engine = 160 total, tsc clean, build OK, 0 em dashes.
- DEFERRED: live Gmail/Calendar OAuth (Milestone B; captured as preference flags now); how-you-write/values turns (same pattern, add when Brain detail pages land); people -> real Contacts/Inner Circle records (stored on profile now); category rename/recolor in onboarding (remove+add only; full editing in the category management screen later); briefTime -> actual scheduled brief (needs notifications, native).

## SETTINGS + CATEGORY MANAGEMENT: BUILT + WIRED (design approved after 4 mock iterations; Dave flagged subtext, action-sheet shell, mock-nav overlap, and a .screen min-height:100vh clip; all fixed in mock then built).
- GLOBAL SHEET FIX (components.css): all bottom sheets now render identically and never clip their actions: ".sheet-scrim > .card { max-height:100%; display:flex; flex-direction:column }", ".sheet-scrim .sheet-form { flex:1; min-height:0; overflow-y:auto }", handle/grp/actions flex-shrink:0. Applies to Task, Event AND Category sheets. (The .screen min-height:100vh clipping was a MOCK-only artifact from a shortened device cell; real app .screen fills the shell.)
- SHARED ICONS: app/src/categories/icons.tsx (CAT_ICONS map for the 11 category icon keys briefcase/heart/dumbbell/wallet/users/user/settings/folder/trending-up/book/trophy + Tag fallback; ICON_KEYS for the picker; catIcon(key)->ReactNode). BrainPage still has its own copies (left untouched to avoid risk; optional later dedup).
- CATEGORY SHEET: app/src/categories/screens/CategorySheet.tsx (mode new|edit; CategoryDraft{name,color:ColorSlot,icon}; Name input required + .input-error "Add a category name."; Color = .swatch-pick of the 8 COLOR_SLOTS (cat-bg-{slot}, .sel ring via --tint); Icon = .icon-pick grid of ICON_KEYS (.icpick, .sel accent); actions Save / btn-danger Delete Category (edit only) / Cancel). CSS added (RULE 9): .swatch-pick/.swatch(+.sel), .icon-pick/.icpick(+.sel), .btn-danger .ic. CategorySheet.test.tsx (3).
- CATEGORIES LIST: app/src/categories/screens/CategoriesPage.tsx (nav-back Categories; card of rows = cat-bg-{color} tile + catIcon + name + chev; Add Category row). CategoriesFlow.tsx wires useCategories: list, tap row -> edit sheet (prefilled), Add -> new sheet; onSave new=create, edit=categories.update(id,{name,color,icon}); onDelete=remove; reloads. CategoriesFlow.test.tsx (1, add end-to-end).
- CategoriesService: added public async update(id, patch:Partial<CategoryData>) that get->merge->store.update (preserves order; trims name). Used by the edit sheet (one merged write instead of 3 partial calls).
- APPEARANCE: app/src/settings/AppearancePage.tsx (nav-back; Theme .seg-card segmented Dark/Light wired to the EXISTING useAppearance provider -> real switch + localStorage persist; provider is multi-axis for skins/gaming mode later).
- PROFILE: app/src/settings/ProfilePage.tsx (nav-back; Name input + Save via profile.save; Template shown read-only in a .input-static field). CSS added: .input-static.
- MORE HUB: the 5th tab is now a hub, not Notes-direct. app/src/more/MorePage.tsx (nav-large More; Your Stuff -> Notes; Preferences -> Profile/Appearance/Categories; About -> Backup/Version, no chev). MoreFlow.tsx manages route hub|notes|profile|appearance|categories with back-to-hub; Notes nested (NotesFlow gained optional onExit -> NotesList gained optional onBack chevron; dock chrome: hub+settings keep tab bar, Notes delegates to NotesFlow.onChrome). AppShell more tab now renders <MoreFlow seed onChrome=setNotesTabBar> (NotesFlow import removed from AppShell). settings.test.tsx (3: Appearance theme switch, Profile save, More hub nav + back).
- IA NOTE surfaced to Dave: Notes is now one tap deeper under More (tradeoff for a real hub); offered the avatar-Settings alternative if he prefers Notes stay the direct 5th tab.
- RESULT: 144 app tests (+7) + 23 engine = 167 total, tsc clean, build OK, 0 em dashes.
- DEFERRED: category reorder (hold-to-drag; service.reorder exists, UI later); Backup + Version rows are static (no backend backup yet); template switching in Profile (read-only now; switching would re-seed/migrate categories - needs a migration story); Appearance skins/gaming mode (provider ready, CSS token blocks + options later).

## EDITABLE TAB BAR: BUILT + WIRED (Dave: "tabs at the bottom I want to be able to page any pages there. Everything else goes into [More]." Chose the editable-tab-bar option.)
- MODEL: ProfileData gained tabs?: string[] (ordered chosen tab keys, persisted + synced). shell/destinations.tsx = central registry of tab-able feature pages: DESTINATIONS [today,tasks,schedule,brain,notes] (key/label/lucide Icon, canonical order), DEFAULT_TABS=[today,tasks,schedule,brain], MAX_TABS=4, destOf(key), extrasFor(tabKeys)->pages not chosen.
- TAB BAR (dynamic): shell/TabBar.tsx rewritten to take {tabKeys, active, onTab}; renders the chosen destinations + a FIXED More tab; highlights the active tab, or More when the active page is not a tab. (Old fixed TABS/TabKey removed.)
- APPSHELL: now key-driven. active:string can be ANY feature key or "more"; tabKeys from profile (or DEFAULT_TABS). Renders the active feature flow (today/tasks/schedule/brain/notes) or MoreFlow. toggleTab(key) enforces min 1 / max 4, persists profile.save({tabs}). Notes rendered here as a feature (gets onChrome for dock; leaving Notes restores dock). showDock = active==="notes" ? notesChrome : true. Default landing = first chosen tab.
- MORE HUB: MorePage.tsx now dynamic: "Your Stuff" = the extras (pages not in the tab bar) each opening full-screen via onOpenExtra (shell sets active, dock stays, More highlighted); "Preferences" = Edit Tabs + Profile + Appearance + Categories; "About" = Backup/Version. MoreFlow.tsx no longer renders Notes (it's a shell feature now); routes hub|profile|appearance|categories|edittabs; props {extras,onOpenExtra,tabKeys,onToggleTab}.
- EDIT TABS: more/EditTabsPage.tsx (nav-back; lists all DESTINATIONS, each a row with the canonical .switch toggle = in/out of tab bar; min 1 / max 4 enforced with a dimmed .switch-locked when toggling would break the limit). Saves through onToggleTab -> AppShell -> profile.
- NOTES back affordance: NotesFlow gained optional onExit -> NotesList optional onBack chevron (added during the More-hub work; with the key-driven shell Notes is reached as a feature and the dock is the way back, so onExit is currently unused but harmless/available).
- CSS: .switch-locked { opacity:.4 } (RULE 9). Everything else canonical (.switch, rows, cards, sec-ico tiles, nav-back).
- TESTS: shell/tabs.test.tsx (4: TabBar renders chosen+More & active highlight, More-highlight when active not a tab; EditTabs toggle calls onToggle, locks last tab + blocks past max). settings.test.tsx +1 (More lists an unpicked page and opens it). 
- RESULT: 149 app tests (+5) + 23 engine = 172 total, tsc clean, build OK, 0 em dashes. No engine/migration change (jarvis-core.zip unchanged).
- DEFERRED: drag-to-reorder tab order (order is canonical for now; toggle-only); making Settings pages (Profile/Appearance/Categories) tab-able too (registry-only change if wanted); per-template default tab sets (e.g., Student lands on Schedule) - currently one DEFAULT_TABS for all.

## DEPLOYMENT-READY (PWA, multi-user): BUILT (Dave: needs the app fully operational for 3-5 daily testers, each their own account, not the master account. Chose PWA-now; App Store/TestFlight later.)
- AUTH (real email login): AuthProvider already wrapped Supabase Auth (getSession/onAuthStateChange/signInWithApple/signInWithEmail-OTP/signOut). ADDED signUpWithPassword(email,pw) (supabase.auth.signUp; falls back to signInWithPassword if no session i.e. email-confirm on) + signInWithPassword(email,pw). SignIn.tsx rewritten: choose screen (Apple + Continue with Email) -> email view with email+password + a Sign In / Create Account toggle + error display + nav-back. Apple still needs Apple Developer setup (testers use email). RLS already in migration 0001 (owner_id default auth.uid(), enable row level security) so each user only sees their own rows. data/store.ts + supabaseClient already env-gated (VITE_SUPABASE_URL/ANON_KEY -> real SupabaseAdapter w/ access token; else InMemory).
- SIGN OUT: wired shell -> MoreFlow -> MorePage as an optional onSignOut (red .row-signout row in More, shown only when backendConfigured). AppShell pulls signOut + backendConfigured from useAuth; passes onSignOut={backendConfigured ? signOut : undefined} (so the in-memory demo + prop-only tests don't show it / don't need AuthProvider).
- PWA: public/manifest.webmanifest (name JARVIS, standalone, theme #0B0B0C, 192/512 + maskable icons), public/icon-180/192/512.png (generated: dark rounded square + red J), public/sw.js (minimal app-shell SW: network-first for navigations + same-origin GET, never intercepts Supabase/cross-origin, offline shell fallback), index.html gained theme-color + apple-touch-icon + apple-mobile-web-app-capable/title/status-bar + manifest link. main.tsx registers /sw.js only on http(s) (skips the file:// single-file demo). app/vercel.json = SPA rewrites all -> /index.html.
- BUILD: production `vite build` (non-singlefile) verified rc=0; dist/ includes index.html + assets + icons + manifest.webmanifest + sw.js. SINGLEFILE demo still builds (in-memory review artifact; auth/signout don't appear there since backendConfigured=false).
- DEPLOY STEPS for Dave (DEPLOY.md in outputs): A) Supabase: run migrations 0003+0004+0005 (0001/0002 already run; all idempotent), enable Email provider + turn OFF Confirm email for instant tester signup, copy Project URL + anon key. B) Vercel: import repo, Root Directory = app, Vite preset, set env VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY, Deploy -> get URL. C) testers: open link in Safari -> Add to Home Screen -> Continue with Email -> Create account. Each user isolated; none touch the master account.
- RESULT: 149 app tests + 23 engine = 172 total, tsc clean, prod build OK, 0 em dashes. No engine change; jarvis-core.zip unchanged EXCEPT Dave still must apply migrations 0003-0005 on the live project.
- STILL TO BUILD (Dave reminder "we still need to build out the rest of the app too"): Brain detail pages (Contacts/Inner Circle/Adversarial/Life Philosophy/How You Write/Values/per-category/Backup), real Backup, voice/AI (Milestone C), Goals/Life Map (catalog 21), Active Projects, Messages, JARVIS Suggests, Today's sample sections wiring, empty states (catalog 24), tab drag-reorder, Apple Sign In config, Terms/Privacy pages, template switching. Onboarding how-you-write/values turns. Notes photo/file/table polish.

## BUILD PLAN LOCKED + PHASE 1 STARTED (AI seam). Dave chose: full app = Personal only complete; AI layer = build now. BUILD_PLAN.md in outputs (Phases: 1 seams, 2 Brain detail pages, 3 AI features, 4 feature screens, 5 polish, 6 ship-prep -> deploy PWA). Native add-ons (Watch/Health/Music/glasses/outside-app voice/AlarmKit) stay in the iOS wrapper phase, not buildable in sandbox.
- AI SEAM (Phase 1, built): 
  - app/src/ai/context.ts: assembleContext(input)->AIContext (pure; filters done tasks, shapes profile/people/categories/tasks/events) + contextToText(ctx) (deterministic compact prompt block).
  - app/src/ai/AIService.ts: client that POSTs to /api/ai; never holds the key. available = injectable (defaults to backendConfigured) so the UI hides AI in the in-memory build. complete(messages, system?) attaches the Supabase bearer, returns data.text. Constructor opts {endpoint,available,fetchImpl,getToken} for testability.
  - app/api/ai.ts: Vercel EDGE function = the ONLY caller of Anthropic. Holds ANTHROPIC_API_KEY (server env), verifies the caller's Supabase token via {SUPABASE_URL}/auth/v1/user (gates to signed-in users), caps max_tokens=1024, model = AI_MODEL || claude-sonnet-4-6, forwards to api.anthropic.com/v1/messages, returns {text}. Untestable in sandbox (no Anthropic net + no function runtime); deploy-tested. Real per-user rate limiting (Supabase-backed counter) deferred to Phase 6/B; current gate = auth + token cap.
  - app/src/ai/ai.test.ts (5): context filtering/defaults; AIService unavailable-refuses, posts-with-auth-returns-text, throws-on-non-ok.
- DEPLOY.md updated: add ANTHROPIC_API_KEY (server-side, optional AI_MODEL) to the Vercel env vars.
- KEY ARCH NOTE: AI key is server-only (Edge function); the PWA never holds it. Cost scales with usage; token cap + auth gate in from day one.
- RESULT: 154 app tests (+5) + 23 engine = 177 total, tsc clean, prod build OK, 0 em dashes. (api/ai.ts is outside src, compiled by Vercel, not app tsc.)
- NEXT: Phase 1 connections seam (generalize Gmail/Calendar flags into a sources model), then Phase 2 Brain detail pages (the AI's memory), then Phase 3 AI features (Quick Capture + JARVIS Suggests).

## PHASE 1 COMPLETE + PHASE 2 KICKOFF (Brain people mock).
- CONNECTIONS SEAM (Phase 1, built): app/src/connections/providers.ts = PROVIDERS registry [gmail/googleCalendar (web), appleMusic (web/MusicKit), appleHealth/appleWatch (native), metaGlasses (native, experimental)] + webProviders()/nativeProviders()/providerOf() + isConnected(profile,key) (reads generic profile.connections map, falls back to legacy gmail/calendar flags). ProfileData gained connections?: Record<string,boolean> (additive; legacy gmail/calendar kept). connections.test.ts (2). Intent: future sources are registry entries + entity records, never a rewrite; kind flag marks PWA-reachable (web) vs native-build-only.
- PHASE 2 KICKOFF: Brain detail = the people set (Contacts/Inner Circle/Adversarial, one shared pattern). MOCK presented for gate (not built yet): brain-people-fullscreen.html + -preview.html + brain-people-gate.md. Surfaces: people list (card of colored-initials avatar rows + Add Person), person detail (av-72 hero + name + attribute card label/value + Notes block + Edit action), empty state. RULE 9 additions proposed (tokens only): .av-72, .person-hero/.person-name, .kv-val, .note-body, .empty-state/.empty-icon/.empty-title. On approval the build adds a "person" entity + People service (create/edit/delete, group=contacts|inner_circle|adversarial), onboarding people seed inner_circle, Brain hub rows open the lists, edit via the sheet family, then people feed the AI context assembler. Text surfaces (Life Philosophy/How You Write/Values) + per-category = a later separate gate.
- RESULT: 156 app tests (+2 connections) + 23 engine = 179 total, tsc clean, 0 em dashes.

## PHASE 2 PEOPLE SET: BUILT + WIRED (Brain people mock approved with "Next").
- ENTITY: app/src/people/types.ts (ENTITY_PERSON="person"; PersonGroup contacts|inner_circle|adversarial; PersonData{name,group,relationship?,birthday?,notes?,order?}; Person{id,data}; GROUP_TITLE; personInitials(name); slotForName(name)->deterministic ColorSlot so avatars are always colored, never grey). Migration 0006_person_entity.sql (entity_type 'person'; Dave runs it). 
- SERVICE: app/src/people/PeopleService.ts (over Store entity 'person'; list(group?) sorted by order then name, get, create(data) reject-empty-name, update(id,patch) get->merge->store.update (trims name, preserves order), remove; Emit narrowed to entity.created|updated|deleted). people.test.ts (3). Provider: usePeople() added.
- SCREENS: PeopleListPage (nav-back + GROUP_TITLE; card of person rows = av-40 cat-bg-{slotForName} initials + name + chev, + Add Person row; canonical empty-state when none). PersonDetail (nav-back + Edit action; person-hero av-72 + person-name; attribute card label/kv-val for Relationship/Birthday; Notes block; sections only render when data present). PersonSheet (sheet family: Name required + .input-error "Add a name.", Relationship, Birthday, Notes textarea .input-multiline; Save/Delete(edit)/Cancel). PeopleFlow(group) wires list<->detail<->sheet over usePeople (create/update/remove + reload). 
- CSS (RULE 9): .av-72, .person-hero/.person-name, .kv-val, .note-body, .empty-state/.empty-icon/.empty-title, .input-multiline.
- WIRING: BrainFlow routes the hub rows contacts/inner-circle/adversarial -> <PeopleFlow group=...> (PEOPLE_GROUP map: inner-circle->inner_circle); other Brain rows keep the placeholder. Onboarding finish seeds inner_circle from profile.people (creates a person per name, only if inner_circle empty).
- TESTS: PersonSheet.test.tsx (3: name required, save payload, edit prefill+delete), PeopleFlow.test.tsx (1: empty -> add -> appears).
- RESULT: 163 app tests (+7) + 23 engine = 186 total, tsc clean, build OK, 0 em dashes. New migration 0006 (Dave applies on the live project alongside 0003-0005).
- NEXT (Phase 2 cont.): text surfaces Life Philosophy / How You Write / Values (doc/editable text pattern) + per-category Brain page, then enrich the AI context assembler to read People + these surfaces. Then Phase 3 (Quick Capture + JARVIS Suggests).

## PHASE 2 TEXT SURFACES: BUILT + WIRED (Life Philosophy / How You Write / Values). Built directly from canonical pattern (titled editable text doc) given the velocity + low novelty; reviewable live in the demo.
- ENTITY: app/src/brain/docs/types.ts (ENTITY_BRAIN_DOC="brain_doc"; BrainDocData{topic,text}; BRAIN_DOCS registry [philosophy/"Life Philosophy", writing/"How You Write", values/"Values"] each w/ a placeholder prompt; docMeta(topic)). Migration 0007_brain_doc_entity.sql.
- SERVICE: BrainDocService (one record per topic per user; get(topic)->text, save(topic,text) create-or-update). Provider: useBrainDocs().
- SCREEN: BrainDocPage(topic) (nav-back + title + nav-action-text Save/Saved disabled-until-dirty + full-height .input-doc textarea w/ topic placeholder; loads existing text). CSS (RULE 9): .nav-action-text(+:disabled), .input-doc.
- WIRING: BrainFlow routes philosophy/writing/values -> <BrainDocPage topic=...> (DOC_TOPIC map). People rows already route to PeopleFlow. Remaining Brain rows (per-category, onboarding, backup) still placeholder.
- TESTS: braindoc.test.tsx (2: service one-record-per-topic + read-back; page edit->Save->Saved). 
- RESULT: 165 app tests (+2) + 23 engine = 188 total, tsc clean, build OK, 0 em dashes.
- MIGRATIONS PENDING for Dave's eventual deploy (he has NOT deployed; do not prompt): 0003 event, 0004 category, 0005 profile, 0006 person, 0007 brain_doc (0001/0002 already run). All idempotent, run in order when he sets up Supabase.
- NEXT: per-category Brain page (aggregates a category's tasks/events/notes), then enrich AI context assembler to pull People + brain docs at the call site, then Phase 3 (Quick Capture + JARVIS Suggests).

## PHASE 2 PER-CATEGORY PAGE: BUILT + WIRED -> PHASE 2 BRAIN DETAIL COMPLETE.
- app/src/brain/CategoryDetail.tsx: read-only overview of everything tagged with one category. Uses useTasks/useSchedule/useNotes, gathers in parallel, filters by category id: open Tasks (text), Schedule (start + title, time-sorted), Notes (title). Sections via grp eyebrow + card; cat-dot in the nav title + on task rows (cat-bg-{color}); canonical empty-state "Nothing tagged <name> yet" when all empty. CSS (RULE 9): .nav-title .cat-dot, .cat-time.
- WIRING: BrainFlow, after people + doc-topic checks, matches open.key against the live categories list -> <CategoryDetail categoryId name color>. So "Your Categories" rows now open the overview. Remaining Brain rows: Onboarding (status only) + Backup (Phase 6) stay placeholders by design.
- TEST: CategoryDetail.test.tsx (2: seeded task shows under Tasks; empty message).
- RESULT: 167 app tests (+2) + 23 engine = 190 total, tsc clean, build OK, 0 em dashes. No new migration.
- PHASE 2 STATUS: COMPLETE (people set + How-You-Think text surfaces + per-category overview). 
- NEXT: Phase 3 AI features. First build a useAIContext() hook (gathers profile + people + brain docs + open tasks + today's events -> AIContextInput -> assembleContext) so features share one context source, then Quick Capture (NL -> entity routing) + JARVIS Suggests on Today, all via the AIService -> /api/ai proxy.

## PHASE 3 STARTED: QUICK CAPTURE BUILT + WIRED (the AI routing feature).
- ai/useAI.ts: useAI() -> AIService bound to useAuth().session.access_token (sent to /api/ai).
- ai/useAIContext.ts: useAIContext() -> gather():Promise<AIContext>; reads profile + people + brain docs (writing/values/philosophy) + open tasks + categories + today's events (schedule.eventsOn) -> assembleContext. Exports todayISO(). context.ts EXTENDED: AIContextInput/AIContext + contextToText now carry voice/values/philosophy (so the AI writes in the user's voice + respects guardrails).
- ai/capture.ts: CaptureResult{kind task|event|note,title,date?,start?,category?,notes?}; captureSystemPrompt(ctx,today) (return ONLY JSON, resolve relative dates, pick a category by name); parseCapture(raw) (strips ```json fences, JSON.parse, validates kind+title); localParse(text,today) (offline heuristic fallback: a time or day word -> event w/ parsed HH:MM + dayToISO, else task) used when AI not configured (demo); applyCapture(result,{tasks,schedule,notes},categories,today) (resolves category name->id, creates the right entity).
- capture/QuickCapture.tsx: a sheet (sheet-scrim>.card). Type a note -> Capture -> (AI parse when available, else localParse, with try/catch fallback) -> preview (kind badge + title + date/time/category) -> Add <Kind> applies it, or Edit. Props {ai:AIService, onClose}. Uses useAIContext + useTasks/useSchedule/useNotes/useCategories. CSS (RULE 9): .app-shell position:relative, .capture-preview/.capture-kind/.capture-title/.capture-meta.
- WIRING: AppShell builds ai=useAI(), VoiceBar onTap -> setCaptureOpen(true), renders <QuickCapture ai onClose> overlay. VoiceBar hint "Tap to speak" -> "Tap to capture" (real voice = Milestone C; text capture works now).
- TESTS: ai/capture.test.ts (7: parseCapture plain/fenced/invalid, localParse event+task, applyCapture task w/ category + event + note), capture/QuickCapture.test.tsx (2: localParse path preview+file+onClose; AI path via mock fetchImpl -> Event preview).
- DEMO NOTE: in the in-memory demo AI is OFF, so Quick Capture uses the local heuristic (time/day -> event, else task). The real model parse runs once deployed (api/ai.ts proxy). Sandbox cannot call Anthropic.
- RESULT: 176 app tests (+11) + 23 engine = 199 total, tsc clean, build OK, 0 em dashes. No new migration.
- NEXT (Phase 3 cont.): JARVIS Suggests on Today (proactive card; reads gathered context via useAIContext + useAI -> /api/ai). Then Phase 4 feature screens.

## PHASE 3 COMPLETE: JARVIS SUGGESTIONS BUILT (mock approved; title set to "JARVIS Suggestions" per Dave; card uses canonical sec-head + suggestion-row after Dave flagged hand-rolled borders + missing section header).
- ai/suggestions.ts: suggestionsSystemPrompt(ctx,today) (return ONLY a JSON array of <=2 short grounded strings, [] if none); parseSuggestions(raw) (strip fences, JSON.parse, keep non-empty strings, cap 2).
- today/TodaySuggestions.tsx: props {ai:AIService}. On mount (if ai.available) gather context via useAIContext -> ai.complete -> parseSuggestions. Renders canonical CANON pattern: sec-head (sec-ico ico-accent zap + sec-title "JARVIS Suggestions") ABOVE a .card of .suggestion-row (.sug-title + .chev); auto-dividers from canon (.suggestion-row + .suggestion-row). Loading -> one dim .sug-dim "Thinking..." row. AI off OR zero suggestions -> renders null (hidden in demo so nothing looks broken). Tap a row -> dismiss (session Set). Only CSS addition: .sug-dim{color:var(--tx-3)}.
- WIRING: TodayPage gained optional suggestions?: ReactNode slot rendered between today-hero and <YourDay> (presentational; existing TodayPage tests unaffected since they pass no slot). TodayFlow: const ai=useAI(); passes suggestions={<TodaySuggestions ai={ai}/>}.
- TESTS: ai/suggestions.test.ts (3), today/TodaySuggestions.test.tsx (2: hidden when AI off; AI-mocked rows + dismiss).
- DESIGN LESSON (logged): build sections from the canonical sec-head + row pattern; NEVER hand-roll card borders/dividers, use .suggestion-row (canon auto-divider). Section title is a noun phrase matching Your Day / Today's Tasks.
- FULL VERIFICATION PASS: app 181 tests (+5) + engine 23 = 204 total, tsc clean, build OK, 0 em dashes. TEST_PLAN.md updated (Suggestions BUILT; demo hides it; 204 count).
- PHASE 3 STATUS: COMPLETE (useAIContext + context voice/values/philosophy; AI seam /api/ai proxy; Quick Capture; JARVIS Suggestions). NEXT: Phase 4 feature screens (Goals/Life Map, Active Projects, Messages, full-text Search) per BUILD_PLAN, or whatever Dave prioritizes. AI live behavior verifies only after deploy.

## SPACING: container gap 16->20px (Dave approved, iOS inset-grouped default). .sec-head + .grp top padding var(--s-4)->var(--s-6) (components.css line85, uniformity.css line305). Header-to-card stays 6px (--s-1h). App-wide +4px between every container/section. 204 tests still pass, 0 em dashes.

## IN-APP TEST PANEL (Dave: "a test panel in the preview that runs the full functionality test"). app/src/testpanel/TestPanel.tsx = in-browser runner: ~24 assertions across Engine, Tasks, Schedule, Notes, Categories, Profile, People, Brain docs, Connections, AI context, Quick Capture, JARVIS Suggestions; runs the REAL services (InMemoryAdapter) + pure AI logic in the browser, auto-runs on load + Run Again, renders grouped pass(green check)/fail(red x + error) rows + "X / Y passed" header. testMain.tsx + test.html = separate entry; vite.config gained TESTPANEL branch (rollup input=test.html). Build: SINGLEFILE=1 TESTPANEL=1 vite build -> dist/test.html -> jarvis-test-panel.html (single inlined file, opens on phone). CSS additions (token-based): .test-summary/.test-ico/.test-ok/.test-bad/.test-err/.test-runbar. 0 inline styles. NOTE: panel tests the same logic as the 204 vitest tests but runnable in the preview; AI network path still device/deploy-only.

## TEST PANEL REVISED -> INTERACTIVE TEST BENCH (Dave wanted buttons he clicks, NOT an auto-runner). app/src/testpanel/TestBench.tsx replaced TestPanel as the testMain entry. Button+input sections (Tasks/Schedule/Notes/Categories/People/Brain doc/Quick Capture/Suggestions/AI context/Connections) each run REAL services on one shared in-memory Store; Output log (last 14, newest green) + Live data counts refresh per action; Reset = reload. CSS: .bench-btns/.bench-act(+.prim)/.bench-log/.bench-line (token-based, compact mobile). Build SINGLEFILE=1 TESTPANEL=1 -> dist/test.html -> jarvis-test-panel.html. AI network path still deploy-only (bench uses localParse). TestPanel.tsx (auto-runner) left in tree but unused. tsc clean, 0 em dashes, 204 vitest still green.

## TEST ALL BUTTON + PHASE 4 SEARCH (Dave: "include a test all button moving forward. continue").
- TEST ALL: testpanel/checks.ts exports CHECKS (22 assertions across all modules, fresh InMemory store each, ok/eq helpers). TestBench got a prominent "Test All" button (btn-primary) that runs CHECKS, logs PASS/FAIL per check (color-coded test-ok/test-bad) + a summary line ("ALL PASS - n/n" or "k FAILED"). bench-line:first-child no longer force-green (color now from PASS/FAIL prefix). Rebuilt jarvis-test-panel.html. Going forward, keep a Test All in the bench.
- SEARCH (Phase 4, mock was shown then approved via "continue"): search/search.ts = runSearch(query, {tasks,events,notes,people}) case-insensitive substring across the human field of each type -> grouped SearchResults; totalHits(). Pure, client-side (fine at personal scale). search/SearchFlow.tsx = full-cover .search-overlay (absolute inset0 over the relative .app-shell): .search-top (search-bar + Cancel) + .search-results scroll; fetches all lists once on open, filters live via useMemo as you type; empty-state prompt when blank, "No matches" when none, else grouped sec-head+card sections (Schedule sched-row / Tasks / People av-40+name / Notes), only non-empty groups shown. Results are display-only for now (deep-link into each flow = later).
- ENTRY POINT: search icon added to the LEFT of the Today bar (TodayPage gained onSearch? -> renders .today-search in a .today-bar-search space-between bar; TodayFlow passes onSearch through; AppShell has searchOpen state, passes onSearch to TodayFlow, renders <SearchFlow onClose> overlay). Only Today shows the entry.
- CSS (RULE 9): .search-overlay/.search-top/.search-cancel/.search-results, .today-bar-search/.today-search; .bench-* (prior).
- TESTS: search/search.test.ts (3), search/SearchFlow.test.tsx (2: empty prompt -> seeded match; Cancel->onClose).
- VERIFY: app 186 (+5) + engine 23 = 209, tsc clean, demo + bench rebuilt, 0 em dashes. TEST_PLAN: 209 + new "11. Search" section.
- PHASE 4 STATUS: Search DONE. Remaining Phase 4: Goals/Life Map (catalog 21), Active Projects, Messages (catalog 14). Then Phase 5 polish, Phase 6 ship-prep.

## PHASE 4 LIFE MAP / GOALS BUILT (Dave: prior decisions already made - confirmed via past chats: Goals+Life Map are ONE page; lives under More; areas = user's orgs + Family/Health/Friends per the 3-layer module doc). Generic, not Dave's orgs.
- ENTITIES: life/types.ts: ENTITY_AREA="life_area" (AreaData{name,state:strong|steady|drifting,order?}), ENTITY_GOAL="goal" (GoalData{title,state:on_track|steady|at_risk,areaId?,order?}). AREA_META/GOAL_META map state->{label,cls good|warn|bad,pct}. AREA_STATES/GOAL_STATES. Migrations 0008_life_area, 0009_goal.
- SERVICES: AreaService + GoalService (CRUD, list sorted, Emit narrowed). Provider: useAreas()/useGoals().
- SCREENS: LifeMapPage (nav-large "Life Map" + page-explainer; card of lm-rows = lm-main[lm-top(name + lm-qual colored state) + lm-bar/lm-bar-fill width=state pct] + chev + Add Area; Active Goals sec-head + card of rows[title + lm-qual state + chev] + Add Goal; empty-state when both empty). AreaSheet (name + segmented Strong/Steady/Drifting + delete). GoalSheet (title + segmented status + optional area chips + delete). LifeMapFlow wires list<->sheets over useAreas/useGoals. Bar fill width is the one data-driven inline style (documented exception).
- CSS (RULE 9, now real): .page-explainer, .lm-row(+divider)/.lm-main/.lm-top/.lm-name/.lm-qual/.lm-bar/.lm-bar-fill, lm-good|warn|bad (+ -bg). 
- NAV: destinations.tsx gained {key:"goals",label:"Life Map",Icon:Target} -> appears in More "Your Stuff" extras (not a default tab), tab-promotable via Edit Tabs. AppShell renders <LifeMapFlow/> on active==="goals". MorePage EXTRA_TILE goals->ico-accent.
- TESTS: life/life.test.ts (5: area CRUD + reject-empty + meta; goal CRUD), life/LifeMapFlow.test.tsx (1: empty->add area->appears). Test Bench "Test All" gained a Life Map check (CHECKS now ~23); bench rebuilt.
- EMPTY-STATE FIX (Dave flagged lifemap empty spacing): canonical .empty-state now flex-column centered (justify-center, gap s-3, min-height 65vh, padding s-6 s-5); .empty-icon 56px; added .empty-title (t-h3 w-semi tx-2) + .empty-state .btn margin-top. Improves ALL empty states app-wide.
- VERIFY: app 191 (+5) + engine 23 = 214, tsc clean, demo + bench rebuilt, 0 em dashes. Migrations now 0003-0009 (Dave runs at deploy). TEST_PLAN: 214 + "12. Life Map".
- PHASE 4 STATUS: Search + Life Map/Goals DONE. Remaining: Active Projects, Messages (catalog 14). Then Phase 5 polish, Phase 6 ship-prep.

## PHASE 4 ACTIVE PROJECTS BUILT (built directly from canonical proj-row + approved list/sheet/empty patterns; reviewed in demo).
- ENTITY: projects/types.ts: ENTITY_PROJECT="project"; ProjectStatus active|on_hold|done; ProjectData{title,category?,status,order?}; PROJECT_META status->{label,cls good|warn|mute}; PROJECT_STATES. Migration 0010_project_entity.
- SERVICE: ProjectsService (CRUD; list sorts active->on_hold->done then order/title; Emit narrowed). Provider: useProjects().
- SCREENS: ProjectsPage (nav-large "Projects"; card of canonical .proj-row = .proj-icon cat-bg-{catColor} initial + .proj-meta[.proj-tag=catName, .proj-title] + lm-qual status tag + chev; Add Project; empty-state). ProjectSheet (title + segmented status + optional category chips + delete). ProjectsFlow wires list<->sheet over useProjects + useCategories. CSS: .lm-mute (done status grey) added; proj-* already canonical.
- NAV: destinations.tsx + {key:"projects",label:"Projects",Icon:Briefcase} -> More Your Stuff extras, tab-promotable. AppShell active==="projects" -> <ProjectsFlow/>. MorePage EXTRA_TILE projects->cat-bg-sky.
- TESTS: projects/projects.test.ts (2), projects/ProjectsFlow.test.tsx (1). Test Bench Test All gained a Projects check (CHECKS ~24); bench rebuilt.
- VERIFY: app 194 (+3) + engine 23 = 217, tsc clean, demo + bench rebuilt, 0 em dashes. Migrations now 0003-0010. TEST_PLAN: 217 + "13. Active Projects".
- PHASE 4 STATUS: Search, Life Map/Goals, Active Projects DONE. Remaining: Messages (catalog 14). Then Phase 5 polish + Phase 6 ship-prep.

## PHASE 4 MESSAGES BUILT (Dave chose: build the shell with realistic placeholder data). Prior decisions (found in 5/21 convo) honored: Messages = unified Email+Texts inbox (catalog 14), Email|Texts segmented, iOS Mail (no avatar, unread dot, sender/date/subject/preview, chev) + Drafts + Scheduled (grp sections, "Sends..."), Texts = iOS Messages (avatar + unread dot), compose pen in nav (no FAB). Lives under More.
- VIEW-ONLY (no entity/migration): Messages mirrors external accounts, not a stored type. messages/demoData.ts = generic realistic dataset (EMAILS/DRAFTS/SCHEDULED/TEXTS; avFor uses slotForName/personInitials). The shipped app swaps this for live Gmail (web OAuth) + iMessage (native) when connected; documented as demo-only.
- messages/MessagesFlow.tsx: nav-large "Messages" + nav-action compose pen; .msg-seg segmented Email|Texts (local state); EmailView (canonical .msg-row/.msg-body/.msg-head/.msg-name/.msg-time/.msg-subject/.msg-preview + leading .msg-lead unread gutter + Drafts/Scheduled grp sections) ; TextsView (av-40 + msg rows). Built entirely from canonical msg-* vocabulary.
- CSS (RULE 9): .msg-seg (segmented wrapper), .msg-lead (8px leading unread gutter, top-aligned), .msg-sends (tint scheduled line). No entity, no migration, no core change.
- NAV: destinations.tsx + {key:"messages",label:"Messages",Icon:MessageSquare} -> More Your Stuff, tab-promotable. AppShell active==="messages" -> <MessagesFlow/>. MorePage EXTRA_TILE messages->cat-bg-green.
- TEST: messages/MessagesFlow.test.tsx (1: email default + drafts/scheduled, switch to texts).
- VERIFY: app 195 (+1) + engine 23 = 218, tsc clean, demo + bench rebuilt, 0 em dashes. No new migration (still 0003-0010).
- PHASE 4 COMPLETE: Search, Life Map/Goals, Active Projects, Messages all done. NEXT: Phase 5 polish (empty states done; Notes polish, Tasks sort/long-press, Schedule week view + tap-to-add, tab drag-reorder) then Phase 6 ship-prep (real Backup, Terms/Privacy, Sentry, Apple Sign In) -> deploy PWA.

## SCOPE UPDATE (Dave, this session): coverage audit done. Dave chose "high-value first (Notifications, Money, Maps, detail screens), defer Voice". Music (FEAT-25) confirmed already spec'd + LOCKED v1 (found in 5/21 convo; music-feature-spec.md): context-matched music via user's Apple Music/Spotify, no engine our side, taste-seeded, 3 control levels (Auto off default), lock-in mode, start kick, Focus Sounds fallback, now-playing strip. Native/streaming-dependent like Messages -> real playback = native phase; UI shell buildable at integration surfaces. CORRECTION to prior note: Music WAS described; not "never spec'd".
## REMAINING PERSONAL SCOPE (sequence): 1) Notifications feed DONE  2) Money/finance card  3) Maps/locations on appointments  4) Detail screens (Settings sub-screens account/about/advanced/backup/notifications; project detail; life-area detail). DEFER: full Voice (FEAT-12). Native phase: Music shell, Media/photo attachments, Charts(maybe). Phase 5 polish (Schedule week, Notes media/table, tab reorder) + Phase 6 ship-prep still pending.

## NOTIFICATIONS FEED BUILT (FEAT-15) - real data, not placeholders.
- notifications/feed.ts: buildFeed({tasks,events,goals,areas}, today)->Nudge[] {id,kind,title,sub,when}. Kinds + order: overdue (task !done, due<today), due_today (due===today), event (date===today, sorted by start, shows start time), goal_risk (goal state at_risk), area_drift (area state drifting). Done tasks excluded.
- notifications/NotificationsFlow.tsx: nav-large "Notifications"; gathers via useTasks().listTasks()+useSchedule().listEvents()+useGoals()+useAreas(); renders canonical .msg-row with leading .proj-icon cat-bg-{slot by kind: overdue/goal_risk=red, due_today=yellow, event=sky, area_drift=graphite} + inline SVG icon + msg-body(title + when + sub). Empty -> empty-state "You're all caught up" + .empty-sub. CSS: .empty-sub added (centered meta).
- NAV: destinations.tsx {key:"notifications",label:"Notifications",Icon:Bell} -> More extras, tab-promotable. AppShell active==="notifications". MorePage EXTRA_TILE notifications->cat-bg-red. No entity, no migration, no core change.
- TESTS: notifications/feed.test.ts (2), NotificationsFlow.test.tsx (1). Test All gained a Notifications check.
- VERIFY: app 198 (+3) + engine 23 = 221, tsc clean, demo + bench rebuilt, 0 em dashes. Migrations still 0003-0010. TEST_PLAN "15. Notifications".

## HIGH-VALUE BATCH BUILT (Money + Maps + detail screens). app 205 + engine 23 = 228, tsc clean, 0 em dashes. Migrations now 0003-0011.
### MONEY (FEAT-17): entity "account" {name,balance:number,kind:cash|savings|investment|credit|other,order?}; migration 0011_account_entity. MoneyService CRUD; useMoney(). money/types.ts: ACCOUNT_META(kind->label+slot), formatMoney (Intl USD, no decimals), totalBalance. MoneyFlow: empty-state; else .money-hero (total) + Accounts card of canonical .proj-row (proj-icon cat-bg-{kind slot} + meta[kind tag, name] + .money-amt + chev) + Add; AccountSheet (name + numeric balance + kind chips + delete). Generic/user-entered (no bank integration). NAV: dest {money, Wallet} -> More extras; AppShell active==="money"; tile cat-bg-green. Tests: money.test.ts(2)+MoneyFlow.test.tsx(1). Test All: Money check. CSS: .money-hero/.money-hero-label/.money-hero-total/.money-amt.
### MAPS/LOCATIONS (FEAT-13): EventData += location?:string. ScheduleService.createEvent opts.location + new editLocation(id,loc). EventSheet: EventDraft += location; "Location (optional)" input. ScheduleFlow: maps location in/out. SchedulePage: .sched-loc line (pin + text) under category; tap = open https://maps.apple.com/?q={enc} (deep link, works on device; stopPropagation so row still edits). LIVE map tile + walk-time + open-now still need the maps API (native/deploy) - documented. Tests: schedule.test.ts location persistence; EventSheet tests updated for location:"". CSS: .sched-loc.
### DETAIL SCREENS (FEAT-11 + FEAT-20 + FEAT-21):
- Settings sub-screens via MoreFlow routes (MoreRoute += account|notifsettings|about|advanced|backup): AccountPage (profile av-72 + name + template + sign out), NotificationsPage (REAL toggles -> ProfileData.notify {overdue,events,goals}, persisted via ProfileService.save; canonical .switch), AboutPage (brand + version + Terms/Privacy/Support rows), AdvancedPage (export stub + double-tap "Clear local data" -> localStorage.clear+reload), BackupPage (status Off + explainer; real backup Phase 6). MorePage reorganized: Preferences (Account/Edit Tabs/Profile/Notifications/Appearance/Categories) + System (Backup/Advanced/About). Bell icon added to MorePage.
- ProfileData += notify?:{overdue,events,goals}. NotificationsFlow now loads profile.notify and FILTERS the feed (overdue+due_today<-overdue, event<-events, goal_risk+area_drift<-goals; default all true).
- Life-area detail: life/AreaDetailPage (area lm-bar + "Goals in this area" = goals filtered by areaId + Edit action + add goal). LifeMapFlow: onOpenArea -> detailId (was edit sheet); detail renders with sheets layered for edit/goal; area delete closes detail.
- Project detail: projects/ProjectDetailPage (proj icon hero + title + status + Status/Area rows + Edit). ProjectsFlow: onOpen -> detailId (was edit sheet); Edit opens sheet; delete closes detail. NOTE: tasks not yet linked to projects, so detail is info-only (task<->project link = future).
- Tests: settings/NotificationsPage.test.tsx (toggle off), life/AreaDetail.test.tsx (drill-in opens), projects/ProjectDetail.test.tsx (drill-in opens). CSS: .account-hero/.account-name/.account-sub/.about-hero/.row-value/.proj-detail-*.
### REMAINING (deferred per Dave): full Voice (FEAT-12). Native/integration phase: Music shell (FEAT-25 locked), Media/photo attachments (FEAT-14), live Maps tiles. Maybe-later: Charts/data-viz (FEAT-19). Phase 5 polish (Schedule week, Notes media/table, tab reorder) + Phase 6 ship-prep (real Backup, Terms/Privacy, Sentry, Apple Sign In) -> deploy PWA.

## SETTINGS RESTRUCTURE (Dave: "shouldn't preferences be settings" + "make sure there isn't repetitiveness"). Chose: one Settings row on More -> Settings screen.
- more/SettingsPage.tsx (new hub): Account, Notifications, Appearance, Categories, Edit Tabs + System group (Backup, Advanced, About). nav-back to More.
- MorePage slimmed: now just "Your Stuff" (extras) + an "App" card with a single "Settings" row -> onNavigate("settings"). Removed inline Preferences/System groups. MoreRoute += "settings" (now: settings|profile|appearance|categories|edittabs|account|notifsettings|about|advanced|backup).
- MoreFlow: route "settings" -> SettingsPage; sub-pages onBack now -> "settings" (not "hub"); profile onBack -> "account".
- DE-DUP: (1) Profile removed as a top-level row; reached via Account -> "Edit Profile" (onEditProfile -> route "profile"). Account & Profile were overlapping. (2) Sign Out removed from MorePage; lives only in AccountPage (onSignOut routed there from MoreFlow). MorePage no longer takes onSignOut.
- Test updated: more/settings.test.tsx nav test now goes More -> Settings -> Categories -> back (asserts "Edit Tabs").
- VERIFY: app 205 + engine 23 = 228, tsc clean, 0 em dashes, demo + bench rebuilt. No entity/migration/core change (still 0003-0011).

## SETTINGS iOS POLISH + REORDER (Dave: adopt iOS large-title on sub-screens + move Settings to top of More).
- shared/LargeTitleNav.tsx (new): iOS pushed-screen header = tinted labeled back button ("< {parent}") in .nav-bar above a .nav-large bold title. Uses the `.nav-bar + .nav-large` spacing rule.
- Converted to LargeTitleNav (was nav-back svg + small nav-title): ProfilePage (back "Account"), AppearancePage, AccountPage, NotificationsPage, AboutPage, AdvancedPage, BackupPage, EditTabsPage, categories/screens/CategoriesPage (all back "Settings"). SettingsPage hub already iOS-style (large title, "More" back, search, headerless grouped icon lists).
- MorePage reordered: Settings card moved to TOP (right under the More title), then "Your Stuff" extras. Dropped the "App" eyebrow.
- Non-settings pushed screens (Notes sub-screens, area/project detail) keep the small centered title (unchanged) - only Settings + its sub-screens adopted large titles.
- Test: more/settings.test.tsx back click now targets the labeled back text ("Settings") instead of aria-label "Back".
- VERIFY: app 205 + engine 23 = 228, tsc clean, 0 em dashes, demo + bench rebuilt. No entity/migration/core change.

## INSIGHTS / CHARTS BUILT (FEAT-19) - last fully in-sandbox-buildable signed-off feature. Real data, no chart lib (CSS bars).
- insights/insights.ts: weekDates(today) (Monday-anchored 7 ISO days), eventsThisWeek (per-weekday event counts; today's bar slot=red else sky), openTasksByArea (open task count per category, drops 0, sorts desc, category color), accountBars (|balance| for bar size, signed formatMoney for display, kind slot), maxValue.
- insights/InsightsFlow.tsx: nav-large "Insights"; gathers tasks+events+categories+money; 3 sections (This week / Open tasks by area / Accounts) each a .card of .chart-row (.chart-label + .chart-track > .chart-fill cat-bg-{slot} width=value/max inline % [documented bar-width exception] + .chart-val). Sections hidden when empty; full empty-state "Nothing to chart yet" when all empty. CSS: .chart-row(+divider)/.chart-label/.chart-track/.chart-fill/.chart-val/.chart-cap.
- NAV: dest {insights, BarChart3} -> More extras, tab-promotable. AppShell active==="insights". MorePage tile cat-bg-blue. No entity/migration/core change.
- TESTS: insights/insights.test.ts (4), InsightsFlow.test.tsx (1). Test All: Insights check. app 210 (+5) + engine 23 = 233, tsc clean, 0 em dashes, demo + bench rebuilt.
## FEATURE COVERAGE NOW: all in-sandbox-buildable signed-off features DONE (Today/Tasks/Schedule/Brain/Messages/Notes/Goals/LifeMap/More/Themes/Search/Contacts/Projects/Onboarding/empty-states/QuickCapture/AI-routing/Notifications/Money/Maps-locations/detail-screens/Insights). REMAINING: full Voice (FEAT-12, Dave deferred); native/integration phase (Music shell FEAT-25 locked, Media/photo attachments FEAT-14, live map tiles+walk-time); Phase 5 polish (Schedule week view, Notes media/table inline, tab drag-reorder); Phase 6 ship-prep (real Backup, Terms/Privacy, Sentry, Apple Sign In) -> deploy PWA.

## HOME TOP BAR redesign (Dave): initials left (tap -> More via onProfile), JARVIS logo center (.today-brand, red .j accent), search right. TodayPage today-bar rewritten 3-part (today-av button + today-brand + today-search); onProfile prop threaded TodayPage<-TodayFlow<-AppShell(setActive("more")). CSS: .today-bar space-between, .today-av, .today-brand(+.j). Dropped today-bar-search modifier usage. app 210 + engine 23 = 233, tsc clean, 0 em dashes, demo rebuilt.
## NEXT: Phase 5 polish, starting with Schedule week view (FEAT-03 completion).

## SCHEDULE WEEK VIEW (FEAT-03 completion, Phase 5). Day | Week | Month segmented toggle.
- calendar.ts +weekOf(iso) (Monday-anchored 7 ISO) +addDays(iso,n).
- ScheduleFlow: mode state day|week|month (default month). reload also setAllEvents(listEvents). weekCells = weekOf(selected) each {date,day,colors=unique categories slice3 via eventsForDate}. onPrev/onNext step by mode: month=stepMonth; week=+/-7d; day=+/-1d (syncView updates y/m so month dots align).
- SchedulePage: segmented Day/Week/Month (.sched-seg); cal-nav label by mode (month name+yr / week range "May 18 - 24" cross-month aware / full day "Sunday, May 24"); MONTH=cal-grid (unchanged), WEEK=.week-strip (7 .wk-cell: wk-wd weekday + wk-day number circle + cal-dots; sel=accent fill, today=accent text), DAY=no grid. Day-events list below unchanged for all modes. CSS: .sched-seg/.week-strip/.wk-cell/.wk-wd/.wk-day(+sel/today).
- TESTS: schedule.test.ts week helpers (weekOf/addDays); WeekView.test.tsx (Week->7 wk-cells, Day->no grid/strip). app 212 (+2) + engine 23 = 235, tsc clean, 0 em dashes, demo rebuilt. TEST_PLAN unchanged (covered by automated). No entity/migration/core change.
## PHASE 5 REMAINING: Notes inline media/tables, tab drag-reorder. PHASE 6: real Backup, Terms/Privacy, Sentry, Apple Sign In -> deploy PWA.

## TAB DRAG-REORDER (Phase 5). Touch-friendly pointer-events reorder.
- more/TabOrderList.tsx: pointer-based vertical reorder of enabled tabKeys. onPointerDown on .drag-handle (setPointerCapture) -> onPointerMove computes target index = floor((clientY - listTop)/rowH), live-splices order on index change -> onPointerUp commits via onReorder(next). Dragged row gets .dragging. GRIP icon (2 lines). touch-action:none on handle.
- EditTabsPage: new "Tab order" section (TabOrderList) shown when onReorder && tabKeys.length>1, above the existing "In the tab bar" toggle list. New optional onReorder prop.
- Wiring: AppShell reorderTabs(next)=setTabKeys+profile.save({tabs}) -> MoreFlow onReorderTabs (optional) -> EditTabsPage onReorder. Order persists in profile.tabs.
- CSS: .drag-handle/.reorder-row.dragging.
- TESTS: more/TabOrder.test.tsx (Tab order list + handle per tab; hidden when 1 tab). Pointer drag not unit-tested (jsdom has no layout); logic is splice-based. app 214 (+2) + engine 23 = 237, tsc clean, 0 em dashes, demo rebuilt. No entity/migration/core change.
## PHASE 5 STATUS: Schedule week view DONE, tab drag-reorder DONE. Remaining Phase 5: Notes inline media/tables (FEAT-14 partial; native-ish for real photo upload). PHASE 6 ship-prep: real Backup, Terms/Privacy, Sentry, Apple Sign In -> deploy PWA.

## TEST BENCH: live drag test added (Dave: "we need to test the drag option"). testpanel/TestBench.tsx renders the REAL more/TabOrderList with sample keys [today,tasks,schedule,brain]; onReorder updates local state + logs "Reordered -> ...". Section "Tab Reorder (drag test)" + an Order readout. Bench loads app CSS so layout/drag work on device. Pointer drag can't run in jsdom, so this manual on-device widget is the test path for drag. bench rebuilt -> jarvis-test-panel.html. app 214 + engine 23 = 237, tsc clean, 0 em dashes.

## TAB DRAG FIX CONFIRMED WORKING (Dave tested in Claude app webview). v1 (setPointerCapture + bubbling, fixed rowH) failed on touch. v2 fix: window-level pointermove/up/cancel listeners (gesture never lost) + document touchmove preventDefault(passive:false) during drag (blocks page scroll stealing the gesture, the iOS failure) + e.preventDefault on pointerdown + target index by row-midpoint via getBoundingClientRect each move. Handle hit area enlarged (min 48x44, full right edge), user-select none. The window-listener + scroll-block pattern is the standing approach for any future touch-drag in this app. Live drag widget remains in Test Bench ("Tab Reorder (drag test)").

## PHASE 6 SHIP-PREP DONE (Dave: "do them all one by one"). All in-sandbox-buildable; 2 are deploy-config slots.
1) BACKUP (REAL export/import). backup/BackupService.ts (store,ownerId): exportBundle()->{app:"jarvis",version:1,exportedAt,items:[{entityType,data}]} via store.listForUser; importBundle(bundle)->count via store.create per item (drops ids, ADDS not merge, validates app==="jarvis" else throws). Wired into NotesProvider (BackupContext + useBackup). settings/BackupPage.tsx rewritten REAL: "Export all data" (Blob+object URL download jarvis-backup-DATE.json), "Import from file" (hidden file input -> FileReader text -> JSON.parse -> importBundle, status line). conn-meta subtitles. TEST: backup/BackupService.test.ts (round-trip + reject non-jarvis) + Test Bench check "Backup: export then import round-trips".
2) TERMS/PRIVACY/SUPPORT screens. settings/LegalScreen.tsx (shared: LargeTitleNav back="About" + yellow-left-border .legal-note "Template copy, replace before launch" + .legal-body). TermsPage/PrivacyPage/SupportPage = template legal copy (clearly marked template; NOT fabricated as real legal). AboutPage rows wired (onTerms/onPrivacy/onSupport). MoreFlow route union += terms|privacy|support; About->each, each back->about. CSS: .legal-note/.legal-updated/.legal-body/.legal-h.
3) SENTRY error tracking = CONFIG SLOT. monitoring/monitor.ts: captureError(e,ctx)->console.error + optional sink; setErrorSink; initMonitoring() attaches window error/unhandledrejection handlers always, and has a DSN-gated Sentry block (commented; activate at deploy by installing @sentry/browser + Sentry.init + setErrorSink(captureException), reads import.meta.env.VITE_SENTRY_DSN). NO SDK installed (keeps bundle clean/no network). monitoring/ErrorBoundary.tsx (class, getDerivedStateFromError + componentDidCatch->captureError; fallback empty-state + Reload). main.tsx: initMonitoring() at boot + <ErrorBoundary> wraps AppearanceProvider>AuthProvider>App. TEST: monitoring/monitor.test.ts (sink forwarding).
4) APPLE SIGN IN = already wired; verified. screens/SignIn.tsx has black HIG .btn-apple "Continue with Apple" -> onApple -> auth signInWithApple (Supabase OAuth apple). AuthValue has signInWithApple + signUpWithPassword. No code change; deploy-config only.
- DEPLOY.md: added "Phase 6 ship-prep config" section (Sentry install+wire+VITE_SENTRY_DSN; Apple Services ID/key/Supabase provider; legal-copy + Privacy/Support URL reminders). SignIn legal links still href="#" (noted).
- VERIFY: 52 files / 217 tests pass (+3: backup x2, monitor x1), tsc clean, 0 em dashes. Demo + bench rebuilt, app zipped. NO engine/migration/core change (BackupService is app-side) -> core zip NOT regenerated.
## STATUS: Milestone A app-complete + ship-prep done. REMAINING TO SHIP: Dave runs deploy (migrations 0003-0011, Supabase env, host PWA, optional Sentry/Apple/legal copy) per DEPLOY.md. Native phase (Music shell locked, Media/photo, live Maps, full Voice) + Milestones B/C later.

## FULL AUDIT (Dave req) + VISUAL ENHANCEMENTS. Cross-ref'd built app vs iOS CONVENTIONS 1-16, RULE 14, Design Gate, canonical jarvis-design-system.css, catalog screen set.
AUDIT RESULT: strongly compliant. Verified by scan: caps (uppercase only on eyebrow/kicker/pill/urgency/status/wk-wd/cal-wd/table-th), urgency=colored text not pills, tx-4 never on content, avatars always colored, chevrons on nav rows only, 0 hardcoded hex in tsx, tokens in sync w/ canonical. Findings (all minor): (1) 3 inline styles = data-viz width% (sanctioned exception). (2) components.css uses raw px for icon/element sizes - canonical does same (no icon-size token category); normalized off-ladder ones (nav-action/nav-back .ic 23->24, sched-loc .ic 13->16). (3) 2 tokens (--scrim,--on-fill-dark) in design-system.css not in canonical - legit. INTENTIONAL divergence (correct): canonical named org colors (--cat-bffsa/elite/friends/money) dropped for 8 generic slots = right for App Store. Catalog coverage: all Milestone-A screens built; deferred=full Voice/Media/live-Maps/Music (native). Catalog class names (frame/bnav/tl-/bn) are mockup-page classes, app uses production classes (expected).
VISUAL ENHANCEMENTS BUILT (Dave: "all of the above"), all token-based + prefers-reduced-motion guarded, 0 new tokens needed:
1) COMPLETION ANIM (task check-off, JARVIS DNA): TasksPage Row detects done-transition via prevDone ref + 650ms burst state -> .just-done class. CSS @keyframes taskDoneFlash (--good-tint wash) + checkPop (check scale 1->1.32->1).
2) SKELETON loading rows: shared/SkeletonRows.tsx (existing skel-row/skel-circle/skel-stack/skel-line/.short classes). Wired loading state into Tasks, Schedule, Projects (init true, set false at end of reload, Page renders <SkeletonRows/> while loading). REMAINING flows (Notes/Today/Money/LifeMap/Notifications/Insights/People/Messages) use identical one-line pattern - not yet wired (instant in-memory demo can't show them; real value at deploy latency).
3) PRESS FEEDBACK: CSS :active background var(--press-3) on row-type classes + .btn:active scale(0.97) + transition var(--dur-fast).
4) LIST ENTER: @keyframes listIn (fade + translateY 6px) on .app-scroll .pad-x > .card, var(--dur-normal)/--ease-out.
- Test Bench: "Visual FX (new)" section = tappable demo task row (completion punch) + SkeletonRows (so Dave SEES skeleton + anim; demo loads too fast to show skeleton naturally).
- VERIFY: 217 tests, tsc clean, 0 em dashes, inline styles still only 3 data-viz. Demo + bench rebuilt, app zipped. No engine/migration/core change.

## MOTION POLISH (Dave: "all of the above") + BOLD LOOK PROPOSAL. All token-based, prefers-reduced-motion guarded, no new tokens, no system restyle.
MOTION (in app, 4 items): (1) Sheet present motion: @keyframes scrimIn (scrim fade) + sheetIn (translateY 100%->0, --ease-spring) on .sheet-scrim / .sheet-scrim>.card. (2) Tab switch: AppShell wraps active flows in <div className="tab-swap" key={active}> + @keyframes tabSwap (fade+translateY5). (3) Detail push: MoreFlow refactored returns into `const screen=(()=>{...})()` wrapped in <div className="route-push" key={route}> + @keyframes routePush (fade+translateX14) - covers all Settings/About/Terms/Privacy/Backup/Advanced/Account sub-nav. (4) Selection transitions: .chip/.seg background+color transition var(--dur-fast); Tab bar polish: .tab color transition + :active opacity 0.55 + .tab.active .ic scale(1.08). All in components.css; reduced-motion block disables. 217 tests pass, tsc clean, 0 em dashes, demo+zip rebuilt.
BOLD PROPOSAL (separate file, does NOT touch app): /mnt/user-data/outputs/jarvis-bold-proposal.html. Standalone = real app CSS (3 files concatenated) + .bold-skin token-override layer + Current/Bold toggle on a representative Today screen. Bold overrides (RULE 9 proposal, NOT yet applied): --r-md 12->14/--r-lg 14->18/--r-xl 18->22 (rounder), --t-h1 28->32 + nav-large letter-spacing -0.03em (bigger tighter title), --accent #FF1F30->#FF2B3C (punchier), --surface-1 #131318->#17171E/surface-2->#1F1F27 + --card-bd 0.08->0.12 + --shadow-card none->0 6px 22px rgba(0,0,0,0.55) (dark cards lift off black). Awaiting Dave's visual verdict; if approved, apply via RULE 9 token edit in jarvis-design-system.css (dark block) + re-run gate. Reference mockup uses some inline styles (allowed for proposals per RULE 14 note).

## BOLD PREVIEW REDONE as a real app build (hand-built mock rendered dirty). Method: backup jarvis-design-system.css -> apply bold tokens (--t-h1 32, --r-md/lg/xl 14/18/22, --accent #FF2B3C, dark --surface-1 #17171E/--surface-2 #1F1F27/--card-bd 0.12/--shadow-card 0 6px 22px rgba(0,0,0,0.55)) -> SINGLEFILE build -> /mnt/user-data/outputs/jarvis-bold-demo.html -> RESTORE css from backup (verified diff-clean) -> rebuild jarvis-notes-demo. App source UNCHANGED. jarvis-bold-proposal.html superseded. Awaiting Dave verdict; if yes, apply same token edits via RULE 9 + re-run gate. tsc clean.

## BOLD APPLIED (Dave approved) + HEADER ALIGNMENT FIX. Bold previews deleted (now the real app).
BOLD (permanent token edits in jarvis-design-system.css, RULE 9 approved): --t-h1 28->32, --r-md/lg/xl 12/14/18 -> 14/18/22, --accent #FF1F30->#FF2B3C (diverges from canonical accent BY APPROVAL), dark --surface-1 #131318->#17171E / --surface-2 #1B1B22->#1F1F27 / --card-bd 0.08->0.12 / --shadow-card none->0 6px 22px rgba(0,0,0,0.55) (dark cards now lift off black).
HEADER ALIGNMENT (Dave: "pages not starting in same spot, same borders"): root cause = 3 header patterns. (A) Tasks/Schedule: nav-bar(action) THEN nav-large below -> title LOW. (B) Projects/Money/LifeMap/Notifications/Insights/Messages: nav-large INSIDE nav-bar -> title high but nav-large's own 16px padding double-indented it. (C) Brain: bare nav-large, no nav-bar. FIX: standardized ALL primary tabs on in-bar pattern <div class=nav-bar><div class=nav-large>Title</div>{optional nav-action}</div> (converted Tasks, Schedule, Brain; rest already in-bar) + CSS `.nav-bar > .nav-large { padding: 0 }` so the bar owns spacing (kills double safe-top + double indent) -> every tab title starts at same Y, 16pt inset aligning with pad-x cards. Today (home: today-bar+hero) intentionally special. Pushed/detail screens use LargeTitleNav (nav-bar+back THEN sibling nav-large) - NOT a child of nav-bar so `.nav-bar > .nav-large` doesn't touch them; they stay consistent among themselves.
VERIFY: 217 tests, tsc clean, 0 em dashes. jarvis-notes-demo rebuilt = real bold app w/ aligned headers; app zipped. CANONICAL DIVERGENCE NOTE: accent + the bold tokens now differ from /mnt/project canonical jarvis-design-system.css by Dave's approval (Track 3 app is source of truth for these).

## FINAL VISUAL AUDIT (post bold + header align) + AVATAR RULE FIX.
AVATAR (Dave asked "does the icon follow rules?"): Today av is av-32 (32px, Conv 11 ok), colored + left-aligned (Conv 5/16 ok) - BUT was hardcoded cat-bg-blue, NOT person-derived (Conv 16 violation: "avatars use a brand color derived from the person"). Scan found 2 hardcoded avatars: today/TodayPage + settings/AccountPage (av-72). FIXED both to slotForName(name): TodayFlow computes avatarSlot=slotForName(name||initials)->TodayPage cat-bg-{avatarSlot} (default "blue"); AccountPage slot=slotForName(p.name||"")->cat-bg-{slot}. Scan now: NO hardcoded-color avatars remain in app screens (bench demo avatars excepted).
AUDIT RESULT (current build): inline styles = only 3 data-viz width% (sanctioned). 0 hardcoded hex in tsx. 0 em dashes. Caps/tx-4/urgency/chevron still compliant. Header alignment: all primary tabs now in-bar pattern, uniform start + 16pt inset (Brain/Tasks/Schedule converted; Brain pushed sub-screen + LargeTitleNav detail screens correctly use back-bar+title-below pattern, sibling nav-large unaffected by .nav-bar>.nav-large rule). MINOR/NOTED: motion keyframes use small raw px/time literals (translateY 6px, translateX 14px, scale 1.32/1.08/0.97, 0.42s, opacity 0.55) - untokened motion offsets, same class as pre-existing animation literals (ticker 40s, spinner 0.8s); acceptable. Bold tokens now diverge from /mnt/project canonical BY DAVE APPROVAL (Track 3 app = source of truth).
VERIFY: 217 tests, tsc clean, 0 em dashes. demo rebuilt, app zipped.

## TODAY AVATAR -> JARVIS RED (Dave). Today top-bar avatar now .av av-32 av-accent (new .av-accent { background: var(--accent); } in components.css) = brand red identity button. Removed the just-added avatarSlot wiring from TodayPage+TodayFlow (dead code) incl slotForName import. DELIBERATE brand exception to Conv 16 person-derived avatars, by Dave's request (home identity affordance = brand). AccountPage avatar stays person-derived (slotForName). 217 tests, tsc clean, 0 em dashes, demo+zip rebuilt.

## APPROVED + LOCKED (Dave: "I approve"): bold token set, header alignment (in-bar uniform), Today red avatar, motion polish (sheet/tab/route/selection/tab-bar), skeletons (Tasks/Schedule/Projects), completion animation, press feedback, icon-size normalization, avatar person-derive fixes. These are now the locked Track 3 visual truth - do not second-guess or revert in future sessions. Milestone A app = feature-complete + ship-prepped + visually locked. ONLY remaining to ship: Dave runs deploy per DEPLOY.md (migrations 0003-0011, Supabase env, host PWA; optional Sentry/Apple/legal copy). Native phase + Milestones B/C later.

## DEMO MOCK DATA EXPANDED (Dave: "fill the app with mock data"). data/seed.ts now seeds ALL entity types (was only categories/tasks/events). Added Extras param {areas,goals,projects,money,people}; AppShell adds useAreas/useGoals/useProjects/useMoney/usePeople hooks + passes them. Seeded (idempotent per type, generic + varied categories): 12 events (today x6 + spread across the week, some w/ locations), 9 tasks (2 overdue, 2 due-today, upcoming, +2 done), 5 areas (Health strong/Career steady/Relationships steady/Finances drifting/Growth steady) w/ 5 goals tied by areaId (on_track/steady/at_risk), 5 projects (active/on_hold/done), 4 accounts (cash/savings/investment/credit incl negative), 7 people (contacts/inner_circle/adversarial w/ relationships). Now Today/Notifications/Insights derive populated. Updated integration.test.ts counts (overdue 1->2, todaysTasks 3->4, events 9->12, tasks 5->9). 217 tests, tsc clean, 0 em dashes, demo+zip rebuilt.

## 3 VISUAL FIXES (Dave screenshots): (1) MODAL/SHEET BORDERS: in dark, sheet card sat on --surface-1 (~black, shadow invisible on black) so modals didn't pop. Fix: .sheet-scrim > .card now background var(--surface-2) + 0.5px var(--card-bd) border; .modal + 0.5px var(--card-bd) border. (2) CONTACT AVATAR RED (pic2 Alex Chen): slotForName drew from COLOR_SLOTS incl "red" -> some names got brand red. Fix: AVATAR_SLOTS = COLOR_SLOTS minus "red"; slotForName uses it (red reserved for accent/urgency/category). (3) DOUBLE BACK CHEVRON (pic1): .nav-back::before already draws an iOS chevron, but 6 detail screens (ProjectDetail, CategoryDetail, BrainDocPage, AreaDetail, PeopleList, PersonDetail) ALSO put an SVG {BACK} inside -> two chevrons. Fix: emptied those nav-back buttons (::before chevron only) + removed the now-dead BACK consts. NOTE: standing rule -> nav-back content is a TEXT label or empty; NEVER an explicit chevron (the ::before provides it). LargeTitleNav already correct (text labels). 217 tests, tsc clean, 0 em dashes, demo+zip rebuilt.

## AVATAR OVERFLOW FIX (Dave pic: "RC" spilling out of circle). Root cause: .av-72 was USED (PersonDetail + AccountPage profile avatars) but NEVER DEFINED in CSS -> no width/height/font-size, so the circle sized to content and 2-letter initials overflowed. Fix: defined .av-72 { width:72px; height:72px; font-size: var(--t-h1); } next to av-56; added overflow:hidden to .av as a clip safety net. (Other used sizes av-32/av-40 were defined; only av-72 missing.) STANDING CHECK: any av-NN class used must exist in CSS. 217 tests, tsc clean, 0 em dashes, demo+zip rebuilt.

## AVATAR COLOR = JARVIS RED DEFAULT + USER-EDITABLE (Dave: "default to Jarvis red with ability to edit; full visual customization"). 
- people/types.ts: PersonData += color?: ColorSlot. avatarClass(color?) => !color||color==="red" ? "av-accent"(brand red) : "cat-bg-"+color. AVATAR_COLORS = COLOR_SLOTS (8, red first = default).
- PersonSheet: PersonDraft += color; default "red"; Color field = .swatch-row of .av-swatch buttons (each avatarClass(slot), selected gets ring). save passes color.
- PeopleFlow create/update pass color: d.color.
- PersonDetail + PeopleListPage render avatarClass(data.color) (was slotForName) -> default red, per-contact override.
- AccountPage avatar -> av-accent (red) to match Today (user identity = brand red).
- CSS: .swatch-row/.av-swatch/.av-swatch.sel(ring). Updated PersonSheet.test (draft now has color:"red").
- slotForName/AVATAR_SLOTS retained but no longer drives avatars (kept for any future use). NOTE: broader "full visual customization" (themes/skins) = planned Milestone C skin system; this delivers per-contact avatar color now.
- 217 tests, tsc clean, 0 em dashes, demo+zip rebuilt.

## STRATEGIC CORRECTION (Dave 5/25): The Project description / "Monetizing Jarvis" plan frames the STUDENT template as "the wedge / fastest path to paying users / first milestone = 50-100 paying Student users". DAVE OVERRIDES THIS: students are NOT a priority over anyone else. Treat the three templates (Personal/Business/Student) as EQUAL; do NOT assume student-first or a student wedge in future planning. Monetize broadly, not segment-gated. (Source of the old framing was the Project instructions, not Claude's invention - but Dave's latest instruction takes precedence.) Revised order under discussion: 1) deploy built Personal app first (de-risk pipeline), 2) settle auth (Supabase-built vs Clerk-in-plan), 3) Stripe payments for everyone, 4) Business+Student templates as equal expansions, 5) legal/IP parallel, 6) native phase last. AWAITING Dave's confirm on order.

## ROADMAP LOCKED (Dave 5/25): Build fully before monetizing; validate each surface with private testers; NO public App Store listing until launch (idea-theft concern -> use private beta instead). Order:
1. Personal -> private PWA beta NOW (Dave + handful, share hosted link, no Apple needed). Iterate on feedback.
2. Build student/business/gaming ONE AT A TIME (not all blind in parallel); add each to private beta as testable; gather feedback per mode. Dave wants ALL modes (incl gaming) tested.
3. Trademark JARVIS + concept doc in escrow NOW (parallel, cheap IP insurance).
4. Public App Store listing + Stripe payments ONLY at real launch, once all modes validated.
- Templates are EQUAL (no student-first). Monetize broadly. Native phase features (Music/photos/Maps tiles/Voice) come with the native shell.
- NATIVE SHELL: app is currently a PWA (correct for the handful). For TestFlight + App Store + native features, recommend wrapping existing React app in CAPACITOR (reuses 100% of build, native plugins; avoid a Swift/RN rewrite). Wrap at the PWA-handful -> broader-beta/native-features boundary. CAPACITOR CONFIRMED (Dave delegated the call 5/25). Wrap existing React app in Capacitor at the PWA-handful -> TestFlight/native boundary; no rewrite.
- Pre-public-launch gates still open: settle auth (Supabase-built vs Clerk-in-plan), rate limiting at /api/ai proxy (Anthropic cost), wire Sentry DSN, deploy the AI proxy. Apple Developer account needed for TestFlight.
