[
 {
  "kind": "needs_other_file",
  "id": 128,
  "file": "jarvis-app/src/tasks/screens/SnoozeSheet.tsx, ReminderSheet.tsx, ReminderSettingsSheet.tsx, ReminderDetailSheet.tsx, ContextPromptSheet.tsx; jarvis-app/src/bigger/GoalDetailPage.tsx; jarvis-app/src/routine/RoutineFlow.tsx",
  "change": "Replace the \" \u00b7 \" baked into <Note> strings with \". \". Sites: SnoozeSheet.tsx:38 \"Only this occurrence changes \u00b7 The schedule stays the same\"; ReminderSheet.tsx:266 \"What this reminder is about \u00b7 Opening it never marks the reminder done\"; ReminderSettingsSheet.tsx:48 \"Follow-ups and in-app prompts wait until quiet hours end \u00b7 A reminder's own alert still rings\"; ReminderDetailSheet.tsx:94 \"Opening it keeps the reminder active \u00b7 The ring marks it done\"; ContextPromptSheet.tsx:44 \"This reminder asked to be shown here \u00b7 It responds to what you open inside JARVIS, never to another app\"; GoalDetailPage.tsx:468 \"Optional \u00b7 Whatever you write is kept with the decision\"; RoutineFlow.tsx:426 \"A call fits \u00b7 Typing does not\". These are sentence notes under a field, not facts lines, so under R6 they may be out of scope. Leave them to the lead or Phase D.",
  "group": "components.css#1",
  "phase": "css",
  "done": true,
  "note": "middot half not changed: shortCopy law + R6 (field notes are not facts lines)"
 },
 {
  "kind": "needs_other_file",
  "id": 114,
  "file": "jarvis-app/src/review/ReportPage.tsx",
  "change": "Line 94 `{stillOpen ? \"Your Month \u00b7 Still Open\" : \"Your Month\"}` bakes a middot into the hero kicker. Suggested reword: `{stillOpen ? \"Your Month So Far\" : \"Your Month\"}`. The nav title is only the month name, so \"So Far\" carries the still-open meaning without a separator. \"Still Open\" is not one of the 13 state words, so a .fact.st route would need a ruling.",
  "group": "components.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 280,
  "file": "jarvis-app/src/gym/SessionScreen.tsx",
  "change": "Line 659: change <span className=\"fact cyan\">{`Saves ${leverSave} min`}</span> to <span className=\"fact est\">{`Saves ${leverSave} min`}</span>. The saving is an estimate, so it takes sky. .fact.est already exists in components.css with --cat-sky, and no ruled.css rule outranks it on health.",
  "group": "components.css#2",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 293,
  "file": "jarvis-app/src/gym/LiftDetailScreen.tsx",
  "change": "Line 309: drop \"blue\" from the trend headline, <div className=\"p3-q\">. For kind === \"weight_reps\" the number is the Epley estimate, so wrap it in the existing primitive, <span className=\"fact est\">\u2026</span>. .fact has no size of its own, so it inherits the h1. Other kinds stay plain white. Then delete `.p3-q.blue { color: var(--blue); }` and its comment from components.css, since this line is its only user. I left the CSS rule in place because deleting it before the TSX changes would paint the estimate white.",
  "group": "components.css#2",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 294,
  "file": "jarvis-app/src/gym/LiftDetailScreen.tsx",
  "change": "Line 284: change <span className=\"fact amber\">{`${bestE1rm}${unit ? \" \" + unit : \"\"}`}</span> to <span className=\"fact est\">\u2026</span>. The estimated one-rep max is an estimate (sky), not over/next (amber). No CSS change is needed because .fact.est already exists.",
  "group": "components.css#2",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 317,
  "file": "jarvis-app/src/gym/ClassifySheet.tsx",
  "change": "Only if Dave picks the neutral chip: drop the hue props (lines 294, 299, 310, 337, 343, 349, 351-354) and the \" chip-lime\" classes (lines 218, 259). Then delete the four :where(.sheet-scrim > .card) .chip\u2026chip-{lime,violet,cyan} rules in components.css and the matching .ex-chip hue rules in ruled.css.",
  "group": "components.css#2",
  "phase": "css",
  "done": true,
  "note": "held for Dave (dave-queue)"
 },
 {
  "kind": "needs_other_file",
  "id": 541,
  "file": "jarvis-app/STYLING_CATALOG_V3.md",
  "change": "Line 56 (STEEL HEADS APP-WIDE) still says detail-page group labels (.grp .eyebrow: Decided, Because, Details) 'ride the same treatment' (red mini-caps + dotted leader). Amend it to say .grp .eyebrow is quiet since \u00a7AM (2026-09-25): --tx-3 with a --divider dotted leader, because a label cannot be tapped.",
  "group": "components.css#3",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 576,
  "file": "jarvis-app/src/styles/ruled.css",
  "change": "Line 755: remove `.ruled .sched-until, .ruled .sched-rep` from the `font-size: var(--t-sub)` list, keeping `.ruled .sched-cat, .ruled .sched-sep, .ruled .sched-until-btn`. At 0,2,0 that rule otherwise beats the new small caps, and Today and Schedule are both .ruled, so the caps would render at 14px (R7). Line 757's nowrap can stay.",
  "group": "components.css#4",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 576,
  "file": "jarvis-app/src/schedule/screens/DayRow.tsx",
  "change": "Optional, for the key colour: line 376, \"Leave by\" means needs you soon, so `className=\"sched-until\"` could become `className=\"sched-until fact warn\"`. .fact.warn is later in components.css at equal specificity, so it wins. It is already R1-clean as grey small caps. Lines 382-386: firstMove's separator is not wrapped in a .sched-fact, so it can strand at a wrap (finding 49's second half).",
  "group": "components.css#4",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 578,
  "file": "jarvis-app/src/tasks/screens/RemindersPage.tsx",
  "change": "Line 147: `const tone = it.state === \"open\" && it.date === today ? \"when\" : \"later\";` should give \"date\" instead of \"later\", so a neutral future date uses the .fact.date primitive. Paused rows are still two greys: whenWords gives \"Paused\" as a plain .fact next to the plain rhythm .fact at line 203. Line 179's `\"fact \" + (it.state === \"open\" ? tone : \"\")` could use \"date\" for non-open states too, or drop the rhythm on a paused row.",
  "group": "components.css#4",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 575,
  "file": "jarvis-app/src/bigger/BiggerPicturePage.tsx",
  "change": "Line 232: `<div className=\"bp-sub\">{sized}</div>` should become `<div className=\"bp-sub\"><span className=\"fact est\">{sized}</span></div>`, the estimate primitive (sky). Line 237: the pace line from progress.ts projectPace bakes in \" \u00b7 \" and mixes meanings: \"Past its date\" is .fact.red, \"Due today/tomorrow\" is .fact.warn, and \"About N a day\" is .fact.est. It needs splitting into facts, or the progress line and the pace line stay two greys.",
  "group": "components.css#4",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 579,
  "file": "jarvis-app/src/today/RemindersStrip.tsx",
  "change": "Comment only, lines ~137-138: \"A missed reminder marks its TIME, not the whole row ... Never red, never a count.\" It is stale now that the time is --sys-red under \u00a7AM. Change it to \"marks its time in the key's red, not the whole row; never a count\".",
  "group": "components.css#4",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 606,
  "file": "src/review/InsightsFlow.tsx, src/review/week.ts, src/notes/screens/NoteEditor.tsx, src/people/screens/PersonDetail.tsx, src/brain/strands/state.ts, src/notes/screens/NotesList.tsx",
  "change": "Move each purp call site to the key first, keeping one grey per line and at most one coloured fact (astra law 4). InsightsFlow.tsx:273 \"3 Moved\": `fact purp` -> `fact good` as the finding says, keeping \"41 Done\" as the one grey; or the \"41 Done\" fact becomes `fact good` and \"3 Moved\" becomes `<span className=\"fact\"><b>{moved}</b> Moved</span>`. week.ts:137 fact `tone: \"purp\"` -> undefined, and check what the line tone at week.ts:139 renders. NoteEditor.tsx:538: the kind word becomes plain `fact` (its `sky` half already paints grey, since .fact.sky is retired), and `c.due` becomes `fact date`. PersonDetail.tsx:350: \"You promised\" -> `fact warn`, the date -> `fact date`. strands/state.ts:41: LEARNED -> \"good\". NotesList.tsx:292: drop ` fact purp` (it already renders grey under .ruled .r-goal.r-cat). Only then, in components.css, delete `.fact.purp { color: var(--cat-purple); }` and its two theme variants (`[data-theme=\"light\"] .fact.purp`, `[data-theme=\"dark\"] .fact.purp`, around components.css:5903 and 5935-5936).",
  "group": "components.css#5",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 607,
  "file": "jarvis-app/src/decisions/DecisionsFlow.tsx",
  "change": "Line 539: `<span className={\"fact fact-link \" + glyphClass(r, projCat)} key={l.id}>{l.label}</span>` -> `<span className=\"fact cat fact-link\" key={l.id}><span className={\"cd \" + glyphClass(r, projCat).replace(\"cat-fg-\", \"cat-bg-\")} /><span className=\"cat-t\">{l.label}</span></span>`. That is the category primitive: the dot carries the hue and the words stay grey. The co-located DecisionsList.test.tsx:32 `screen.getByText(\"Rebuild Bridge App\").className` would then find the inner .cat-t span, so change it to `.closest(\".fact-link\")!.className` or similar.",
  "group": "components.css#6",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 623,
  "file": "jarvis-app/src/gym/CondReceipt.tsx",
  "change": "Lines 50-52: `{entries.length === 0 && (<div className=\"cr-empty\">{lastLine ?? \"Not run yet\"}</div>)}` -> `{entries.length === 0 && lastLine && (<div className=\"cr-empty\">{lastLine}</div>)}`. This drops the placeholder that says nothing (R1); it is a wording change, so list it for Dave.",
  "group": "components.css#6",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 648,
  "file": "jarvis-app/src/styles/jarvis-design-system.css",
  "change": "Line 693, the base `.btn`: `background: var(--press-3); color: var(--tint); }` -> `background-color: var(--capsule-fill); color: var(--tint); }`. Warning: laws.test.ts:3148 ('the quiet CTA is neutral...') pins `/background: var\\(--press-3\\); color: var\\(--tint\\)/` against all stylesheets combined, and after my components.css change that text survives only in jarvis-design-system.css:693. Changing DS will fail that law, so amend it in place with a dated reason, e.g. to `/background-color: var\\(--capsule-fill\\); color: var\\(--tint\\)/`.",
  "group": "components.css#6",
  "phase": "css",
  "done": true,
  "resolution": "Resolved 2026-09-26 by the lead's decision, not as proposed: the 50px base .btn in jarvis-design-system.css is not a capsule and keeps `background: var(--press-3); color: var(--tint)` (so the laws.test.ts quiet-CTA pin stays true, unamended). \u00a7AL binds .btn-sm: the unthemed .btn-sm:not(.btn-primary):not(.btn-danger):not(.btn-secondary) chain already takes background-color var(--capsule-fill) with the --tint label, and components.css now states the dark half explicitly beside the light rule ([data-theme=\"dark\"] chain: capsule fill, --tint label)."
 },
 {
  "kind": "needs_other_file",
  "id": 621,
  "file": "jarvis-app/src/gym/GymFlow.tsx",
  "change": "Optional tidy. The comment at lines 478-480 ('pairing wears the blue data tag, a ramp wears the amber prep tag') is now stale: pairing is Health violet and a ramp is the neutral tag. You could also rename the classes to say what they are (for example xtag-blue -> xtag-pair); that also needs components.css.",
  "group": "components.css#6",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 16,
  "file": "jarvis-app/src/today/TodayPage.tsx, jarvis-app/src/brain/CategoryDetail.tsx, jarvis-app/src/today/TodayPage.test.tsx",
  "change": "Optional cleanup; the visual fix is already live in ruled.css. (1) TodayPage.tsx:464 and CategoryDetail.tsx:2047: `stat-tile st-time` -> `stat-tile st-quiet`. (2) TodayPage.test.tsx:53-54: change the `.st-time` selectors to match. (3) Then remove `.ruled .st-time,` and `.ruled .st-time .st-n,` from the two combined st-quiet selectors in ruled.css.",
  "group": "ruled.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 45,
  "file": "jarvis-app/src/styles/jarvis-design-system.css (+ ruled.css)",
  "change": "Mint a late-red text token. The light --sys-red (#FF3B30) fails text contrast, so light lateness is routed through --on-light-red, which the rulebook counts as brand (tap) red. Add e.g. `--sys-red-tx: var(--sys-red)` in dark and a text-safe late red in the light block. Then ruled.css `[data-theme=\"light\"] .ruled .uchip.u-late { color: var(--on-light-red, #B8001A); }` -> `color: var(--sys-red-tx)`, and do the same for `[data-theme=\"light\"] .ruled .st-late`. That also drops the raw #B8001A fallback on both. The GUILT law still exempts both, since the matched selector is `.u-late` / `.st-late`.",
  "group": "ruled.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 154,
  "file": "jarvis-app/src/styles/jarvis-design-system.css, jarvis-app/src/styles/components.css (+ ruled.css)",
  "change": "Duplicate of 45. Same new late-text token, also applied to components.css `[data-theme=\"light\"] .urgency-red { color: var(--on-light-red); }` (~line 1873).",
  "group": "ruled.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 68,
  "file": "jarvis-app/src/schedule/screens/SchedulePage.tsx",
  "change": "Line 604 has a baked middot inside the wk-note line. After it, the duration is a second bare grey run, and it only repeats the range already stated. Reword: `Longest open stretch <b>{WK[best.row.dow]} {stretchLabel(best.s, best.e)}</b> \u00b7 {spanShort(best.e - best.s)}` -> `Longest open stretch <b>{WK[best.row.dow]} {stretchLabel(best.s, best.e)}</b>` (e.g. 'Longest open stretch Wed 1:00 to 5:30 PM \u00b7 4h 30m' -> 'Longest open stretch Wed 1:00 to 5:30 PM'). If the duration is kept instead, use `<span className=\"sched-sep\">` and `<b>` the duration.",
  "group": "ruled.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 102,
  "file": "jarvis-app/src/styles/components.css",
  "change": "Cleanup: line ~4056 `.r-cue { color: var(--cat-sky); font-variant-numeric: tabular-nums; }` -> `.r-cue { font-variant-numeric: tabular-nums; }`. Sky means an estimate, not a cue. The rule never painted inside .ruled, which is the only place .r-cue renders.",
  "group": "ruled.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 104,
  "file": "jarvis-app/src/tasks/screens/TasksPage.tsx",
  "change": "Rewording (dropping a repeated fact). When the origin branch at lines ~422-432 drew the envelope and 'Email' (no kicker, tag, parent or named category, but originLabel(t) present), line ~488's `<Provenance compact .../>` repeats it as 'From an email'. Render Provenance only when that origin branch did not draw, so '\u2709 Email \u00b7 From an email' becomes '\u2709 Email'. The row law needs `<Provenance` to stay inside the r-k block, so keep it there, conditioned.",
  "group": "ruled.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 245,
  "file": "jarvis-app/src/health/screens/RefillRunwayScreen.tsx (+ ruled.css)",
  "change": "Numbers are white now. For the state colour, give the Doses Left tile `stat-warn` when needsRefillCall(state), then add to ruled.css `.ruled.health-ruled .stat-row.stat-hblue .stat-tile.stat-warn .stat-num { color: var(--hl-amber-ink); }`. The health rule (0,5,0) outranks components.css `.stat-tile.stat-warn .stat-num`. Optional: rename `stat-hblue` to a neutral name, since it no longer paints blue. Also review the 'Not Yet' placeholder in the runway tile (\u00a7AK: a row with nothing to say shows nothing), and `.fact hblue` on 'Filled {date}' on the same card.",
  "group": "ruled.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 271,
  "file": "jarvis-app/src/gym/GymFlow.tsx (+ ruled.css, new chip variant = lead decision)",
  "change": "GymFlow.tsx:3428: the Up Next 'EST 45 Min' chip is `se-chip se-chip-budget` (Health violet = budget or pair), but it is an estimate, which is sky under the key. No existing se-chip variant is sky, and the rulebook forbids inventing one here. Lead: add e.g. `.ruled.health-ruled .se-chip-est { color: var(--cat-sky); background: color-mix(in srgb, var(--cat-sky) 16%, transparent); }` to ruled.css and change the className to `se-chip se-chip-est`. SessionScreen.tsx:588's real Budget chip keeps se-chip-budget.",
  "group": "ruled.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 278,
  "file": "jarvis-app/src/gym/progression.ts",
  "change": "R6: the Basis values render in .ins-sub value cells with baked ' \u00b7 ' separators. Suggested (the finding's own fix): :163 `\" \u00b7 \" + opts.equipmentLabel` -> `\", \" + opts.equipmentLabel`. :164 `when + \" \u00b7 \" + ...` -> `when + \", \" + ...`. :165 'Completed working sets only \u00b7 Warm-ups and drops left out' -> 'Completed working sets only, warm-ups and drops left out'. :168 `.join(\" \u00b7 \")` -> `.join(\", \")`. Check progression.test.ts for these basis strings. These are wording changes, so report them in 'reworded'.",
  "group": "ruled.css#2",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 389,
  "file": "jarvis-app/src/notes/screens/NotesList.tsx",
  "change": ":292 `<span className=\"r-goal r-cat fact purp\">{\"\u00b7 JARVIS found \" + n.found}</span>` -> `<span className=\"r-goal\">{\"JARVIS found \"}<b>{n.found}</b></span>`. This uses the 'number with no state' primitive; `.ruled .task-row .r-goal b` already paints it --tx-1, and .note-row is a .task-row, so no CSS is needed. Drop `r-cat fact purp`: `.ruled .r-goal.r-cat` beats .fact.purp so the purple never painted, and purple words would be a category colour on words anyway. The leading '\u00b7 ' belongs to middot finding #391. If Dave reads 'found' as waiting on him, use amber instead, which needs a .r-goal-level amber rule (a lead decision, not a new per-screen class).",
  "group": "ruled.css#2",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 418,
  "file": "jarvis-app/src/settings/AIControlPage.tsx",
  "change": ":96 R6: the .row-value string `${formatTokens(inTok)} in \u00b7 ${formatTokens(outTok)} out` -> `${formatTokens(inTok)} in, ${formatTokens(outTok)} out`. This is a wording change, so report it in 'reworded'.",
  "group": "ruled.css#2",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 433,
  "file": "jarvis-app/src/settings/LearnedRulesPage.tsx",
  "change": ":69 `className=\"pill-act row-act-pill\"` -> `className=\"pill-act\"`. In the SAME change, delete `.ruled .row-act-pill { font-size: var(--t-meta); }` and its comment from ruled.css. laws.test.ts fails if the rule goes first, and the rule is dead once the class goes.",
  "group": "ruled.css#2",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 435,
  "file": "jarvis-app/src/settings/kit.tsx",
  "change": ":85 `return <div className=\"pad-x set-foot\">{children}</div>;` -> `return <div className=\"pad-x\"><div className=\"input-hint\">{children}</div></div>;`. In the SAME change, delete the `.ruled .set-foot` rule and its comment from ruled.css. Once nothing renders .set-foot, subtextLaw 'no subtext class survives without a call site' fails on it, and laws.test.ts fails if the rule goes first.",
  "group": "ruled.css#2",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 580,
  "file": "jarvis-app/src/styles/components.css",
  "change": "Line 1008: `.money-amt.paid { color: var(--tx-3); font-weight: var(--w-regular); }` -> `.money-amt.paid { color: var(--good); }`. This only matters outside `.ruled`. Every current call site (MoneyFlow.tsx:614) is under `.screen.ruled`, where the ruled.css rule wins. Apply it together with the ruled.css change and the law amendment in blocked_by_law.",
  "group": "ruled.css#3",
  "phase": "css",
  "done": true
 },
 {
  "kind": "blocked_by_law",
  "id": 9,
  "law": "src/laws/browserWalk.test.ts:567-574 (\"live metadata takes the quiet token, not the dim one\"). The LIVE list requires the exact `.focus-skip` rule to contain `color: var(--tx-quiet)`.",
  "why": "\"Not This One\" (FocusScreen.tsx:159) is a bare-words tappable control drawn in --tx-quiet. The key gives tappable non-button text --tint, and \u00a7AL says a control is never bare words. The finding's fix, --tint on .focus-skip, would fail this law. \u00a7AL offers another route: make it the quiet option, `className=\"quiet-action\"` in FocusScreen.tsx (press-1 fill, pill, --tx-2). That also clears \u00a7AL, but it needs a file I do not own. The lead should either amend the law, dropping .focus-skip from LIVE with a dated reason, or take the quiet-action route in FocusScreen.tsx.",
  "group": "components.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "blocked_by_law",
  "id": 43,
  "law": "src/laws/browserWalk.test.ts:567-574 (LIVE list pins `.receipt-line` to `color: var(--tx-quiet)`), with the BROWSER-F-09 note on the rule itself",
  "why": "The finding adds `button.receipt-line { color: var(--tint) }`. That would pass the law's text match, because only the base rule is read. It would defeat the law's stated intent, though: the pin on --tx-quiet was written knowing every receipt line is a BUTTON (\"13 More waiting\" and so on), and the I5 comment says \"It stays dim; the glyph does the work, not colour\". Dave also moved \"N more in Anytime\" from a red link to this quiet line on 2026-09-11. Turning all of them red on Today, Life and Goals conflicts with that pin and that history. It needs the law amended with a dated reason, or the finding rejected. I did not game the law with an override. The 11px-caps half (#42) is applied.",
  "group": "components.css#1",
  "phase": "css",
  "done": true,
  "note": "held for Dave (dave-queue)"
 },
 {
  "kind": "blocked_by_law",
  "id": 555,
  "law": "src/laws/browserWalk.test.ts line ~414-424, \"the sheet and toast verbs take it\"",
  "why": "The finding is real: in dark, every .row-act inside a sheet (Add Item, Add a Window, Edit Routine) paints a white label. `.form-sheet .row-act` and `.sheet-scrim > .card .row-act` (components.css:1588/1590, 0,3,0) set it to --tint-on-sheet (#FFFFFF), which beats the capsule's --tint. The correct fix is to delete those two selectors. The capsule now carries an opaque --capsule-fill (#17171A), where --tint measures 4.82:1, so the white-label workaround for a translucent sheet grey no longer applies. Light is unaffected, because --accent-tx in light is #BC000E, the same as --on-light-red. The law loops over [\".sheet-bar-save\", \".row-act\", \".note-fix\", \".toast-action\"] and requires each in the --tint-on-sheet selector list, so the law must drop \".row-act\". I did not work around it with a later override, because that would defeat the law's stated intent.",
  "group": "components.css#4",
  "phase": "css",
  "done": true
 },
 {
  "kind": "blocked_by_law",
  "id": 606,
  "law": "src/laws/astra.test.ts:62 (\"components.css draws .facts, .fact variants, .row-star, .why and .dring\")",
  "why": "It requires \".fact.purp\" to be in components.css, so the correct fix (delete the purple text variant, since purple is not in the key) fails it. The same file's \"every fact variant the app renders has a rule somewhere\" would also fail while the six call sites still render `purp`. Amend in place once the call sites move: take \".fact.purp\" out of the required list and add it to the not.toContain retired-variant check beside \".fact.sky\".",
  "group": "components.css#5",
  "phase": "css",
  "done": true
 },
 {
  "kind": "blocked_by_law",
  "id": 104,
  "law": "src/laws/laws.test.ts, \"the row's provenance is the compact fact, and gives up the tap padding that would not fit\" (~line 5612): `expect(r).toMatch(/font-size:\\s*var\\(--t-caption\\)/)` on `.ruled .r-k-one > .prov-line`",
  "why": "The fix needs a subtext size other than the pinned 13 (R7), plus a distinction from the recurrence's grey on the same line (R1). The right change is the letterform the cue now wears: `font-size: var(--t-eyebrow); font-weight: var(--w-semi); letter-spacing: var(--track-caps); text-transform: uppercase;`. The law's reason (15px meta would be sliced by the 19px clamp) does not apply to 11 caps. Amend the pin from --t-caption to --t-eyebrow with a dated reason.",
  "group": "ruled.css#1",
  "phase": "css",
  "done": true
 },
 {
  "kind": "blocked_by_law",
  "id": 580,
  "law": "src/laws/browserWalk.test.ts:599-607, 'the deliberately dimmed states recede to secondary, never to --tx-4'",
  "why": "The finding is real now. On a paid bill row, 'Paid Sep 1' (`.r-goal.r-cat`, `--tx-3` regular) sits beside '$1,800' (`.ruled .task-row .money-amt.paid`, `--tx-2`, which is the same hex, and semibold because the ruled base rule sets `--w-semi`). That is two greys that differ only by weight (R1). The key also says paid is green (R3). The correct fix is `.ruled .task-row .money-amt.paid { color: var(--good); }`. This law requires that exact selector to exist with `color: var(--tx-2)`, a 2026-09-14 ruling that \u00a7AK V5.2 and \u00a7AM have since overtaken. Amend it to drop `.ruled .task-row .money-amt.paid` from its list (or pin `--good` for it), with a dated reason. A more-specific override rule would get around the law without meeting its intent, so it was not added. Without a law change, the fallback is to mark the sub line `r-goal fact good` in MoneyFlow.tsx:611 when paid. That does not work either: `.ruled .r-goal.r-cat` (0,3,0) out-ranks `.fact.good` (0,2,0), so that markup would also need a ruled rule.",
  "group": "ruled.css#3",
  "phase": "css",
  "done": true
 },
 {
  "kind": "needs_other_file",
  "id": 33,
  "file": "jarvis-app/src/today/TodaySuggestions.tsx (+ TodaySuggestions.test.tsx, + my routinePatterns.ts in the same change)",
  "change": "Land these together. Applying the routinePatterns.ts half alone would either break TodaySuggestions.test.tsx:107 (/Gym \u00b7 Around 6 AM/) or drop the evidence count the offer card must show, so I left routinePatterns.ts untouched. (1) routinePatterns.ts: add `sub: string` to RoutineCandidate, import { capAfterNumber } from \"../shared/casing\", and change line 95 `text: `${title} \u00b7 Around ${min12h(startMin)} ${daysSummary(days)} \u00b7 ${evs.length} times this month`,` to `text: `${title} around ${min12h(startMin)} ${daysSummary(days)}`, sub: capAfterNumber(`${evs.length} times this month`),`. (2) TodaySuggestions.tsx:139: `{ id: routineC.id, text: routineC.text, routineBlock: routineC.block }` becomes `{ id: routineC.id, text: routineC.text, sub: routineC.sub, routineBlock: routineC.block }`. The candidate type already has sub?, and the card already renders sub={pattern.sub}. (3) TodaySuggestions.test.tsx:107: /Gym \u00b7 Around 6 AM/ becomes /Gym around 6 AM/ (optionally also assert '3 Times this month'). Reword: 'Gym \u00b7 Around 6 AM weekdays \u00b7 5 times this month' becomes title 'Gym around 6 AM weekdays' with sub '5 Times this month'. Accepting a routine uses routineBlock, not the text, so nothing stored changes.",
  "group": "code-2:today:MoveHeadliner.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 59,
  "file": "jarvis-app/src/today/YourDay.test.tsx",
  "change": "Line 349 pins screen.getByText(\"Protected\") on Today, and every YourDay DaySet branch passes stateWords, so that row always carries the PROTECTED state word too. The finding's full fix drops the kicker words that repeat the state word, and that would break this test. Change the assertion to getByText(\"PROTECTED\"), the state word. Then follow up in LockedRow.tsx (mine) with: kicker = state ? (m === \"blends\" ? \"Can blend, \" + free : \"\") : <current kicker>. That drops 'Protected' beside PROTECTED and 'Focus time' beside FOCUS. Not done here, so the test keeps passing.",
  "group": "code-3:schedule:stateWord.ts",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 77,
  "file": "jarvis-app/src/styles/ruled.css",
  "change": "When the event page's provenance line cannot be opened, it is still a 14px regular grey, a second grey beside the place. Extend the existing caps rule so it covers this line: `.ruled .sched-body > .prov-line, .ruled .row-pair + .prov-line { font-size: var(--t-eyebrow); font-weight: var(--w-semi); letter-spacing: var(--track-caps); text-transform: uppercase; }`. EventDetailPage's header card is the only place a Provenance follows a .row-pair. Related, and outside my files: sourceLine in shared/provenance.ts bakes ' \u00b7 ' into 'From Smart Paste \u00b7 2:14 PM'.",
  "group": "code-3:schedule:stateWord.ts",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 603,
  "file": "jarvis-app/src/styles/components.css",
  "change": "Nothing uses these rules any more; delete them (currently about lines 4971-4975): .rep-win-blue {...} .rep-win-blue .rep-win-val {...}, .rep-win-purple {...} .rep-win-purple .rep-win-val {...}, .rep-win-warn {...} .rep-win-warn .rep-win-val {...}, and the two raw-hex light rules [data-theme=\"light\"] .rep-win-blue .rep-win-val { color: #0A5FD0; } and [data-theme=\"light\"] .rep-win-purple .rep-win-val { color: #7A2F9E; }. Keep .rep-win-good. (This covers 115 as well.)",
  "group": "code-4:review:week.ts",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 604,
  "file": "jarvis-app/src/styles/components.css",
  "change": "Nothing uses these rules any more; delete them (currently about lines 4943-4945): .stat-tile.stat-blue { background: rgba(10, 132, 255, 0.14); }, .stat-tile.stat-blue .stat-num { color: var(--blue); } and the raw-hex [data-theme=\"light\"] .stat-tile.stat-blue .stat-num { color: #0A5FD0; }. Also trim the \"Sessions tile\" sentences in the comment above them. Keep the .week-dots rules below it, and keep .stat-tile.stat-sky and .stat-tile.stat-warn, which RefillRunwayScreen uses.",
  "group": "code-4:review:week.ts",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 139,
  "file": "jarvis-app/src/bigger/GoalDetailPage.tsx",
  "change": "Line 341 renders the same decision banner, with a middot baked into promo-sub and the \"No reason recorded\" placeholder. Apply the same fix there so the two banners match: `{decision.data.why && <div className=\"promo-sub\">Because {decision.data.why}</div>}<div className=\"facts\"><span className=\"fact date\">Decided {fmtDay(decision.data.createdAt)}</span></div>`",
  "group": "code-6:projects:types.ts",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 311,
  "file": "jarvis-app/src/gym/classify.ts",
  "change": "Three functions bake \" \u00b7 \" into strings that render inside `.fact` spans: `valueLine` for equipment (`${label} \u00b7 ${COUNTED_LABEL[counted]}`), `valueLine` for muscles (`${p} \u00b7 Also ${s}`), and `identityLine` (`bits.join(\" \u00b7 \")`). The places are DuplicateReview's Side kit fact, DuplicatesSheet's amber 'X and Y' fact, and the Conflicts facts. Join with \", \" instead, the way equipment.ts styleSummary now does, and update classify.test.ts:101. Until then the Side equipment fact can mix separators, e.g. 'Dumbbells \u00b7 The Whole Load, Home Gym \u00b7 Leg Press'.",
  "group": "code-10:gym:SetStrip.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 265,
  "file": "jarvis-app/src/gym/editor.test.ts",
  "change": "Apply these two changes together. (1) GymFlow.tsx ExerciseRow (~line 519): change `{exercise.restSec ? <span className=\"fact\">{`${mmss(exercise.restSec)} rest`}</span> : null}` to `{exercise.restSec ? <span className=\"fact\"><b>{`${mmss(exercise.restSec)} rest`}</b></span> : null}`. The rest is a length with no state, so it is a white <b>, the lead's settled rule for untappable lengths. That leaves the row's one grey to the .row-ghost note. (2) editor.test.ts:323: change the regex to `/exercise\\.restSec \\? <span className=\"fact\"><b>\\{`\\$\\{mmss\\(exercise\\.restSec\\)\\} rest`\\}<\\/b><\\/span> : null/`. I applied (1), confirmed it was right, then reverted it: editor.test.ts pins the old literal and is not a co-located test of a file I own. GymFlow.tsx is back to its original line. If you would rather use caps, .fact.date (\"1:30 REST\") is the alternative primitive, with the same test edit.",
  "group": "code-11:gym:ClassifySheet.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 619,
  "file": "jarvis-app/src/gym/editor.test.ts",
  "change": "Same fix as #265: the rest fact becomes a white <b> in GymFlow.tsx:519, and editor.test.ts:323's regex is updated to match. The finding's CSS half is not needed and should not be applied. It proposed a new `.fact.fact-time` class, and the rulebook forbids new classes when a primitive exists. It also proposed changing `.row-ghost` from `--t-meta`/`--tx-2` to `--t-sub`/`--tx-3`, but those are the same 14px and the same hex, so nothing would change on screen.",
  "group": "code-11:gym:ClassifySheet.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 269,
  "file": "jarvis-app/src/styles/ruled.css",
  "change": "Optional tidy: `.ruled.health-ruled .se-chip-skip { color: var(--hl-amber-ink); background: var(--hl-amber-tint); }` (ruled.css ~1905) now has no user in src after the Logged Later chip moved to se-chip-when. Delete it so a dead amber variant isn't picked up by accident. No law currently catches it.",
  "group": "code-11:gym:ClassifySheet.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 377,
  "file": "jarvis-app/src/insights/exportData.ts",
  "change": "The CSV Detail column read r.detail, which used to contain ' \u00b7 Duration needs review'. That text is now in the new optional r.review field, so until this is changed a flagged workout's CSV row no longer says it needs review. In buildCsv, write the Detail column as `[r.detail, r.review].filter(Boolean).join(\"; \")`. The summary export already says 'Sessions whose duration needs review: N'.",
  "group": "code-13:insights:AllDataPage.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 377,
  "file": "jarvis-app/src/health/checkin.ts",
  "change": "checkInLine() joins 'Energy X' and 'Mood Y' with ' \u00b7 '. That string is a check-in record's value and renders inside a .fact on All Data, so it is a baked middot (R6). Join with ', ' instead. Its other callers (log.ts detail, CheckInScreen title, doctorReport) are fine with a comma.",
  "group": "code-13:insights:AllDataPage.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 387,
  "file": "jarvis-app/src/insights/AssignMusclesSheet.tsx",
  "change": "Line 58: `<span className=\"fact cyan\">{capAfterNumber(`${assigned} of ${untagged.length} assigned`)}</span>` is a count painted in Health's 'now' cyan. Use `fact amber` while assigned < untagged.length (it still needs the person), otherwise `fact lime`.",
  "group": "code-13:insights:AllDataPage.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 498,
  "file": "jarvis-app/src/money/budget.ts",
  "change": "Comment only, nothing depends on it. Lines 102-103 say \"Overspend is stated in words and never dressed up in red, the same way an overdue bill is.\" That is now wrong: MoneyFlow shows the shortfall in `.fact.red` per \u00a7AM (over the limit is red), and the overdue bill's late chip is already red. Suggested replacement: \"Overspend is stated in words; on the page it wears the Colour Key's red (over the limit), as a late bill's chip does.\"",
  "group": "code-18:money:BillSheet.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 23,
  "file": "jarvis-app/src/today/goalPulse.ts",
  "change": "Optional cleanup, not needed for the fix: untouchedLine() (typed middot) has no callers in the app now. It can be removed with its test in goalPulse.test.ts, or left alone.",
  "group": "code-22:today:TodayFlow.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 25,
  "file": "jarvis-app/src/today/welcomeBack.ts",
  "change": "Optional cleanup, not needed for the fix: WelcomeBack.sub (\"Nothing was lost \u00b7 Start with one?\", typed middot) is no longer rendered on Today. adhdPack.test.ts still pins it.",
  "group": "code-22:today:TodayFlow.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 27,
  "file": "jarvis-app/src/today/meetingPrep.ts",
  "change": "Optional cleanup, not needed for the fix: MeetingPrep.line (the typed-middot join) is no longer rendered by Today; only meetingPrep.test.ts reads it.",
  "group": "code-22:today:TodayFlow.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 20,
  "file": "jarvis-app/src/gym/liveCard.ts",
  "change": "Optional cleanup, not needed for the fix: currentLine() (typed middot) no longer has an app caller; only liveCard.test.ts uses it.",
  "group": "code-22:today:TodayFlow.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 608,
  "file": "jarvis-app/src/styles/components.css",
  "change": "Line ~3978: delete `.plan-load.over { color: var(--amber, #FF9F0A); font-weight: var(--w-semi); }`. Nothing applies .over any more, and it was the raw-hex fallback. Optionally trim line ~3977 `.plan-load` to `{ padding: 2px var(--s-1) var(--s-2); font-variant-numeric: tabular-nums; }`, because .facts now sets the size and ink.",
  "group": "code-24:schedule:EventSheet.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 88,
  "file": "jarvis-app/src/styles/components.css",
  "change": "The trailing time on a picked plan row is a tappable button, but `.p3-row.on .p3-time { color: var(--tx-2) }` (~1836) and `.p3-time-btn { ... color: inherit }` (~975) draw it grey, a second grey on the row. Set the picked, tappable time to `var(--tint-on-sheet)` (red words straight on a sheet), keeping `.p3-time-btn.placing` as is.",
  "group": "code-24:schedule:EventSheet.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 84,
  "file": "jarvis-app/src/schedule/planLoad.ts",
  "change": "`loadLine` has no caller outside tests now, and it still bakes ' \u00b7 '. Delete it and its three cases in src/schedule/planning.test.ts (lines ~47, 54, 58), or keep it on purpose. noDeadExport only covers gym/, so nothing fails either way.",
  "group": "code-24:schedule:EventSheet.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 89,
  "file": "jarvis-app/src/schedule/splitSitting.ts",
  "change": "`splitLine` has no caller outside tests now, and it still bakes ' \u00b7 '. Delete it and its cases in src/schedule/planning.test.ts (lines ~108 and ~123).",
  "group": "code-24:schedule:EventSheet.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 92,
  "file": "jarvis-app/src/schedule/planClock.ts",
  "change": "`ClockVerdict.sub` (\"Plan tomorrow instead?\") is no longer rendered. Drop the field, and the `expect(v?.sub)` assertion at src/schedule/planning.test.ts:137.",
  "group": "code-24:schedule:EventSheet.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 148,
  "file": "jarvis-app/src/styles/components.css",
  "change": "Delete the orphaned rule `.dec-meta { font-size: var(--t-meta); color: var(--tx-3); }` (line 3046, under the DECISION RECORD comment). DecisionsFlow.tsx was its only call site. Until it is deleted, src/laws/subtextLaw.test.ts > 'every class that sets a size and a secondary ink is rendered somewhere' fails with `expected [ 'dec-meta' ] to deeply equal []`. No law or other file references .dec-meta, so deleting it is safe. `.dec-old` is still used and stays.",
  "group": "code-27:decisions:DecisionsFlow.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 301,
  "file": "jarvis-app/src/brain/CategoryDetail.tsx",
  "change": "Optional cleanup: line ~1752 `rangeRows[0]!.range.source.split(\" \u00b7 \").join(\", \")` now does nothing, because muscles.ts's source string no longer contains ' \u00b7 '. It can become `rangeRows[0]!.range.source`. What it renders is unchanged.",
  "group": "code-34:gym:LiftDetailScreen.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 53,
  "file": "jarvis-app/src/today/YourDay.test.tsx",
  "change": "Line 59 builds a gym door with meta: \"6 exercises \u00b7 Est 42 min\", and line 63 asserts that string. Change the fixture to facts: { exercises: 6, estMin: 42 } and assert getByText(\"6 exercises\") and getByText(\"Est 42 min\"). Then the @deprecated GymDoorView.meta field and its fallback branch in DayRow.tsx (': gymDoor.meta && <div className=\"sched-cat\">{gymDoor.meta}</div>') can be deleted.",
  "group": "code-35:gym:UploadFlow.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 516,
  "file": "jarvis-app/src/shared/provenance.ts",
  "change": "Export sourceWhen(source, now = Date.now): string | null (the when half of sourceLine, clock time today and shortDateFromMs otherwise), and optionally build sourceLine from it. ProvenanceLine.tsx can then import it and drop its local whenOf() duplicate.",
  "group": "code-35:gym:UploadFlow.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 459,
  "file": "jarvis-app/src/messages/MessagesFlow.test.tsx",
  "change": "The finding is real. SendHold (MessagesFlow.tsx, failed branch) renders `item.error` inside `<div className=\"conn-meta\">`, and sendPump.ts:174 and TodayOutboxPump.tsx:53 fill that from humanError. So the five constants in humanError.ts (lines 18-26) put a hard-coded middot into a meta line. The fix is ready but I did not apply it: this test asserts the exact old strings at lines 1010 and 1042 (\"Your Google sign-in expired \u00b7 Reconnect in Settings\") and at line 1026 (\"You're offline \u00b7 Nothing was lost\"), and I don't own this file. The finding's suggested \". \" wording would also fail src/laws/shortCopy.test.ts, which rejects any rendered string containing \". \" followed by a capital. Apply this in one commit. In humanError.ts: SIGNED_OUT \"Your Google sign-in expired; reconnect in Settings\", NO_ACCESS \"Google refused that; reconnect in Settings to update permissions\", TOO_FAST \"Google is rate-limiting us; try again in a minute\", GOOGLE_DOWN \"Google's mail service is having trouble; try again shortly\", OFFLINE \"You're offline; nothing was lost\". In this test: change lines 1010 and 1042 to \"Your Google sign-in expired; reconnect in Settings\" and line 1026 to \"You're offline; nothing was lost\". The co-located humanError.test.ts uses regexes (sign-in expired, permissions, rate-limiting, trouble, offline) and still passes unchanged.",
  "group": "code-41:connections:ConnectionsPage.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 459,
  "file": "jarvis-app/src/ai/failureLine.test.ts",
  "change": "Same atomic change as the MessagesFlow.test.tsx entry for 459. Line 16: expect \"You're offline; nothing was lost\". Line 17: expect \"Google's mail service is having trouble; try again shortly\". aiFailureLine passes non-proxy errors through humanError, so these assertions follow the humanError.ts constants. Optional: update the comment at src/connections/google/api.ts:55, which quotes the old SIGNED_OUT string. It is a comment only, so nothing renders it.",
  "group": "code-41:connections:ConnectionsPage.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 512,
  "file": "jarvis-app/src/styles/components.css",
  "change": "Optional cleanup: `.mt-sum-value.warn { color: var(--warn); }` (around line 6533) now has no call site, because TrackerScreen's negative Net uses .fact.red. It can be deleted. No law requires this; it only removes dead CSS.",
  "group": "code-44:money:TrackerScreen.tsx",
  "phase": "code",
  "done": false
 },
 {
  "kind": "needs_other_file",
  "id": 515,
  "file": "jarvis-app/src/shared/ProvenanceLine.tsx",
  "change": "Optional cleanup, no visual change: import { sourceLabel, sourceWhen, type Source } from \"./provenance\", and replace the private whenOf(source) (lines 38-46, a copy of provenance.ts's same-day logic) with `const when = compact ? null : sourceWhen(source);`. Then drop the now-unused shortDateFromMs import and change the comment at lines 38-40, which still says sourceLine() 'still joins the two halves for the callers that want one string'.",
  "group": "code-45:shared:provenance.ts",
  "phase": "code",
  "done": false
 },
 {
  "kind": "blocked_by_law",
  "id": 325,
  "law": "src/laws/laws.test.ts:4251-4252 ('every estimate names its evidence'): expect(gym(\"FitSheet.tsx\")).toMatch(/default pace \u00b7 improves as you log/). The same law also pins pacing.ts:4247.",
  "why": "The FitSheet honesty line is a `.conn-meta` meta line with a baked middot ('default pace \u00b7 improves as you log'), so R6 says it should read 'default pace, improves as you log'. The law requires the exact middot text in FitSheet.tsx, so the correct fix fails it. The law needs its regex changed to the comma form (in both FitSheet and pacing.ts) before this part can land. The rest of #325 is applied.",
  "group": "code-33:gym:CondReceipt.tsx",
  "phase": "code",
  "done": false
 }
]